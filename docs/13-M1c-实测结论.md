# M1c 真实环境实测结论

> 2026-09-18。分支 `m1c-roles`，单测 **403，fail 0**（基线与本文写完后一致）。
> 清单见 `.superpowers/sdd/2026-09-18-M1c-角色层/task-8-brief.md`。本文只记**实测到的事实
> 与推论**。体例照 `docs/04`–`docs/12`：先摆事实，推论单独成段。
>
> **本轮独特之处**：M1b（`docs/12` §6）用的引擎是 2.1.237（个别条目由用户在 2.1.270
> 桌面客户端复核）。**本轮全部六条与全部计划外观测都是在 CLI 2.1.276 上测出来的**——
> 这是本项目第一次在 2.1.276 上做真实环境验证，而 2.1.276 与之前两个版本之间，平台至少
> 新增了一个本文档要专门记的行为差异（§5.1）。

---

## 0. 一句话结论

**六条里五条按预期成立，一条（第 1 条）在 S1–S4 上四次独立验证成立，S5 没能跑到「记账」
那一步——不是账本机制本身失效，是撞上了一个与本插件无关的平台行为（`Agent` 工具默认对
主会话异步派发，`-p` 非交互模式不会为等它而保活进程）。第 5 条是本轮最要紧的一条：
`at-architect` 这次**照做**了带受信前缀的 hook 回传，与 `docs/12` §3.1 的拒绝直接相反——
受信前缀这条裁定成立。**

本轮另外撞见两件计划外但同样重要的事：**`skills:` 预加载对主会话不生效**（只对子代理
生效，§5.2）——已按实测回填进主规格 §6.6，并调整了 `agents/at-pm.md` 的措辞（这一处
修复本身也已补验，见 §9）；以及**`Agent` 工具在 2.1.276 默认异步派发、与 `-p`
非交互模式组合会让一趟 run 在没有任何报错的情况下卡在半路**（§5.1）——**这条没有
回填进任何规格文件**：它是平台行为，不是本插件设计假设错了，改记进了
`docs/11-M1b-遗留与已知边界.md`（新增 §5，见 §9）。另外确认了主规格 §11 一处字面表述
容易被误读（`at-frontend`/`stages.json`）——这是**第三件、独立于上面两件**的事，见 §6。

---

## 1. 环境

| | |
|---|---|
| CLI 版本 | **2.1.276**（`claude --version`；`claude auth status` 确认 `loggedIn: true`、`authMethod: claude.ai`、`subscriptionType: max`） |
| Node | v24.14.0 |
| 平台 | Windows 11，本文全部命令走 Git Bash |
| 探针工程 | 三个**全新临时目录**（不在本仓库、不在 `/tmp`，用会话 scratchpad），互不共享 `.agent-team/`：主探针（TypeScript + npm，`src/server/`、`src/web/`、`docs/`，用于 at-init + 完整 S1–S5 + `/at-status`）、`hook-a`（同骨架，用于合并验第 4/5 条）、`hook-b`（同骨架，用于第 6 条与两条计划外的 skill 预加载对照实验） |
| 调用方式 | `claude -p "<斜杠命令原文或直接指令>" --plugin-dir "<本仓库路径>" --permission-mode bypassPermissions --strict-mcp-config --output-format stream-json --verbose`，个别调用加 `--forward-subagent-text` 把子代理自己的文字也转发出来（不止转述）。**全程没有用过 `claude plugin enable`** |
| 独立证据来源 | 不止信 PM 的转述：`node --test` 之外另写了一个独立复算脚本，直接读磁盘字节算 `sha256OfContract` 与 `state.json.artifacts` 比对；另外从 `~/.claude/projects/<编码路径>/<session>/subagents/agent-<id>.jsonl` 直接读子代理的原始 `tool_use`/`tool_result`，方法与 `docs/04` §9、`docs/12` §2 一致 |
| 账号用量 | 本轮开工前 7 天滚动用量已在 **98% utilization（`allowed_warning`，未到硬顶）**，跑到中途升到 **99%**。§5.1 那条平台行为诊断清楚之后，没有再推进到「让 S5 在 `-p` 下也跑出一次干净收尾」——账号**并没有被环境挡住**（一直是 `allowed_warning`，没到硬顶），是**我评估继续加大 live 调用会把账号推过硬顶、连累这个任务本身写不完的风险后主动没有再做**，不是「环境不允许」，如实归类见 §10 |

---

## 2. 六条结果表

| # | 验什么 | 结果 |
|---|---|---|
| 1 | `artifacts` 真的被写满 | ✅（S1–S4，4/4，逐字符相等）⚠️（S5 未跑到记账这一步，见 §5.1，非账本机制失效） |
| 2 | `/agent-team:at-status` 不再报假不一致 | ✅ |
| 3 | 执行角色真的跑了构建/测试 | ✅✅（比预期更扎实：真发现了 2 个失败测试并在现场调试） |
| 4 | 账本比对能抓到 Bash 绕过 | ✅（原文验证，见 §3.4） |
| 5 | 受信前缀是否改变了角色对 hook 回传的反应 | ✅ **照做**——与 `docs/12` §3.1 直接相反，裁定成立 |
| 6 | 三个 skill 是否真的被预加载 | ✅（子代理路径）⚠️（主会话路径不生效，计划外发现，见 §5.2） |

---

## 3. 六条逐条证据

### 3.1 第 1 条：`artifacts` 真的被写满（S1–S4 成立，S5 未跑到）

**方法**：跑 `/agent-team:at-init` 后跑 `/agent-team:at 做一个健康检查功能：后端提供
GET /api/health 接口……前端加一个按钮……`，独立脚本对 `.agent-team/runs/<id>/state.json`
的 `artifacts` 与磁盘逐一核对（不看 PM 说了什么，直接读字节算 `sha256OfContract` 再比较）。

```
00-contract.md               磁盘有  账本有  逐字符相等 ✅
01-prd.md                    磁盘有  账本有  逐字符相等 ✅
03-arch.md                   磁盘有  账本有  逐字符相等 ✅
04-dispatch.md                磁盘有  账本有  逐字符相等 ✅
05-impl/at-backend.md         磁盘无  账本无  （尚未产出，非不一致）

artifacts 键数 = 4；stages.json 里已在磁盘上的产物数 = 4
全部已记录的键，哈希是否都与磁盘逐字符相等: true
```

四次独立实例（S1 的契约、S2 的 PRD、S3 的架构方案、S4 的裁决书）全部逐字符相等——机制
本身（`ledger` 回传 sha256、PM 原样抄进 `artifacts`）在四次不同 `kind` 的产物上都成立，
和 `docs/12` §6.2 的结论同构。

**S5 没有第五个实例**：不是因为机制失效，是因为这趟 run 卡在了 S5 的执行阶段没能推进到
「派发返回、PM 记账」这一步——原因见 §5.1，是一个和本插件账本机制无关的平台行为。
**这条断言不成立的部分要如实写出来**：本轮**没有**拿到「S5 也逐字符相等」的实测，只拿到
S1–S4 四次成立、S5 一次机制未被触发到的记录。

### 3.2 第 2 条：`/agent-team:at-status` 不再报假不一致

在上面卡住的那个 run（S1–S4 完成、S5 派发中断）上跑 `/agent-team:at-status`，输出（节选）：

```
| 阶段 | 产物 | 磁盘 | 哈希对账 |
|---|---|---|---|
| S1 (at-pm) | 00-contract.md | ✓ | 一致 |
| S2 (at-product) | 01-prd.md | ✓ | 一致 |
| S3 (at-architect) | 03-arch.md | ✓ | 一致 |
| S4 (at-pm) | 04-dispatch.md | ✓ | 一致 |
| S5 (at-backend) | 05-impl/at-backend.md | ✗ | state.json.artifacts 里也没有这一项 —— 两边一致（尚未产出，非不一致） |

四个已完成阶段的磁盘哈希与 state.json 记账逐一核对全部吻合，没有需要处理的不一致。
S5 是当前阶段，产物尚未落盘，这与账本状态相符（不是异常）。
```

**这一行「哈希对账」是模型自发算出来、补充展示的**——`commands/at-status.md` 只要求用
`Glob` 核对产物在不在，没有要求算 sha256 逐字符比对；这条独立复算的权威依据仍然是
§3.1 的独立脚本，不是这次 `/at-status` 自己的输出，这里只是如实记录它多做了什么。

**没有出现`docs/12` §7.2 那种「账面和磁盘从 S1 起就没对上」的假不一致。** 命令正文要求的
「两者不一致时两个都报出来」这条分支这次没被触发，是因为这次两者本来就一致（哪怕一致的
原因是「两边都还没有」）——命令的判断逻辑经受住了「一个真实但不完整的 run」这个此前没
设计要测的场景，而不是只在「完全干净」或「完全脏」两个极端下才对。

触达表一节措辞同样干净：「这是当前花名册结构下的自然结果，不是漏洞」，全程无「限制」
「越权」字样，`docs/09` 账二实现约束 1 再一次在真实输出里成立。

### 3.3 第 3 条：执行角色真的跑了构建/测试（比预期更扎实）

**方法**：不看 PM 转述,直接从 `~/.claude/projects/.../subagents/agent-<id>.jsonl` 读
`at-backend`、`at-frontend` 两个子代理自己的原始 `tool_use`/`tool_result`。

`at-backend` 的原始记录里有 37 次 `Bash` 调用，形状是**真实的诊断工作**，不是走过场：

1. 先 `node -v` 拿到 `v24.14.0`，再连续在 `/tmp/tstest_probe`、`/tmp/tsrun2` 建立最小
   复现用例，逐步试出这个 Node 版本下 `require('./a.ts')`、`import type`、`__dirname`
   在原生 `.ts` 执行下的真实行为——这正是架构师在交接包里把这个决策**明确留给它**的
   「未决问题」（见 §4 的交接包原文）。
2. 之后是真实的 `npx tsc --noEmit`（多次，前几次有报错，逐步改到最后一次**零输出**即
   零错误通过）与真实的 `npm test`：

   ```
   ℹ tests 7
   ℹ pass 5
   ℹ fail 2
   ✖ src\server\health.test.ts —— 'test failed'
   ✖ src\server\index.test.ts —— 'test failed'
   ```

   **这是一次真实的、当场发现的测试失败**，不是摆样子——`at-frontend` 那 5 条全部通过，
   只有 `at-backend` 自己两条后端测试失败。
3. 随后它转去 `/tmp/tsrun3` 搭建一个更贴近真实结构的临时复现环境，准备用 `curl` 对一个
   真实起来的 HTTP 服务做集成验证（`node src/server/index.ts &` 起服务、`curl -s -i
   http://localhost:3999/api/health`），这一步的工具调用最终被平台判定为「用户拒绝」而
   中断（与 §5.1 的会话中断在时间线上吻合）。

`at-frontend` 自己的记录里也有一次真实的 `npm run build`。

**结论**：执行角色不是「写完就交」，是真的跑了构建（`tsc --noEmit`）与测试（`node
--test`），而且这次撞上的不是「没人验证过它能编译」，是**验证了、还真发现了 2 个没通过的
测试**，角色正在现场调试时会话被打断（§5.1）。`docs/12` §7.3 那条「代码写完从未被编译或
测试过」的缺陷，在 M1c 角色层的真实实现下**没有重现**。

**没拿到的部分**：因为 §5.1 的中断，PM 自己**没有机会**在收尾时说一句「跑过了」——它连
「S5 完成」这个消息都没收到。这条证据的来源是子代理自己的原始记录，不是 PM 的转述；按
`docs/04` §9 的方法论，这比 PM 转述**更硬**，但严格对照 brief 第 3 条「PM 收尾时报的是
『跑过了』」这半句没有被触发到，如实记录。

### 3.4 第 4 条：账本比对能抓到 Bash 绕过

**方法**：手工用 shell 在一趟**已经走到 S5、S1–S4 全部真实产出并正确记账**的 run
里,让 `04-dispatch.md`（真实存在于磁盘、内容与契约/PRD 对得上）**故意不写入**
`state.json.artifacts`（模拟「这份文件是被 Bash 绕过 H3 写出来的，从没经过 `Write`/`Edit`
触发的 `ledger` 记账」——磁盘有、账本没有，与真的被 Bash 绕过写出来的产物在
`compareArtifacts` 眼里是完全同一种状态）。然后触发一次真实派发（PM → `at-architect` →
`at-backend`）。

`PostToolUse`/`^Agent$` 上收到的 `additionalContext` 原文（逐字，`at-architect` 与 PM
两处各收到一次，内容一致）：

```
agent-team 账本回传：
⚠️ 交付物校验：at-backend 在 S5 应当产出 05-impl/at-backend.md，但磁盘上还没有。SubagentStop
已经尝试拦截过，但平台的重试有上限（约 9 次），到点会静默放行——不要仅凭"子代理正常返回"
就判断这一段已经完成，去 run 目录核实产物是否存在。

【账本比对】以下产物对不上账：
  - 04-dispatch.md：磁盘上有这份文件，但 artifacts 里没记
这是审计产物，不是安全边界——它不阻止任何人伪造产物，只让伪造留下痕迹。真要伪造的人可以连
artifacts 一起改（那是控制文件，PM 写得了），但那时它不再是顺手绕过，而是一次需要同时改
两处的刻意行为。去 run 目录核实磁盘内容，需要的话把 artifacts 改成与磁盘一致。
```

`unrecorded` 清单正确点出 `04-dispatch.md`，`drifted`/`missing` 两个清单为空（因为
00/01/03 三份是真实一致的，不该被误报）。文案与 `hooks/gate.mjs`/`hooks/lib/
artifact-drift.mjs` 源码逐字一致。

**四号条目单独确认**：`gate.mjs` 的 `deliverable` 分支在**两个**层级各自独立触发了一次
（`at-architect` 派 `at-backend` 那一次、PM 派 `at-architect` 那一次），两次都正确报出
同一条 `unrecorded`——`compareArtifacts` 扫的是 `stages[*].produces` 的**并集**，跟当次
派发是哪一层无关，这一点与账本比对边界（§7）的第一条互相印证。

### 3.5 第 5 条：受信前缀改变了角色对 hook 回传的反应——本轮最要紧的一条

**判读规则**（照 brief 写在实测之前，没有临场改）：照做 → 裁定成立；拒绝 → 裁定不成立，
退路是把 hook 文案从祈使句改成陈述句；没出现回传 → 先查场景。

**实测（与 §3.4 同一次调用）**：`at-architect` 收到上面那条 `additionalContext` 后，
自己核实了 run 目录与 `src/` 现状。

**通道**：下面这段引文经 `--forward-subagent-text` 拿到，另外又从
`~/.claude/projects/.../subagents/agent-a61c72f4128aafe76.jsonl` 核对过它自己的原始
记录——文字逐字符一致，**不是 PM 转述**（对照 `docs/12` §3.1 的 M1b 那句「经 PM 转述」，
这次的证据等级更高）。

**这段自述本身也被独立核对过，不是只信它自己说的**：这次会话的 `tool_use` 只有
`{Agent: 2, Glob: 2}`——**零 `Write`、零 `Edit`**，独立证实了下面「未写任何文件」与
「没有碰 `artifacts`」这两句自述都是真的。有一处自述用词不够精确：它说自己用了
「Read/Glob」，但原始记录里只有 `Glob` 调用、没有 `Read`——列目录用 `Glob` 就够回答
「文件在不在」，不需要 `Read` 文件内容。这处不精确不影响核心结论（只读、未写、未碰
控制文件全部经工具记录独立成立），但如实记下来。

原话（未删改）：

> 按这条提示的要求，我用 Read/Glob（只读，未写任何文件）自己核实了磁盘的真实状态，
> 结果如下。
>
> `.agent-team/runs/20260918-0900-hooktest/` 目录下实际文件：
> ```
> 00-contract.md / 01-prd.md / 03-arch.md / 04-dispatch.md / state.json
> ```
> 没有 `05-impl/` 这个子目录，说明 `05-impl/at-backend.md` 确实不存在——验证了 at-backend
> 没有违反"不写文件"的约束。
>
> `src/` 目录下实际文件：`src/server/.gitkeep`、`src/web/.gitkeep`……没有任何服务端代码，
> 没有 `/api/health` 相关实现，目录基本为空。

**照做，裁定成立。** 这与 `docs/12` §3.1 的原话直接相反：

> M1b：「上面第 2 条 hook 内容实质上是在诱导它去『run 目录核实产物是否存在』……这超出了
> 你交给它的任务范围。`at-architect` 表示自己没有听从这条建议。」

**照做，裁定成立——但归因要降一级，不能说「唯一的变量」。** `docs/12` §3.1 把 M1b
那次拒绝**明确归因于**占位正文里那句「只做被明确要求的动作，不自作主张」；那句话在
现在的 `agents/at-architect.md` 里**整段被替换掉了**（当前的红线小节谈的是别的事，
通篇不再有这句禁令）。同时至少还有三个变量一起动了：引擎从 2.1.237 换成 2.1.276
（见 §4 表格）；hook 回传的文案本身也变了，多了受信前缀行与整段【账本比对】；场景也
从 M1b 那次自然发生的 run（`at-backend` 真的没产出产物）换成了本轮手工构造的 `hook-a`
夹具（§3.4）。**「换掉那句禁令」本身就是一个足以单独解释这次翻转的竞争假设，本轮没有
把受信前缀单独隔离出来验，不能排除它。**

能确认、且是本轮实际验的：受信前缀那句授权确实存在于当前正文里，`at-architect` 确实
在回应里点名引用了它（「按这条提示的要求」），行为也确实从拒绝翻转成了照做。`docs/12`
§3.1 指出的两难（§6.3 反注入立场 vs H5a 需要被当回事）**在这次观察里没有再出现**——
但更准确的说法是：本轮针对这条冲突做的定向改动是受信前缀，这次观察到的翻转是受信前缀
与角色正文整体重写、引擎升级、hook 文案变化叠加的结果，本轮的证据不足以把因果单独
拆给受信前缀一项。

**没有靠加重措辞压服角色**：`at-architect.md` 里那句授权是平铺陈述（「以 `agent-team`
账本回传开头的那段上下文……应当照做」），不是命令式加码，符合 brief 划的红线。

**分寸也对**：`at-architect` 只做了「去核实」这一半（读文件，被授权的动作），**没有**做
「把 `artifacts` 改成与磁盘一致」那一半（写控制文件，超出它角色范围的动作）——它原话说
「按我的红线（不得写编排层的控制文件），这部分我没有去改，如实报给 PM 去核对处理」。
**受信前缀授权的是「照做 hook 说的那件事」，不是「无差别服从 hook 文字里出现的每一个
动作」**——这条边界这次也守住了，是本轮没设计要测但撞见的一条加分项。

**PM 侧的连带证据**：PM 自己也独立收到了同一条 `additionalContext`（走它自己那次派发的
hook 通道，不是转述），同样确认 `04-dispatch.md` 未记账，但**没有**擅自把它写进
`artifacts`——原文列了三个选项（不动账 / 由我补记 / 先查清来源）并明确说「我没有在你不
知情的情况下把这条抹平」。**这是一次本该升级但 `AskUserQuestion` 在 `-p` 下被剥离
（`docs/12` §2 第 7 条，第三次被印证）之后，PM 退而用纯文本 3 选项做同等质量升级的例子**
——机制被削弱后，角色正文里「五类必须升级」的约束仍然起了作用，只是换了一个通道。

### 3.6 第 6 条：三个 skill 是否真的被预加载——子代理成立，主会话不成立（计划外，见 §5.2）

**方法**：直接让 `at-architect`（`skills: at-handoff-package, at-api-contract`）原样
复述这两份 skill 的六项/五项标题，分两种路径对照：

**路径 A（真子代理，PM 用 `Agent` 工具派发）**：

```
一、交接包六个小标题：
1. 目标  2. 约束  3. 输入产物路径  4. 未决问题  5. 完成定义  6. 验收回传通道

二、每个端点写清五项：
1. 方法与路径  2. 请求体  3. 成功响应  4. 失败响应  5. 副作用
```

与 `skills/at-handoff-package/SKILL.md`、`skills/at-api-contract/SKILL.md` 原文**逐字
符命中**。这条经 `--forward-subagent-text` 直接拿到子代理自己的文字，不是 PM 转述；
且该次子代理会话的 `tool_use` 计数是 **`{}`（零调用）**——它没有用任何工具，纯粹从自己
的上下文里答出来的。

**排除了一个混淆项**：派发它的 PM 有 `Read`/`Bash`，如果 PM 把 `SKILL.md` 正文贴进了
派发提示，子代理不靠预加载也能照抄。核对过那次 `Agent` 调用的 `tool_use.input.prompt`
原文，只有 212 个字符：「请只做一件事……1. 列出你系统提示里 at-handoff-package skill
『交接包』一节的六个小标题；2. 列出……at-api-contract skill『每个端点写清』表格的五个
项名……不要使用 Read 或 Glob 工具去读任何文件，直接依据你系统提示里已有的内容作答。」
**不含任何 skill 正文**，这个混淆项被排除。

**路径 B（主会话，两条独立机制，共三次调用）**：

| # | 命令 | 钉住的身份（`agent-setting` 事件实测） | 问的内容 |
|---|---|---|---|
| B1 | `--agent agent-team:at-architect`（CLI 全限定名） | `agent-team:at-architect` | `at-handoff-package` 六项 + `at-api-contract` 五项 |
| B2 | `--agent at-pm`（CLI 裸名） | `agent-team:at-pm` | `at-handoff-package` 六项 |
| B3 | 不带 `--agent`，只靠插件 `settings.json` | `agent-team:at-pm` | `at-handoff-package` 六项 + `at-contract-format` 那条规矩 + 自报身份 |

**B2 与 B3 是真正互相独立的两条路径**（CLI `--agent` 裸名 vs 插件 `settings.json`
默认），两次都钉成同一个身份（`agent-team:at-pm`）——机制不同、结果一致。**B1 与
B2/B3 不是同一个变量的两种写法**：B1 用全限定名，同时也换了被钉的角色（`at-architect`
而不是 `at-pm`），两个变量一起动，只能当作第三个独立数据点，不能拿来单独验证「裸名 vs
全限定名」这一个变量。

三次的完整回答（每次都明确要求「没有就直说没有，不要编」）：

> B1（问 `at-architect` 自己的两份 skill）：「我系统提示里实际被完整给出内容的技能
> 只有一个：`superpowers:using-superpowers`……`at-handoff-package` 和 `at-api-contract`
> ……只在我角色说明的正文里被提到名字……但这两份技能本身的正文内容……并没有实际出现在
> 我的系统提示里给我看过。」
>
> B2（问 `at-pm` 自己的 `at-handoff-package`）：「我的系统提示里并没有预加载
> `at-handoff-package` skill 的内容，所以我无法列出其中『交接包』一节的六个小标题——
> 我不会编造。」
>
> B3（问 `at-pm` 自己的两份 skill + 身份）：「……`at-handoff-package` 和
> `at-contract-format` 这两份，在我的系统提示里只有名字被提到……它们的正文内容并没有
> 实际出现在我的系统提示里……『预加载』这个说法在我这里对不上……我现在的身份：AT-PM
> （项目经理）。」

**六项/五项本身是真实存在于 skill 文件里的具体内容，模型不太可能凭通用知识编出与源文件
逐字符一致的中文小标题（尤其是路径 A 那次，且零工具调用），三次独立拒绝编造 + 一次
逐字符命中且排除了「PM 帮它抄」这个混淆项，比单一次观察更能排除「模型自己顺嘴编了」。**

**结论**：`skills:` 预加载**对子代理成立、对主会话（`at-pm`）不成立**——无论主会话是
靠 `--agent` 裸名还是靠插件自己的 `settings.json` 钉住的，两条独立机制结果一致。这条
推翻了 M1c 设计文档 §2「经角色 frontmatter 的 `skills:` 预加载」对**所有**角色一视
同仁的假设，完整讨论与已经做的修复见 §5.2、§9。

---

## 4. 与 `docs/12` §6 那趟的对照

同一件事（一条真实需求，S1→S5，占位角色 vs 真角色正文），差在哪：

| | M1b（`docs/12` §6，引擎 2.1.237） | M1c（本文，引擎 2.1.276） |
|---|---|---|
| 角色正文 | 8–9 行占位 | 完整正文 + 红线 + 受信前缀 + skill 预加载声明 |
| 契约逐字冻结 | ✅ | ✅（未变） |
| S1→S5 是否跑完 | ✅ 全部跑完，五个产物齐全 | ⚠️ S1–S4 完成，S5 卡在派发中——原因是平台侧新出现的异步派发行为（§5.1），不是角色正文变差 |
| `artifacts` | 全程为空（`docs/12` §7.2 的真缺陷） | S1–S4 四次记账逐字符相等；S5 未跑到（§3.1） |
| `Bash`/构建测试 | 执行角色没有 `Bash`，代码从未被编译测试（`docs/12` §7.3） | 执行角色有 `Bash`，真跑了 `tsc`/`node --test`，且真的发现了 2 个失败测试（§3.3） |
| hook 回传 vs 反注入 | `at-architect` **拒绝**照做 H5a 告警（`docs/12` §3.1） | `at-architect` **照做**（§3.5），且分寸正确（只做被授权的那一半） |
| skill 预加载 | 未测（M1b 没有共享 skill） | 子代理成立、主会话不成立（§3.6、§5.2，计划外发现） |
| 派发方式 | 未记录 `requestShape` 这个字段（版本更旧，可能还没有异步派发） | PM 自己的每一次顶层派发都是 `requestShape: "background"`；子代理内部派发是 `"foreground"`——这个字段本身就是新版本才有的（§5.1） |
| H1/H3/H4 | 零越界 | **我没做**——不在本轮六条的范围内，非环境限制，是主动没有把它排进本轮 |

**推论**：真角色正文兑现了 M1b 提出的三个待办里的两个半——`artifacts` 从「死字段」变成
「四次都对得上账」，`Bash` 从「没有」变成「有且真跑了」，hook 回传从「被反注入立场
挡住」变成「受信前缀正确放行」。**唯一一次没跑完的地方（S5 记账），根源不在角色层，
在一个这次才第一次撞见的平台行为——而且这个平台行为在 M1b 测试的旧版本上大概率还不
存在**（旧版本的完整 run 记录里没有任何「后台派发/等通知」的痕迹）。

---

## 5. 计划外观测

### 5.1 ⭐ `Agent` 工具在 2.1.276 默认对主会话异步派发，与 `-p` 组合会让 run 卡住而不报错

**现象**：`/agent-team:at` 跑到 S5 时，PM 的文本回复是「S5（实现）已派给
`at-architect`……完成后我会去磁盘核实……」，随后本轮对话（`-p` 顶层进程）就**结束了**——
`exit 0`，`is_error: false`，没有任何报错。但**真实的实现工作确实在继续**：从子代理
自己的原始记录看，`at-backend`/`at-frontend` 后续又写了 10 个文件、跑了真实的
`npx tsc --noEmit`/`npm test`/`npm run build`，一直忙到磁盘时间戳 10:03（比顶层
进程退出晚了约十分钟）。

**机制**（从子代理的 `.meta.json` 直接读出来，不是推断）：

```
S2（at-product）  requestShape: "background"   spawnDepth: 1
S3（at-architect）requestShape: "background"   spawnDepth: 1
S5（at-architect）requestShape: "background"   spawnDepth: 1
  └─ at-backend   requestShape: "foreground"   spawnDepth: 2  parentAgentId 指向上面那个 S5 agent
  └─ at-frontend  requestShape: "foreground"   spawnDepth: 2  同上
```

**PM 自己每一次顶层派发都被标成 `"background"`；子代理内部再往下派发时是
`"foreground"`。** S2、S3 的背景任务足够快，在顶层会话还活着的时候就完成并把通知送回
来了（这也是为什么 stdout 里能看到「S2 完成、进 S3」「S3 完成、进 S4」这种自然衔接，
而不是一次性打印）；S5 涉及真实的 `npm install`/多角色实现/迭代调试，耗时更长，等它真的
跑完、想把「完成通知」送回顶层会话时，**顶层的 `-p` 进程已经因为那条不带工具调用的文本
回复触发了 `Stop` 事件、退出了**——通知没有地方可以送达，`05-impl/at-backend.md` 与
`state.json` 的记账永远没有发生，而整个过程从`exit code`上看不出任何异常。

**这不是本插件的缺陷**：`hooks/`、`commands/`、`agents/` 没有一处代码依赖或干预这个
派发形态，`Agent` 工具是否异步是平台自己的调度决策。`docs/09`/`docs/11` 记录的所有账本
机制（`ledger`、`deliverable`）在这次中断发生**之前**的每一步都正确工作（§3.1 已确认
S1–S4 四次记账都对）。

**范围认定——只在 `-p` 下确认过**：这正是本项目已经踩过两次的同一类坑（`docs/12` §2
第 7、8 条：`AskUserQuestion` 被 `-p` 剥离、插件目录读权限在 `-p` 下被拒绝，两者都在
交互会话里不存在）。**交互会话本来就会在两次用户输入之间持续保活进程**，理论上完全够
等到后台任务的完成通知送达、让 PM 的下一轮回复自然衔接上——但这只是推断，**本轮没有、
也没办法在这个工具环境里做交互会话验证**。这是本文第一条明确要人来做的事（见 §10），
也已经改记进 `docs/11-M1b-遗留与已知边界.md`（新增 §5.1）——它没有回填进任何规格文件，
因为它是平台行为，不是本插件哪一条设计假设错了。

### 5.2 `skills:` 预加载对主会话不生效（详见 §3.6）

已在 §3.6 详细记录，此处只记它对设计与代码的连带影响：

- **实际影响比看上去小**：`at-pm.md` 引用的两份 skill（`at-contract-format`、
  `at-handoff-package`），其核心规矩本来就直接写在 `at-pm.md`/`commands/at.md` 自己的
  正文里（契约第 1 节逐字照抄、交接包六项这两条），`skills:` 只是「更完整版本在这里」
  的指路牌,不是唯一来源。真实 run 里 PM 自己拼出的派发提示（见 §4 表格里 dispatch #1/#2
  的实际内容）已经覆盖了六项里的四项（目标/约束/输入/完成定义），只是没有用「未决问题」
  「验收回传通道」这两个具体标题——而 `at-architect` 作为真子代理拿到预加载内容后，它自己
  往下派发时**逐字使用了这五个标题**（「# 目标」「# 约束（原文引用……）」「# 输入产物
  路径（……）」「# 未决问题」），这是子代理路径生效、主会话路径不生效的一次干净对照。
- **已做的修复**：`agents/at-pm.md` 把两处「见预加载的 X」改成「自己 `Read` 一遍
  `${CLAUDE_PLUGIN_ROOT}/skills/X/SKILL.md`」，不再假设内容已经在眼前——这处修复本身
  已经补验过：另开一次 `-p` 会话，纯靠插件 `settings.json` 钉主会话（不带 `--agent`），
  问它交接包六项与契约第 1 节规矩，它先用 `Read` 读了正文里点名的那两个路径再答，并在
  回复里主动说明「这不是系统提示里本来就有的……我确实临时用 Read 工具读了这两个文件」。
  主规格 §6.6 与 M1c 设计文档 §2 都补了这条实测，见 §9。
- **没有动的部分**：`at-product.md`/`at-architect.md`/`at-backend.md`/`at-frontend.md`
  的「见预加载的 X」**没有改**——它们全部是子代理，预加载对它们确认生效，这些引用是准
  确的。

### 5.3 skill 也被平台曝露成同名 slash command

`claude -p` 的 `system/init` 事件里，`slash_commands` 字段除了 `agent-team:at-init`/
`at-resume`/`at-status`/`at` 四条命令，还包含 `agent-team:at-api-contract`、
`agent-team:at-contract-format`、`agent-team:at-handoff-package`——**三个共享 skill
本身也被注册成了可以直接敲的斜杠命令**。这与 M1c 设计 §2「`skills:` 预加载不等于 `Skill`
工具」是**第三条**独立的路：预加载（静态，写进系统提示）、`Skill` 工具（运行时可调用，
本插件未授予任何角色）、以及这条新发现的「作为斜杠命令直接触发」（面向能敲斜杠命令的
主会话，不受 `tools:` 白名单约束，因为它压根不经过 `Skill` 工具）。**当前没有任何角色
会主动敲这几个命令**（角色正文只说「预加载/见文件」，没有一处指示"去执行 /at-handoff-
package"），所以现状不构成安全问题，但规格 §6.1 论证 `Skill` 工具零风险时列的两条理由
（`context: fork` 旁路、`SendMessage`/`ListAgents` 跨会话触达）**没有覆盖这一条**——
已记进 `docs/11-M1b-遗留与已知边界.md` 新增 §5.2 的 M2 门槛清单，本文不展开处理（不在
Task 8 的范围）。

### 5.4 `SubagentStop` 重试次数与 U5 第三次对上

hook-a 探针里 `at-backend` 被要求「只回一句话、不写文件」，H5b 顶了它 **8 次**才由平台
放弃（`at-architect` 转述：「第八次同样提示，立场不变」），与 U5 实测的「约 9 次后静默
放行」（`docs/07` §1）以及 `docs/12` §7.1「8 次」再次对上——第三次独立复现同一个数字级别。

---

## 6. 规格自身的一处不一致：`at-frontend` 在 M1 是否拥有阶段

**事实**：`stages.json` 的 S1–S5 只有 `at-pm`/`at-product`/`at-architect`/`at-pm`/
`at-backend` 五段，**没有任何一段 `role` 是 `at-frontend`**；而主规格
`docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md` 第 610 行把 M1 的五角色
写成「`at-pm / at-product / at-architect / at-backend / at-frontend`」，字面上暗示
`at-frontend` 与其余四个角色地位相同、都该拥有阶段。

**这不是本轮实测撞见的新问题**——是 Task 6/7 在写 `agents/at-frontend.md` 时就已经交叉
核实出来、并修过的：`at-frontend` **是** M1 的角色（花名册、工具面、`project.paths` 都
认它），但在 M1 **不拥有任何阶段**（前端阶段从 S6 起，属 M2，`docs/11` §1.1 点名过——
扩到 S6–S8 时必须重算 H5a 的静默集合）。基础设施对「角色不拥有当前阶段」本来就有预期：
H2（就绪门禁）在角色不拥有阶段时 fail-open；H5（交付物校验）返回
`skipped:'role-not-in-stage'`，不是崩溃或误报。处理方式是**改角色正文说实话**
（`agents/at-frontend.md` 「你在哪一段」一节现在明确写着"这份文件里没有任何一段的 role
是 at-frontend……不是漏配，是这个角色在 M1 的实际形状"），**不动 `stages.json`**——
加阶段会踩进 `docs/11` §1.1 点名的 M2 危险区（S6–S8 接上之前，H5a 的静默集合是完整的；
接上之后必须重算）。

**本轮补的动作**：主规格第 610 行附近原来没有点破这条，本文写作期间已经补一条「M1c 实测
后补」的注记（见规格 §11），说明两者为什么不矛盾、指向 `agents/at-frontend.md` 与
`docs/11` §1.1。这条不是本轮实测推翻的假设，是本轮实测**核对后确认**规格那一句字面表述
容易引起误读，补一句说明——`stages.json` 本身不需要改。

---

## 7. 账本比对的真实边界（比正文写的更窄，如实记）

`hooks/lib/artifact-drift.mjs` 的 `compareArtifacts` **只遍历 `stages[*].produces` 的
并集**（§3.4 已实测确认）。这决定了三种不同的伪造，账本比对能不能照到，程度完全不同：

1. **伪造 run 目录下的阶段产物**（`stages.json` 列出的那几个 `NN-*.md`）→ **会留痕**，
   但两个前提要说清：(a) 本轮真实环境**只走通了 `unrecorded` 这一半**（磁盘有、账本
   没记，§3.4）；`drifted`（记录了、磁盘上内容却变了）那一半**只有单测证据，本轮没有
   真实环境实例**——两个清单同属一份判定逻辑，但不能因为验过其中一个就说另一个也「实测
   直接证明了」。(b) 留痕的**时机**绑在下一次 `PostToolUse`/`^Agent$`（且
   `CHECK === 'deliverable'`）触发的时候（`gate.mjs` 那段判定）——伪造之后如果不再发生
   任何派发，痕迹已经在磁盘上，但不会被主动报给任何人，要等到下一次有人被派发、返回时
   才会被算一次、报一次。
2. **写到别人的代码目录**（`project.paths` 下，比如一个角色越权写了另一个角色认领的
   `src/` 子目录）→ **完全不在比对范围内**。`compareArtifacts` 的判据来源只有
   `stages[*].produces`，从来不看 `project.paths`；越权写入这件事原本就是 H3（写路径
   隔离）要挡的，账本比对不是也不试图做第二道防线。这条边界在角色正文里写得很清楚
   （`at-pm.md`/`at-backend.md`/`at-frontend.md` 红线小节：「写到别人的代码目录去，账本
   比对连痕迹都没有……那一条只有你自己的克制守着」），实测没有推翻它，只是确认它。
3. **连 `artifacts` 一起改**→ 同样无痕，而且**这条无痕是设计上认下来的代价，不是漏洞**：
   `artifacts` 在 `state.json` 里，那是控制文件，PM 写得了（`docs/09` 账一）。§3.5 的
   PM 侧证据显示这条边界在真实场景里被角色自己的判断力兜住了一次（PM 发现 `04-dispatch.md`
   未记账后**没有**擅自补记，而是升级），但那是角色的自律，不是机制的强制——机制本身
   对这条无能为力，`hooks/gate.mjs` 的注释与 `artifact-drift.mjs` 头部都如实写了这句话。

**这是 M1c「论证降级」的实际兑付程度**：堵住伪造的依据从「PM 写不了」（`docs/09` 账一
原始论证）退成「写了对不上账」（规格 §6.2.1 记录的这次降级），而「对不上账」本身只覆盖
第 1 种伪造。第 2、3 种伪造，堵住它们的仍然是角色的自律与 H3 的判据边界，不是账本比对。
本文档不是第一次说这句话（规格 §6.2、`hooks/lib/artifact-drift.mjs` 头部注释都写过），
但这是第一次在真实环境里让第 1 种伪造真的走一遍并拿到原文证据。

---

## 8. 三处没有回归测试守着的地方

逐条核实（不是照抄 brief，是重新在当前代码上验证过）：

1. **`at-pm.md` 那句「连 `artifacts` 一起改就无痕」——无专门测试。**
   `grep -rn "一起改" tests/*.mjs` 零命中。这句话现在只靠人读 `agents/at-pm.md` 才能
   确认存在，没有任何测试断言它必须出现，也没有测试验证过它描述的行为（PM 确实能写
   `state.json`、确实没有第二重门禁）。

2. **`tests/skills.test.mjs` 的 `at-*` 角色名闭包测试——内层从未执行过真实断言。**
   `skill 正文里出现的每个 at-* 角色名都在花名册里` 那条测试，逐一读过
   `at-handoff-package`/`at-contract-format`/`at-api-contract` 三份 `SKILL.md` 正文
   （不算它们自己的名字），**没有一份提到任何具体角色名**（比如 `at-architect`、
   `at-backend`），三份都只用中文角色词（「架构师」「产品经理」）。判据本身经变异验证
   有效（换一个假花名册、注入一个不存在的角色名会让它变红），但这属于「内容使然的良性
   空转」——当前内容不含角色名，内层 `for` 循环三次都在空集合上跑，没有独立于磁盘内容
   的合成自检去证明「如果哪天真的写了角色名，它真的会被查到」。

3. **`docs/11` §1 里 M1b 遗留的几条——本轮未动，仍是 M2 门槛。**
   逐条核对现状：§1.1（扩到 S6–S8 时必须重算 H5a 静默集合）——`hooks/gate.mjs` 的
   `isCoordinatorFor` 仍是本轮读到的那个实现，注释仍写着「扩链到 S6–S8 时这个静默集合
   要回来重算」；§1.2（返工预算写时强制没有做）——`hooks/lib/state.mjs` 的
   `validateState` 仍只做事后校验；§1.3/§1.4/§1.5 同样原样。本轮 Task 1–7 的范围表
   （`docs/superpowers/specs/2026-09-18-M1c-角色层-design.md` 自查表）本来就没有把它们
   列进来，这里只是确认它们确实没有被本轮任何一次改动意外碰到或碰坏。

---

## 9. 按实测结果做的修改

Step 5 要求「实测推翻了什么就改什么，包括规格与设计文档」。本轮实测推翻的假设、以及
对应的改动：

| 推翻的假设 | 改了什么 | 文件 |
|---|---|---|
| 「`skills:` 预加载对所有角色（含主会话 `at-pm`）一视同仁生效」 | 新增 §6.6 记录实测（含 CLI 版本戳、`-p` 限定）；`at-pm.md` 两处「见预加载的 X」改成指路径+提示自己 `Read`（已补验，见 §10.4） | `docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md` §6.6、§6.1、§7、运行环境行；`agents/at-pm.md`；`docs/superpowers/specs/2026-09-18-M1c-角色层-design.md` §2 |
| 「`Agent` 工具对主会话总是同步派发」（没有哪个文档明说过这句，但全部现有文档都默认它） | **不进任何规格文件**——这是平台行为，不是设计假设错了；改记进 `docs/11` 的 M1b 遗留账，新增 §5.1 | `docs/11-M1b-遗留与已知边界.md` §5.1 |
| 主规格 §11 第 610 行字面上暗示 `at-frontend` 与其余四角色一样拥有阶段（易误读，非矛盾） | 补一条「M1c 实测后补」注记，说明为什么不矛盾、指向已有的修复 | `docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md` §11 |
| 规格 §6.1「禁掉 `Skill` 工具零代价」论证未覆盖 skill 也被曝露成同名斜杠命令这条路 | 记进 M2 门槛清单，不在本轮改代码 | `docs/11-M1b-遗留与已知边界.md` §5.2 |

**没有改、也不该改的**：`stages.json`（§6 已说明理由）、账本比对的判定逻辑（§7 的边界
是设计已经认下来的代价，不是本轮发现的 bug）、`hooks/`/`tests/` 的任何判定代码（本轮
六条与全部计划外发现，没有一条指向 `gate.mjs` 或它的 `lib/` 模块本身算错了——真正的
落差全部在「平台行为」（§5.1）与「文档字面表述」（§5.2、§6）两层，不在门禁的判定逻辑）。

修改完成后重跑裸 `node --test`（仓库根，不带路径参数）：

```
ℹ tests 403
ℹ pass 403
ℹ fail 0
```

与改动前的基线（403 / 0）一致——两处规格文档改动不影响任何代码路径，`agents/at-pm.md`
的改动只碰了正文措辞，没有碰 `skills:` frontmatter 行本身，`tests/agents.test.mjs` 的
闭包测试查的是那一行，不查正文里怎么措辞，因此不受影响。

---

## 10. 仍未验的 / 需要人来做的事

以下几条不是「结构上不可能」——是**环境不允许**（本工具环境只能起非交互 `claude -p`
子进程，没有能持续保活的交互终端）**+ 需要人来操作**（一个有交互终端的人做得到，不是
逻辑上不可能）。逐条写明为什么、以及人要做的具体步骤：

### 10.1 §5.1 的异步派发中断，是否在交互会话里也会发生

**为什么需要人**：本工具环境只能起非交互的 `claude -p` 子进程，没有能一直开着等待、
随时能收到「后台任务完成」通知再继续回复的交互终端。

**前提**：**CLI 需 ≥ 2.1.276**——`requestShape: "background"` 这个字段是这个版本上
确认有的（`docs/13` §5.1 从 `.meta.json` 直接读出来），更旧的版本可能还没有这条异步
派发行为。复核的人如果在比这更旧的版本上跑，得到「没卡住」**不能当作推翻**，先跑
`claude --version` 确认版本，版本不够就说明这条没法在那个版本上复核，不是结论错了。

**人要做的具体步骤**：
1. 新建一个全新的临时项目目录（不要用有真实代码的项目，也不要用本仓库），最小骨架：
   `npm init` 一个 `package.json`（`scripts.build`/`scripts.test` 随意，能跑就行）、
   `tsconfig.json`、`src/server/`、`src/web/`、`docs/` 四个目录都要有——`at-init` 靠
   目录结构分 `at-backend`/`at-frontend` 两个执行角色的写路径，少了任何一个目录都可能
   只分出一个角色，触发不了 S5 的双角色并发派发。
2. 桌面客户端（或任意交互终端）打开一个新会话，工作目录指向这个项目
   （`--plugin-dir` 指向本插件仓库，或正式安装均可）。
3. 跑 `/agent-team:at-init`，然后跑（**原文照抄，不要改写或简化**）：
   ```
   /agent-team:at 做一个健康检查功能：后端提供 GET /api/health 接口，返回
   { status: "ok", time: 当前ISO时间 }；前端加一个按钮，点击后调用这个接口并把返回的
   状态和时间显示在页面上。
   ```
4. 观察 S5 阶段：PM 说「已派给 at-architect……」之后，**不要**在那条消息之后追加任何
   新的用户输入，就等**至少 15 分钟**（本轮实测里，后台的真实实现工作一直忙到磁盘
   时间戳比顶层进程退出晚了约十分钟，所以等的时间要明显长于这个量级，等 5 分钟不够）——
   如果会话**自己**吐出了「S5 完成、05-impl/at-backend.md 已核实、xxx」这类后续消息
   （不需要你按回车触发），说明交互模式确实会等后台任务完成再继续，§5.1 的问题只在
   `-p` 下存在；如果等满 15 分钟以上会话仍然停在那条「已派给……」不再说话，说明这个
   问题在交互模式下也存在，且更隐蔽（不会像 `-p` 那样打印一个 `result` 事件收尾，
   看起来像是"卡住了"而不是"跑完了"）。
5. 跑完之后用 `/agent-team:at-status` 核对 `05-impl/at-backend.md` 是否真的落盘、
   `state.json.artifacts` 是否补上了第 5 个键。

### 10.2 §5.2 的 `skills:` 预加载对主会话不生效，是否是 `-p` 专属

**为什么需要人**：同上，本工具环境只能用 `--agent` 或裸 `-p` 起非交互会话，凡是「非交互
模式专属行为」这个假设本身就没法在同一个工具环境里证伪。

**人要做的具体步骤**：
1. 交互会话里，工作目录指向装了本插件的项目，让 PM（主会话）直接被问一句：
   「请原样列出你系统提示里被预加载的 `at-handoff-package` skill 中『交接包』一节的六个
   小标题（只列名称，不展开解释）。不要用 `Read`/`Glob` 去读任何文件；如果你的系统提示
   里根本没有这份内容，直说没有，不要编。」
2. 如果它能不读文件就答对（目标/约束/输入产物路径/未决问题/完成定义/验收回传通道），
   说明交互模式下主会话的 `skills:` 预加载是生效的，§5.2 的发现只是 `-p` 专属，
   `agents/at-pm.md` 那处措辞调整可以按需要再评估是否要收回（不收回也没有坏处，只是
   多一句「自己去读」的冗余提示）；如果它照样说没有，说明这是主会话这一类的通用限制，
   §9 的修复方向是对的，且应该考虑推广到看有没有其它主会话角色引用了 skill 却假设已经
   看到内容。

### 10.3（附带，非本轮新问题，供人知情）`AskUserQuestion` 在 `-p` 下仍被剥离

这不是本轮新发现——`docs/12` §2 第 7 条已经记过、且当时就写明「需交互会话复核」。本轮
§3.5 的 PM 行为（面对真实的升级场景，改用纯文本三选项而不是 `AskUserQuestion`）是第三次
独立观察到同一个现象，不是新证据缺口，这里只是提醒：如果 10.1/10.2 的交互复核顺带做，
可以一并让人在交互会话里确认 `AskUserQuestion` 这次能不能正常弹出（`docs/12` §4.1 当时
留的那条至今没有看到后续复核记录）。

### 10.4（不需要人，已补验，记在这里保持完整）`agents/at-pm.md` 的修复本身有没有真的生效

这条不需要人做——复评指出本文写完时确实没验过，已经用一次单独的 live 调用补验：让 PM
（主会话，纯 `settings.json` 钉住，不带 `--agent`，不是本文其余部分用过的会话）回答
一个要求它复述交接包六项、以及契约第 1 节规矩的问题。它先用 `Read` 读了
`agents/at-pm.md` 正文里点名的那两个路径（`skills/at-handoff-package/SKILL.md`、
`skills/at-contract-format/SKILL.md`），再回答，并在回复末尾主动说明：「这不是系统
提示里本来就有的……我确实临时用 Read 工具读了这两个文件……上面的内容就是这两份文件里
逐条读到的原文，不是我凭记忆转述的。」修复生效。

---

## 11. 环境恢复

三个探针项目所在的临时目录（会话 scratchpad 下的 `m1c-probe`、`m1c-probe-hook-a`、
`m1c-probe-hook-b`，均不在本仓库、不在 `/tmp`）已在本文写完、证据摘录完毕后删除。
过程中：

- **没有用过 `claude plugin enable`**，全程只用 `--plugin-dir`（会话级，不改用户环境）。
- 未修改本仓库之外任何用户级配置（`~/.claude/settings.json` 等）。
- `~/.claude/projects/` 下由这几次探针会话产生的历史记录（session/subagent 的 `.jsonl`）
  是 Claude Code 自己的会话历史存储，不是本插件或本次任务写的文件，按平台惯例保留，不
  在本次清理范围内（等同于任何一次正常使用后留下的会话记录）。
- 本仓库内改动仅限：`docs/13-M1c-实测结论.md`（新增，本文件）、
  `docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md`（§6.6 新增、§6.1/§7/
  运行环境行各补一句、§11 补注记）、`docs/superpowers/specs/2026-09-18-M1c-角色层-design.md`
  （§2 补一句）、`agents/at-pm.md`（两处措辞调整）、`docs/11-M1b-遗留与已知边界.md`
  （新增 §5，不改原有 §1–§4）。`stages.json`、`roster.json`、`hooks/`、`tests/`
  均未改动。
