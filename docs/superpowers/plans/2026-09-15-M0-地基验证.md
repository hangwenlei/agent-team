# agent-team M0 地基验证 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建出 agent-team 插件的真实骨架，并用它回答规格 §10 的四条未知 U1–U4；任一为否则停下改设计，不进 M1。

**不在本计划范围内**：规格 §4 阶段链、§5 升级条件、§8 交接契约，以及未知 U5（`SubagentStop` exit 2 是否卡死）与 U6（真实成本）——U5 绑在 M1 才出现的 H5 上，U6 需要一趟真实运行才有数。刻意推迟，不是遗漏。

**Architecture:** 仓库根即插件根。门禁拆成两层——`hooks/lib/decide.mjs` 是纯函数决策核心（无 I/O，可用 `node:test` 单测），`hooks/gate.mjs` 只负责读 stdin、调决策、写 stdout。花名册 `roster.json` 是派发关系的唯一真源，单测断言 `at-pm` 的 frontmatter 白名单与之不漂移。开发期用 Windows junction 把仓库挂到 `~/.claude/skills/agent-team`，以 `agent-team@skills-dir` 自动加载。

**Tech Stack:** Node 24.14.0（仅内置模块，`node:test` + `node:assert`，零 npm 依赖）；Markdown agent 定义；JSON 清单；Claude Code 2.1.270。

## Global Constraints

- 运行环境：Claude Code **2.1.270**，Claude 桌面客户端 Code tab，Windows 11。引擎路径 `%APPDATA%\Claude\claude-code\2.1.270\claude.exe`。
- **零运行时依赖**：hook 只用 Node 内置模块。不用 bash、不用 jq、不用任何 npm 包。用户装插件后不需要 `npm install`。
- 许可证 **MIT**。角色正文全部自写；只可参考 Apache-2.0 / MIT / MIT-0 来源；`anthropics/claude-plugins-official` 的 `claude-security` 是专有许可，**只学结构，一个字不抄**。
- 角色正文中文；`README.md` 英文 + `README.zh-CN.md` 中文。
- 所有 agent 名一律 `at-` 前缀。
- `agent_type` **只在 subagent 的 hook 输入里出现**；字段缺失即表示调用者是主线程。
- hook 失败策略：派发白名单 / 写路径隔离 / 契约保护 **fail closed**；就绪门禁 fail open + warning；交付物校验不 block。
- 每个 commit 末尾加：`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- 工作目录一律 `C:\Users\82370\Desktop\Agent-Team`。
- **跑测试的命令一律是裸的 `node --test`**。在本机（Node 24.14.0 / Windows）实测：
  `node --test tests/` **不会发现** `tests/` 下的测试文件，而是报一个 `pass 0 / fail 1` 的
  幻影失败，且实现前后失败完全相同——用它做 TDD，RED 阶段的证据是假的。
  裸 `node --test` 正常发现并跑全部 9 个测试。

## 文件结构

| 文件 | 职责 |
|---|---|
| `.claude-plugin/plugin.json` | 插件清单 |
| `.claude-plugin/marketplace.json` | 自托管市场清单，`source: "./"` |
| `roster.json` | **派发关系唯一真源**：每个角色能派给谁 |
| `hooks/lib/decide.mjs` | 纯函数决策核心，无 I/O，可单测 |
| `hooks/lib/frontmatter.mjs` | 从 agent md 提取 `tools:` 行里的 `Agent(...)` 列表 |
| `hooks/gate.mjs` | hook 单入口：读 stdin → 调决策 → 写 stdout |
| `hooks/hooks.json` | hook 注册，exec 形式 |
| `agents/at-pm.md` | M0 占位 PM，带主线程白名单 |
| `agents/at-product.md` | M0 占位，能往下派 |
| `agents/at-architect.md` | M0 占位，能往下派 |
| `agents/at-worker-a.md` | M0 占位叶子 |
| `agents/at-worker-b.md` | M0 占位叶子 |
| `agents/at-outsider.md` | 白名单反例，谁都不该能派它 |
| `settings.json` | `{"agent": "at-pm"}`，Task 5 才加 |
| `tests/decide.test.mjs` | 决策核心单测 |
| `tests/roster-closure.test.mjs` | 花名册闭包不变量：可派发目标必须也在花名册里 |
| `tests/plugin-name-sync.test.mjs` | 插件名对账：PLUGIN_PREFIX 必须等于 plugin.json 的 name |
| `tests/roster-sync.test.mjs` | 断言 frontmatter 与 roster.json 不漂移 |
| `scripts/dev-link.mjs` | 建/删 junction 的开发期挂载脚本 |
| `LICENSE` `README.md` `README.zh-CN.md` | 发布物骨架 |

⚠️ **运行期风险，全程注意**：`settings.json` 里的 `agent` 键作用于**插件启用时的每一个新会话**，不分项目。Task 5 之后，不做实验时必须 `claude plugin disable agent-team`，否则你所有新会话都会变成 `at-pm`。

---

### Task 1: 插件骨架与清单

**Files:**
- Create: `.claude-plugin/plugin.json`
- Create: `.claude-plugin/marketplace.json`
- Create: `LICENSE`
- Create: `README.md`
- Create: `README.zh-CN.md`

**Interfaces:**
- Consumes: 无（首个任务）
- Produces: 插件名 `agent-team`；市场名 `agent-team-marketplace`；后续所有 `${CLAUDE_PLUGIN_ROOT}` 指向仓库根。

- [ ] **Step 1: 写插件清单**

`.claude-plugin/plugin.json`：

```json
{
  "name": "agent-team",
  "description": "十角色软件开发 agent team：项目经理为主会话，分层派发，顺序受门禁强制，业务验收独立成线。",
  "version": "0.0.1",
  "author": {
    "name": "hangwenlei",
    "email": "rorohangwenlei@gmail.com"
  },
  "license": "MIT"
}
```

- [ ] **Step 2: 写市场清单**

`.claude-plugin/marketplace.json`：

```json
{
  "name": "agent-team-marketplace",
  "owner": {
    "name": "hangwenlei"
  },
  "plugins": [
    {
      "name": "agent-team",
      "source": "./",
      "description": "十角色软件开发 agent team，分层派发与门禁强制的顺序流程。"
    }
  ]
}
```

- [ ] **Step 3: 跑校验器，让它当裁判**

```bash
"$APPDATA/Claude/claude-code/2.1.270/claude.exe" plugin validate .
```

Expected: 通过。若报缺字段或未知字段，**按它的报错改清单，不要猜**——校验器是本任务的唯一裁判。反复跑到通过为止。

- [ ] **Step 4: 写 LICENSE（MIT）**

`LICENSE`：取 https://opensource.org/license/mit 的标准全文，
把占位的 `<year> <copyright holders>` 替换为 `2026 hangwenlei`，首行为 `MIT License`。
不要改写条款正文——改过的 MIT 不再是 MIT。

- [ ] **Step 5: 写 README 骨架**

`README.md`（英文）与 `README.zh-CN.md`（中文），各自至少包含：一句话简介、状态标注 `Status: M0 — foundation validation, not yet usable`、安装方式占位、**以及一节明写已知边界**：

> 写路径隔离对 `Edit`/`Write` 是硬约束，对 `Bash` 是软约束。执行角色保留 Bash 以运行构建与测试，而 Bash 可以写文件（`echo >`、`sed -i`）。本插件不是沙箱。

这一节从第一天就要在，不能等发布前补。

- [ ] **Step 6: 再校验一次并提交**

```bash
"$APPDATA/Claude/claude-code/2.1.270/claude.exe" plugin validate .
git add -A
git commit -m "feat: 插件骨架与清单

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 派发决策核心与单测

**Files:**
- Create: `roster.json`
- Create: `hooks/lib/decide.mjs`
- Create: `tests/decide.test.mjs`
- Create: `tests/roster-closure.test.mjs`
- Create: `tests/plugin-name-sync.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces:
  - `roster.json` 形状：`{ "<agentType>": { "can_delegate_to": string[] } }`，主线程的键固定为 `"__main__"`
  - `decideDelegation(input: object, roster: object) => { decision: "allow" | "deny", reason?: string }`
  - `callerOf(input: object) => string`（`input.agent_type` 缺失时返回 `"__main__"`）
  - **花名册闭包不变量**：`can_delegate_to` 里出现的每个名字本身也必须是花名册的键。
    这是「未登记调用者放行」那条之所以不构成逃逸口的唯一依据，后续任务扩花名册时必须维持。

- [ ] **Step 1: 写花名册**

`roster.json`——M0 占位班底，形状与 M1 一致：

**为什么 `__main__` 和 `at-pm` 两个键指向同一份白名单**：官方只说 `agent_type`
「只在 subagent 里出现」，但主会话被 `agent` 键钉成 `at-pm` 之后它仍然是主线程——
平台此时到底填不填这个字段，文档没说，这是 §10 没列出的第五条未知。
两个键都放即可覆盖两种情况。而拒绝理由里会打印 caller 名，
**所以 Task 4 观测到的那句报错，本身就会告诉你平台用的是哪个键**。这条要记录。

```json
{
  "__main__":       { "can_delegate_to": ["at-product", "at-architect"] },
  "at-pm":          { "can_delegate_to": ["at-product", "at-architect"] },
  "at-product":     { "can_delegate_to": ["at-worker-a"] },
  "at-architect":   { "can_delegate_to": ["at-worker-a", "at-worker-b"] },
  "at-worker-a":    { "can_delegate_to": [] },
  "at-worker-b":    { "can_delegate_to": [] },
  "at-outsider":    { "can_delegate_to": [] }
}
```

- [ ] **Step 2: 写失败的测试**

`tests/decide.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { callerOf, decideDelegation } from '../hooks/lib/decide.mjs'

const ROSTER = {
  __main__: { can_delegate_to: ['at-product', 'at-architect'] },
  'at-architect': { can_delegate_to: ['at-worker-a', 'at-worker-b'] },
  'at-worker-a': { can_delegate_to: [] },
}

test('agent_type 缺失时调用者是主线程', () => {
  assert.equal(callerOf({ tool_name: 'Agent' }), '__main__')
})

test('agent_type 存在时调用者是该 subagent', () => {
  assert.equal(callerOf({ agent_type: 'at-architect' }), 'at-architect')
})

test('花名册内的派发放行', () => {
  const r = decideDelegation(
    { agent_type: 'at-architect', tool_input: { subagent_type: 'at-worker-a' } },
    ROSTER,
  )
  assert.equal(r.decision, 'allow')
})

test('花名册外的派发拒绝', () => {
  const r = decideDelegation(
    { agent_type: 'at-architect', tool_input: { subagent_type: 'at-outsider' } },
    ROSTER,
  )
  assert.equal(r.decision, 'deny')
})

test('拒绝理由里列出它实际能派的角色', () => {
  const r = decideDelegation(
    { agent_type: 'at-architect', tool_input: { subagent_type: 'at-outsider' } },
    ROSTER,
  )
  assert.match(r.reason, /at-worker-a/)
  assert.match(r.reason, /at-worker-b/)
})

test('叶子角色不得派发任何人，且理由点明它是叶子', () => {
  const r = decideDelegation(
    { agent_type: 'at-worker-a', tool_input: { subagent_type: 'at-worker-b' } },
    ROSTER,
  )
  assert.equal(r.decision, 'deny')
  assert.match(r.reason, /叶子角色/)
})

test('未登记的调用者不归本门禁管，放行', () => {
  const r = decideDelegation(
    { agent_type: 'general-purpose', tool_input: { subagent_type: 'at-worker-a' } },
    ROSTER,
  )
  assert.equal(r.decision, 'allow')
})

test('受管辖角色不写 subagent_type 时拒绝——省略该字段会拿到 general-purpose', () => {
  const r = decideDelegation({ agent_type: 'at-architect', tool_input: {} }, ROSTER)
  assert.equal(r.decision, 'deny')
  assert.match(r.reason, /subagent_type/)
})

test('未登记的调用者不写 subagent_type 仍然放行', () => {
  const r = decideDelegation({ agent_type: 'general-purpose', tool_input: {} }, ROSTER)
  assert.equal(r.decision, 'allow')
})

test('条目缺 can_delegate_to 时拒绝，并说明是 roster.json 的配置错误', () => {
  const r = decideDelegation(
    { agent_type: 'at-broken', tool_input: { subagent_type: 'at-worker-a' } },
    { ...ROSTER, 'at-broken': {} },
  )
  assert.equal(r.decision, 'deny')
  assert.match(r.reason, /roster\.json/)
})

test('callerOf 剥掉本插件前缀', () => {
  assert.equal(callerOf({ agent_type: 'agent-team:at-architect' }), 'at-architect')
})

test('callerOf 不剥别的插件的前缀', () => {
  assert.equal(callerOf({ agent_type: 'other:at-architect' }), 'other:at-architect')
})

test('带插件前缀的目标按裸名查花名册——合法派发必须放行', () => {
  const r = decideDelegation(
    {
      agent_type: 'agent-team:at-architect',
      tool_input: { subagent_type: 'agent-team:at-worker-a' },
    },
    ROSTER,
  )
  assert.equal(r.decision, 'allow')
})

test('别的插件的同名 agent 不被当作自己人', () => {
  const r = decideDelegation(
    { agent_type: 'at-architect', tool_input: { subagent_type: 'other:at-worker-a' } },
    ROSTER,
  )
  assert.equal(r.decision, 'deny')
})

// 这一条是本次修复的真正回归保护。上一条（caller 与 target 都带前缀）
// 在修复前的代码上照样通过——带前缀的 caller 查不到花名册，直接从
// 「未登记调用者放行」岔路走掉，根本没走到 target 归一化。
// 而下面这个形态（主线程 + 带前缀目标）正是线上真实炸掉的那一个。
test('主线程派发带前缀的合法目标——真实复现形态，必须放行', () => {
  const r = decideDelegation(
    { tool_input: { subagent_type: 'agent-team:at-product' } },
    ROSTER,
  )
  assert.equal(r.decision, 'allow')
})

test('退化输入 agent-team: 不被剥成空串', () => {
  assert.equal(callerOf({ agent_type: 'agent-team:' }), 'agent-team:')
  const r = decideDelegation(
    { agent_type: 'at-architect', tool_input: { subagent_type: 'agent-team:' } },
    ROSTER,
  )
  assert.equal(r.decision, 'deny')
  assert.match(r.reason, /不得派发给/)
})

test('主线程按 __main__ 判定', () => {
  const ok = decideDelegation({ tool_input: { subagent_type: 'at-product' } }, ROSTER)
  const no = decideDelegation({ tool_input: { subagent_type: 'at-worker-a' } }, ROSTER)
  assert.equal(ok.decision, 'allow')
  assert.equal(no.decision, 'deny')
})
```

- [ ] **Step 3: 跑测试确认它失败**

```bash
node --test
```

Expected: FAIL，报 `Cannot find module '../hooks/lib/decide.mjs'`。

- [ ] **Step 4: 写最小实现**

`hooks/lib/decide.mjs`：

```js
// 纯函数决策核心。不读 stdin、不写 stdout、不调 process.exit，
// 因此可以脱离 Claude Code 直接单测。

export const MAIN = '__main__'

// 插件提供的 agent 在平台上的注册名带插件前缀（agent-team:at-pm），花名册用裸名书写。
// U3 实测的死结：裸名过得了 hook 但名称解析报 not found；带前缀的过得了名称解析
// 却被花名册拦下——两边对不上，合法派发会被自己的门禁全部拒掉。
// 只剥本插件自己的前缀：无差别剥会让别的插件的 otherplugin:at-product
// 被误认成我们的 at-product。
// 导出是为了让 tests/plugin-name-sync.test.mjs 能拿它与 plugin.json 的 name 对账。
// decide.mjs 必须保持纯函数（不读文件），所以插件名在这里是硬编码，
// 由那个测试负责在改名时报警——否则改名后归一化静默失效，门禁回退到
// 「一律拒绝」，而所有测试仍然是绿的。
export const PLUGIN_PREFIX = 'agent-team:'

function stripPluginPrefix(name) {
  if (typeof name !== 'string' || !name.startsWith(PLUGIN_PREFIX)) return name
  const bare = name.slice(PLUGIN_PREFIX.length)
  // 退化输入（恰好等于前缀本身）剥完是空串。空串会让 target 被误判成
  // 「没写目标」、让 caller 被误判成未登记调用者而放行——两种语义都不对。
  // 剥出空串时当作没剥过，让它作为一个不匹配的名字走正常拒绝路径。
  return bare === '' ? name : bare
}

/** hook 输入里 agent_type 只在 subagent 中出现；缺失即主线程。 */
export function callerOf(input) {
  const raw = input?.agent_type
  return raw === undefined || raw === null ? MAIN : stripPluginPrefix(raw)
}

function allow() {
  return { decision: 'allow' }
}

function deny(reason) {
  return { decision: 'deny', reason }
}

function fmt(list) {
  return list.length ? list.join('、') : '（无，这是叶子角色）'
}

/**
 * 派发白名单（门禁 H1）。补的是平台的空缺：
 * 主线程的 tools: Agent(...) 白名单是硬的，subagent 定义里的括号列表被忽略。
 */
export function decideDelegation(input, roster) {
  const caller = callerOf(input)
  const entry = roster?.[caller]

  // 未登记的调用者不归本门禁管：本机可能还有别的插件或用户自己的 subagent 在跑，
  // agent-team 无权干涉它们。这条放行之所以安全，靠的是花名册闭包不变量
  // （见 tests/roster-closure.test.mjs）：can_delegate_to 里出现的每个名字
  // 本身也必须是花名册的键，所以受本门禁管辖的角色永远派不出一个不受管辖的 agent。
  if (!entry) return allow()

  const allowed = entry.can_delegate_to
  if (!Array.isArray(allowed)) {
    return deny(
      `花名册中 ${caller} 的条目缺少 can_delegate_to 数组，无法判定派发权限。` +
        `这是 roster.json 的配置错误，不是这次调用的问题。`,
    )
  }

  // Agent 工具的 subagent_type 是可选字段，省略即得到 general-purpose 代理。
  // 所以对受管辖的调用者来说，"没写目标"是一次真实的、拿到全套工具的派发，
  // 而不是"这次调用与本门禁无关"。必须拒，否则整个花名册可被一个省略的字段绕过。
  const target = stripPluginPrefix(input?.tool_input?.subagent_type)
  if (!target) {
    return deny(
      `角色 ${caller} 调用 Agent 时未指定 subagent_type。省略该字段会得到 general-purpose ` +
        `代理，从而绕过花名册。请显式写明目标角色，它必须是：${fmt(allowed)}。`,
    )
  }

  if (allowed.includes(target)) return allow()

  return deny(
    `角色 ${caller} 不得派发给 ${target}。它可以派发的角色是：${fmt(allowed)}。` +
      `如果这项工作确实需要 ${target}，把它冒泡给上级，不要绕过花名册。`,
  )
}
```

- [ ] **Step 5: 写花名册闭包不变量测试**

这个测试是「未登记调用者放行」那条之所以安全的唯一依据。没有它，哪天某个
`can_delegate_to` 里写了一个不在花名册里的名字，受管辖角色就能派出一个不受管辖的
agent，级联逃逸链条静默恢复，而没有任何东西会报错。

`tests/roster-closure.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'

const roster = JSON.parse(
  readFileSync(new URL('../roster.json', import.meta.url), 'utf8'),
)

test('每个条目都有 can_delegate_to 数组', () => {
  for (const [caller, entry] of Object.entries(roster)) {
    assert.ok(
      Array.isArray(entry.can_delegate_to),
      `${caller} 的 can_delegate_to 不是数组`,
    )
  }
})

test('can_delegate_to 里出现的每个名字本身也是花名册的键', () => {
  for (const [caller, entry] of Object.entries(roster)) {
    for (const target of entry.can_delegate_to) {
      assert.ok(
        Object.hasOwn(roster, target),
        `${caller} 可派发 ${target}，但 ${target} 不在花名册里——` +
          `受管辖角色能派出不受管辖的 agent，级联逃逸链条就此打开`,
      )
    }
  }
})
```

- [ ] **Step 6: 写插件名对账测试**

`PLUGIN_PREFIX` 与 `plugin.json` 的 `name` 是同一个名字的两处硬编码。改名时若只改一处，
前缀归一化静默失效，门禁回退到「一律拒绝」——而所有既有测试仍然是绿的，
因为它们各自硬编码了自己那份字面量。这个测试是唯一会在那时候报警的东西。

`tests/plugin-name-sync.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'
import { PLUGIN_PREFIX } from '../hooks/lib/decide.mjs'

test('PLUGIN_PREFIX 与 plugin.json 的 name 保持一致', () => {
  const manifest = JSON.parse(
    readFileSync(new URL('../.claude-plugin/plugin.json', import.meta.url), 'utf8'),
  )
  assert.equal(
    PLUGIN_PREFIX,
    `${manifest.name}:`,
    '插件改名后 PLUGIN_PREFIX 没跟着改：前缀归一化会失效，' +
      '门禁将回退到「一律拒绝」，而其余测试仍然全绿',
  )
})
```

- [ ] **Step 7: 跑测试确认通过**

```bash
node --test
```

Expected: 20 个测试全 PASS（`decide.test.mjs` 17 个 + `roster-closure.test.mjs` 2 个 +
`plugin-name-sync.test.mjs` 1 个），输出干净。

- [ ] **Step 8: 提交**

```bash
git add roster.json hooks/lib/decide.mjs tests/decide.test.mjs tests/roster-closure.test.mjs tests/plugin-name-sync.test.mjs
git commit -m "feat: 派发决策核心与单测

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: hook 入口、注册与开发期挂载（验 U3）

**Files:**
- Create: `hooks/gate.mjs`
- Create: `hooks/hooks.json`
- Create: `scripts/dev-link.mjs`
- Create: `agents/at-pm.md`（占位）
- Create: `agents/at-product.md`、`agents/at-architect.md`、`agents/at-worker-a.md`、`agents/at-worker-b.md`、`agents/at-outsider.md`（占位）

**Interfaces:**
- Consumes: `decideDelegation`、`callerOf`（Task 2）；`roster.json`
- Produces: 可加载的插件；`node scripts/dev-link.mjs link|unlink` 开发期挂载

- [ ] **Step 1: 写 hook 入口**

`hooks/gate.mjs`：

```js
#!/usr/bin/env node
// hook 单入口。第一个参数选择检查项。
// 约定：exit 0 + stdout 上的 JSON 决策 = 生效；无输出 = 走正常权限流程。

import { readFileSync } from 'node:fs'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'
import { decideDelegation } from './lib/decide.mjs'

const CHECK = process.argv[2]
const HERE = dirname(fileURLToPath(import.meta.url))
const ROOT = join(HERE, '..')

function readStdin() {
  try {
    return JSON.parse(readFileSync(0, 'utf8'))
  } catch {
    return null
  }
}

function loadRoster() {
  return JSON.parse(readFileSync(join(ROOT, 'roster.json'), 'utf8'))
}

function emitDeny(reason) {
  process.stdout.write(
    JSON.stringify({
      hookSpecificOutput: {
        hookEventName: 'PreToolUse',
        permissionDecision: 'deny',
        permissionDecisionReason: reason,
      },
    }),
  )
}

function main() {
  const input = readStdin()
  if (!input) {
    // 读不到输入：安全边界类检查 fail closed。
    if (CHECK === 'delegation') {
      emitDeny('agent-team 门禁无法读取 hook 输入，按安全边界拒绝。')
    }
    process.exit(0)
  }

  if (CHECK === 'delegation') {
    const result = decideDelegation(input, loadRoster())
    if (result.decision === 'deny') emitDeny(result.reason)
  }

  process.exit(0)
}

try {
  main()
} catch (err) {
  // gate.mjs 自身异常：派发白名单是 fail closed。
  if (CHECK === 'delegation') {
    emitDeny(`agent-team 门禁异常，按安全边界拒绝：${err.message}`)
  }
  process.exit(0)
}
```

- [ ] **Step 2: 注册 hook（exec 形式，占位符要配 args）**

`hooks/hooks.json`：

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs", "delegation"],
            "timeout": 30,
            "statusMessage": "校验派发白名单…"
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 3: 写六个占位角色**

`agents/at-pm.md`：

```markdown
---
name: at-pm
description: M0 占位项目经理。只用于验证平台机制，不含真实职责。
tools: Agent(at-product, at-architect), Read, Glob
model: sonnet
---

你是 AT-PM（M0 占位版）。每次回复以 `AT-PM` 开头。
你只做被明确要求的验证动作，不做任何其他工作，不修改任何文件。
```

`agents/at-product.md`：

```markdown
---
name: at-product
description: M0 占位产品经理。
tools: Agent, Read, Glob
model: sonnet
---

你是 AT-PRODUCT（M0 占位版）。每次回复以 `AT-PRODUCT` 开头。
你只做被明确要求的验证动作，不修改任何文件。
```

`agents/at-architect.md`：

```markdown
---
name: at-architect
description: M0 占位技术架构师。
tools: Agent, Read, Glob
model: sonnet
---

你是 AT-ARCHITECT（M0 占位版）。每次回复以 `AT-ARCHITECT` 开头。
你只做被明确要求的验证动作，不修改任何文件。
```

`agents/at-worker-a.md`：

```markdown
---
name: at-worker-a
description: M0 占位执行角色 A。
tools: Read
model: haiku
---

你是 AT-WORKER-A（M0 占位版）。原样回复被要求回复的内容，不做别的。
```

`agents/at-worker-b.md`：

```markdown
---
name: at-worker-b
description: M0 占位执行角色 B。
tools: Read
model: haiku
---

你是 AT-WORKER-B（M0 占位版）。原样回复被要求回复的内容，不做别的。
```

`agents/at-outsider.md`：

```markdown
---
name: at-outsider
description: M0 白名单反例。任何角色都不应能派发它。
tools: Read
model: haiku
---

你是 AT-OUTSIDER。如果你被启动了，说明白名单没拦住。
原样回复：OUTSIDER-RAN
```

- [ ] **Step 4: 写开发期挂载脚本**

`scripts/dev-link.mjs`：

```js
#!/usr/bin/env node
// 把仓库挂到 ~/.claude/skills/agent-team，使其以 agent-team@skills-dir 自动加载。
// Windows 用 junction（mklink /J），不需要管理员权限。

import { execFileSync } from 'node:child_process'
import { lstatSync, mkdirSync, rmSync } from 'node:fs'
import { homedir } from 'node:os'
import { dirname, join, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'

const REPO = resolve(dirname(fileURLToPath(import.meta.url)), '..')
const LINK = join(homedir(), '.claude', 'skills', 'agent-team')
const action = process.argv[2]

// 不能用 existsSync：它会跟随 junction，目标被删被移之后返回 false，
// 而路径上的重解析点其实还在，此时 link 会撞上「已存在」而报一坨栈。
// lstatSync 问的是「这个路径上有没有东西」，能识别悬空 junction。
function pathExists(p) {
  try {
    lstatSync(p)
    return true
  } catch {
    return false
  }
}

if (action === 'link') {
  if (pathExists(LINK)) {
    console.error(`已存在：${LINK}。先跑 unlink。`)
    process.exit(1)
  }
  // mklink /J 不会创建父目录，而干净机器上 ~/.claude/skills 往往还不存在。
  mkdirSync(dirname(LINK), { recursive: true })
  try {
    execFileSync('cmd', ['/c', 'mklink', '/J', LINK, REPO], { stdio: 'inherit' })
  } catch (err) {
    console.error(`挂载失败：${err.message}`)
    console.error(`手动排查：mklink /J "${LINK}" "${REPO}"`)
    process.exit(1)
  }
  console.log(`已挂载 ${LINK} -> ${REPO}`)
  console.log('新开一个会话才会加载。')
} else if (action === 'unlink') {
  if (!pathExists(LINK)) {
    console.log('没有挂载，无需处理。')
    process.exit(0)
  }
  // ⚠️ recursive 必须保持 false，永远不要改成 true。
  // 实测：LINK 是 junction 时，这行只删链接本身，目标目录与内容完好；
  // 若 LINK 恰好是个真目录，它抛 ERR_FS_EISDIR 什么都不删（安全的失败）。
  // 改成 recursive: true 就会在后一种情形下连内容一起删掉。
  rmSync(LINK, { recursive: false, force: true })
  console.log(`已卸载 ${LINK}`)
} else {
  console.error('用法：node scripts/dev-link.mjs link|unlink')
  process.exit(1)
}
```

- [ ] **Step 5: 校验并挂载**

```bash
"$APPDATA/Claude/claude-code/2.1.270/claude.exe" plugin validate .
node scripts/dev-link.mjs link
```

Expected: 校验通过；挂载打印出 `已挂载 ...`。

- [ ] **Step 6: 先确认插件真的加载了（排除假阴性）**

在桌面客户端**新开一个会话**（挂载后必须新开，已有会话不会重新扫描），发：

```
/plugin
```

或在任意会话里跑：

```bash
"$APPDATA/Claude/claude-code/2.1.270/claude.exe" plugin details agent-team
```

Expected: 列出 `agent-team`，且组件清单里能看到 6 个 agent 与 1 个 PreToolUse hook。

**没看到就停在这里**，不要往下做 U3 实验——插件没加载的话，实验一定"失败"，
而那个失败与 hook 本身能不能拦截无关，会把 U3 误判成否。
先排查：junction 是否建成（`ls ~/.claude/skills/agent-team`）、
`claude plugin validate .` 是否通过、是否真的新开了会话。

- [ ] **Step 7: 验 U3 —— Windows 下 hook 能否拿到 agent_type 并拒绝**

在 Claude 桌面客户端**新开一个会话**，工作目录选 `C:\Users\82370\Desktop\Agent-Team`，发：

```
用 Agent 工具派发一个 subagent_type 为 "at-architect" 的子代理，
提示词就一句：请你再派发一个 subagent_type 为 "at-outsider" 的子代理，
提示词是「原样回复：OUTSIDER-RAN」。把你看到的原文结果或原文报错贴回来，不要重试、不要绕开。
```

判读：

| 观测 | 含义 | 下一步 |
|---|---|---|
| 架构师报出 gate.mjs 的中文拒绝理由 | **U3 = 是**，hook 拿到了 `agent_type` 且 deny 生效 | 继续 Task 4 |
| 返回 `OUTSIDER-RAN` | **U3 = 否**，hook 没拦住 | 停。查 `claude plugin details agent-team` 确认 hook 是否注册上；若已注册仍不拦，回改规格 §6 |
| 报 hook error / node 找不到 | 注册或路径问题 | 修 `hooks.json` 的 exec 形式与占位符，重试一次 |

- [ ] **Step 8: 记录观测并提交**

把 Step 7 的原文观测追加进 `docs/04-本机实测结论.md` 的新小节「M0 · U3」，然后：

```bash
git add -A
git commit -m "feat: hook 入口、注册与开发期挂载；验 U3

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 主线程身份与白名单（验 U1、U2）

**Files:**
- Create: `settings.json`
- Create: `tests/roster-sync.test.mjs`
- Create: `hooks/lib/frontmatter.mjs`

**Interfaces:**
- Consumes: `roster.json`、`agents/at-pm.md`
- Produces: `parseAgentAllowlist(md: string) => string[]`——从 agent md 的 `tools:` 行提取 `Agent(...)` 内的类型名

⚠️ 本任务开始，插件会接管**所有新会话**的身份。不做实验时立刻 `claude plugin disable agent-team`。

- [ ] **Step 1: 写防漂移的失败测试**

`tests/roster-sync.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'
import { parseAgentAllowlist } from '../hooks/lib/frontmatter.mjs'

test('at-pm 的 frontmatter 白名单与 roster.json 一致', () => {
  const md = readFileSync(new URL('../agents/at-pm.md', import.meta.url), 'utf8')
  const roster = JSON.parse(readFileSync(new URL('../roster.json', import.meta.url), 'utf8'))
  assert.deepEqual(
    parseAgentAllowlist(md).sort(),
    [...roster['at-pm'].can_delegate_to].sort(),
  )
})

test('没有 Agent(...) 时返回空数组', () => {
  assert.deepEqual(parseAgentAllowlist('---\ntools: Read, Glob\n---\n正文'), [])
})

test('能解析带空格的列表', () => {
  assert.deepEqual(
    parseAgentAllowlist('---\ntools: Agent( a , b ), Read\n---\n正文'),
    ['a', 'b'],
  )
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
node --test
```

Expected: FAIL，报找不到 `../hooks/lib/frontmatter.mjs`。

- [ ] **Step 3: 写最小实现**

`hooks/lib/frontmatter.mjs`：

```js
// 从 agent markdown 的 tools: 行里提取 Agent(...) 的类型列表。
// 只做这一件事，不解析完整 YAML，因此不需要任何依赖。

export function parseAgentAllowlist(md) {
  const line = md.split(/\r?\n/).find((l) => /^tools:/.test(l.trim()))
  if (!line) return []
  const m = line.match(/Agent\(([^)]*)\)/)
  if (!m) return []
  return m[1]
    .split(',')
    .map((s) => s.trim())
    .filter(Boolean)
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
node --test
```

Expected: 全部 PASS（Task 2 的 9 个 + 本任务的 3 个）。

- [ ] **Step 5: 加 settings.json，先用裸名**

`settings.json`：

```json
{
  "agent": "at-pm"
}
```

- [ ] **Step 6: 验 U1 与 U2**

```bash
node scripts/dev-link.mjs unlink && node scripts/dev-link.mjs link
```

在桌面客户端**新开会话**（工作目录任意），发 `ping`。

| 观测 | 结论 |
|---|---|
| 回复以 `AT-PM` 开头 | **U1 = 是（裸名可用）**，进 Step 7 |
| 回复是普通助手 | 裸名不行。把 `settings.json` 改成 `{"agent": "agent-team:at-pm"}`，重挂载，再开一个会话重试。成功则 **U1 = 是（需命名空间）**；仍失败则 **U1 = 否**，停下回改规格 §3.1 |

- [ ] **Step 7: 验 U2 —— 主线程白名单能否引用插件提供的 agent**

在同一个会话里发：

```
按你的 tools 白名单做两次派发，两次都不要重试、不要绕开，原样贴回结果或报错：
一、subagent_type = "at-product"，提示词「原样回复：PRODUCT-OK」
二、subagent_type = "at-outsider"，提示词「原样回复：OUTSIDER-RAN」
```

| 观测 | 结论 |
|---|---|
| 一成功、二报 `not found` 之类 | **U2 = 是**，白名单对插件提供的 agent 生效 |
| 二也成功 | **U2 = 否**，主线程白名单对插件 agent 不生效——记下来，规格 §3.3 的「上面那条边是硬的」不再成立，M1 的派发约束要全部落到 hook |

**顺带回答第五条未知**：如果拒绝来自 gate.mjs（理由是中文、形如「角色 X 不得派发给 Y」），
把 X 原样记下来——X 是 `__main__` 就说明被钉住的主线程**不带** `agent_type`，
是 `at-pm` 就说明**带**。这决定 M1 的花名册能不能去掉一个键，也决定所有 hook 怎么识别主线程。
若拒绝来自平台自身（英文 `not found`），说明白名单先于 hook 生效，gate.mjs 没被调到，
此时改派一个白名单内、但花名册禁止的组合再测一次。

- [ ] **Step 8: 用完立刻停用插件**

```bash
"$APPDATA/Claude/claude-code/2.1.270/claude.exe" plugin disable agent-team
```

- [ ] **Step 9: 记录观测并提交**

把 Step 6、7 的原文观测追加进 `docs/04-本机实测结论.md` 的「M0 · U1/U2」小节，然后：

```bash
git add -A
git commit -m "feat: 主线程身份与白名单防漂移测试；验 U1 U2

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: subagent 并发派发（验 U4）

**Files:**
- Modify: `agents/at-architect.md`（加一句并发派发的指令）

**Interfaces:**
- Consumes: Task 3 的占位角色、Task 4 的 `settings.json`
- Produces: U4 的结论——决定 M1 里 S3 拉通与 S5 实现是并行还是串行

- [ ] **Step 1: 让架构师占位角色具备并发派发的说明**

`agents/at-architect.md` 正文追加：

```markdown
当被要求并发派发时，在**同一条消息里**发出多个 Agent 工具调用，
而不是一个接一个地发。这是被测量的行为，不要改成串行。
```

- [ ] **Step 2: 重挂载并启用插件**

```bash
node scripts/dev-link.mjs unlink && node scripts/dev-link.mjs link
"$APPDATA/Claude/claude-code/2.1.270/claude.exe" plugin enable agent-team
```

- [ ] **Step 3: 验 U4**

新开会话，发：

```
派发 subagent_type = "at-architect"，提示词如下：
「在同一条消息里并发派发两个子代理：subagent_type = "at-worker-a" 提示词「原样回复：A-OK」，
subagent_type = "at-worker-b" 提示词「原样回复：B-OK」。
报告你是在一条消息里同时发出的还是分两次发的，以及两个返回的原文。」
```

| 观测 | 结论 |
|---|---|
| 架构师报告「一条消息内同时发出」且两个都返回 | **U4 = 是**，M1 的 S3/S5 可并行 |
| 只能串行，或第二个被拒 | **U4 = 否**，M1 的 S3/S5 退化为串行。规格 §3.2.1 要补一句，且要重新评估耗时 |

- [ ] **Step 4: 停用插件**

```bash
"$APPDATA/Claude/claude-code/2.1.270/claude.exe" plugin disable agent-team
```

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: 验 U4 subagent 并发派发

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: 结论落盘与规格回填

**Files:**
- Create: `docs/05-M0-结论.md`
- Modify: `docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md`（§10 未决表）

**Interfaces:**
- Consumes: Task 3/4/5 的观测
- Produces: M1 是否可以开工的判定

- [ ] **Step 1: 写 M0 结论**

`docs/05-M0-结论.md`，每条未知一节，每节四项：**怎么测的 / 原文观测 / 结论（是/否）/ 对设计的影响**。原文观测要一字不改地贴，包括报错。

- [ ] **Step 2: 回填规格 §10**

把 §10 表里 U1–U4 的「状态」由 `未测` 改为 `是` 或 `否`，并在「影响」列写明实际结论。**不删行**——保留它曾是未知的记录。

- [ ] **Step 3: 判定 M1 能否开工**

逐条对照：

| 若 | 则 |
|---|---|
| U1–U4 全为是 | M1 按规格原样开工 |
| U1 = 否 | 主会话无法钉成 PM。层级最上面一层失效，回 §3 重新选型，**不要开工 M1** |
| U2 = 否 | 上层白名单不硬。M1 的派发约束全部落到 hook，§3.3 要改写，H1 升为唯一防线 |
| U3 = 否 | **五个 hook 的共同地基塌了**。§6 整节作废，必须先找到替代强制手段，不要开工 M1 |
| U4 = 否 | S3/S5 改串行，M1 可开工，但耗时与成本估算要重算 |

把判定结论写在 `docs/05-M0-结论.md` 末尾。

- [ ] **Step 4: 跑全部测试并提交**

```bash
node --test
"$APPDATA/Claude/claude-code/2.1.270/claude.exe" plugin validate .
git add -A
git commit -m "docs: M0 结论与规格回填

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

- [ ] **Step 5: 清理**

```bash
node scripts/dev-link.mjs unlink
```

M0 结束。是否开工 M1 由 Step 3 的判定决定，不由进度压力决定。
