# M2a 阶段链与返工闭环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把阶段链从 S5 接到 S8，并补上只有接上之后才第一次成立的两个机制——返工预算的写时强制、驳回路由。

**Architecture:** `stages.json` 引入 `producers` 与 `<role>` 占位符（规格 §4 本来就写的是模式，实现窄化成了字面量）；四个消费方各自按不同集合展开占位符，单一真源放在既有的 `hooks/lib/stages.mjs`。返工写时强制做成第六个闸 H6，挂在既有的 `PreToolUse` 写路径 matcher 上——`PostToolUse` 看不到改之前那一版，`PreToolUse` 看得到。驳回路由把规格 §4.3 那张表搬进代码。

**Tech Stack:** Node 内置模块（`node:test`、`node:assert/strict`、`node:fs`、`node:crypto`），零运行时依赖，无 `package.json`。

**Spec:** `docs/superpowers/specs/2026-09-19-M2a-阶段链与返工闭环-design.md`

## Global Constraints

- **零运行时依赖**：只用 Node 内置模块，**不得创建 `package.json`**。
- **跑测试一律裸 `node --test`**（仓库根，不带任何路径参数）。本机实测 `node --test tests/` **不会发现**测试文件，只报幻影的 `pass 0 / fail 1`。
- **基线 pass 407 / fail 0**。只许加测试，**不得有任何一条从绿变红**。
- **任何角色的 `tools:` 一律不得含 `Skill` / `SendMessage` / `ListAgents`**（主规格 §6.1）。frontmatter 的 `skills:` 键是允许的，与 `Skill` 工具是两回事。
- **插件自带的 skill 绝不能声明 `context: fork`**。
- **受信前缀 `agent-team 账本回传` 的单一真源是 `hooks/lib/trusted.mjs`**；新增任何 `additionalContext` 回传路径必须走 `trustedBlock(...)`，不得另写字面量。`templates/` 里不得出现该前缀。
- **不要动 master**；分支 `m2a-stage-chain`。
- commit message 末尾加 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。
- **变异验证用 `cp` 备份到临时目录再改回来，不要用 `git checkout` / `git restore`**——本项目被它毁过两次未提交的工作。
- **写含正则的测试代码不要走 heredoc**（backslash 会被吞掉导致正则失效、测试假绿），用 Write 工具。

### 两条测试硬规矩（`docs/11` §3.3，**brief 给的测试代码不豁免**）

1. **一条断言一个 `test()`**——多条**检验不同侧面**的固定断言要拆开。例外：同一断言形状遍历互不耦合的数据点可共块，**但失败消息必须点名具体数据点**。
2. **否定式断言必须有正向自检锚**——「没有 / 不含 / 保持沉默」这类断言，在「遍历的集合是空的」或「这段代码没被执行到」时同样是绿的。

M1c 为第 1 条栽过两次、为第 2 条栽过两次，其中三次源头都是计划正文逐字给的测试代码。**「逐字照用」只豁免值（魔法字符串、措辞、哈希），不豁免测试结构判断。**

---

## File Structure

**新建**

| 文件 | 职责 |
|---|---|
| `hooks/lib/rework-guard.mjs` | H6 的纯函数：给定旧 state 与新 state，判断是否构成「把计数改小」 |
| `tests/rework-guard.test.mjs` | H6 纯函数单测 |
| `tests/gate-rework.test.mjs` | H6 经 `gate.mjs` 的传导链测试（本仓库对每个 `hooks/lib/*.mjs` 的既有分层：纯函数单测 + `gate-<check>` 传导链） |
| `tests/stages.test.mjs` | `stages.mjs` 三个新函数（`stageRoles`/`expandProduces`/`expectedArtifacts`）+ `producedNames` 行为扩展的单测 |
| `agents/at-ui.md` 等五份 | 占位 agent（真正文在 M2b） |
| `docs/14-M2a-实测结论.md` | Task 9 产出 |

**修改**

| 文件 | 改什么 |
|---|---|
| `stages.json` | 加 S6/S7/S8；S5 加 `producers`，`produces` 改 `<role>` 模式 |
| `hooks/lib/stages.mjs` | 加 `stageRoles` / `expandProduces` / `expectedArtifacts`；`producedNames` 支持占位符展开 |
| `hooks/lib/state.mjs` | `isStageDone` 加可选 `roster`；加 `REJECTION_KINDS` / `rejectTo`；头部 ⚠️ 注记改写（写时强制已由 H6 承担） |
| `hooks/lib/writepath.mjs` | `stageOwnerOfRunPath` 支持 `<role>` 展开 |
| `hooks/lib/artifact-drift.mjs` | `compareArtifacts` 改用 `expectedArtifacts(stages, roster)` |
| `hooks/gate.mjs` | 加 `CHECK === 'rework'` 分支；`deliverable` 分支加 `isStageDone` 与新静默判据；`loadRoster()` 加 try/catch |
| `hooks/hooks.json` | `PreToolUse` 写路径 matcher 下注册 `rework` |
| `roster.json` | 五个新角色的边 |
| `templates/project.json` | `available_roles` 与 `paths` 补五个角色 |
| `stages.README.md` | 控制文件清单那三行（`docs/11` §1.5 第 2 点）；扩链后的静默表 |
| `docs/11-M1b-遗留与已知边界.md` | §1.1–§1.5 逐条结清或改状态；新增「推进判据依赖 roster」这条新洞 |
| 主规格 | §4 阶段表、§4.2 ③、§4.3、§6 闸表、§11 |

---

## Task 1: 规格回填与新洞入账

**为什么排第一**：本轮有一条是**新开的洞**，不是功能增加——§1.1 的裁定让「阶段推进判据」与「账本比对」**从此依赖 `roster`**，而 `roster` 是 PM 写的控制文件。PM 漏写一个角色，那个角色的产物就既不拖住推进、也不进账本比对的视野。这与 `docs/13` §6.1 记的「PM 代笔与伪造机制上无法区分」同族，M2a 只是多开一条路径。**这种事最不能只活在设计文档里**——`docs/08` §2.3 记的教训就是「结论落在某一处、没传播到别处」，而 M1c 终审第 1 条发现正是「宣称回填了、实际没回填」。

**Files:**
- Modify: `docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md`（§4 阶段表、§4.2 ③、§4.3、§6 闸表、§11）
- Modify: `docs/11-M1b-遗留与已知边界.md`（新增一节）

**Interfaces:**
- Consumes: 无（纯文档）
- Produces: 后续任务引用的规格小节编号；`docs/11` 里那条新洞的落点

- [ ] **Step 1: §4 阶段表的 S5 那行补说明**

在规格 §4 阶段表下方、那条「M1b 终审补」注记**之后**，追加：

```markdown
> **注记（M2a 补）：上表 S5 的 `05-impl/<role>.md` 里的 `<role>` 是一个模式，不是字面量。**
> `stages.json` 用 `producers` 字段列出这一阶段允许的产出角色，`produces` 写成
> `["05-impl/<role>.md"]`。`<role>` 的展开**按消费方不同而不同**，这一点必须照着来，
> 混用会重演 `docs/11` §5.6 那个缺口：
>
> | 消费方 | `<role>` 展开成 |
> |---|---|
> | H3 写路径 | 写入者自己（角色 R 可写 `05-impl/R.md`，当且仅当 `R ∈ producers`） |
> | 阶段推进判据 `isStageDone` | `roster ∩ producers`（这一趟实际派到的执行角色都交了才算 done） |
> | 账本比对 `compareArtifacts` | `roster ∩ producers`（与推进判据同集合，否则两者互相打架） |
> | `validateState` 的 artifacts 键校验 | **全部 `producers`**（账本里记着任何一个合法产者的文件都该被接受） |
>
> 单一真源是 `hooks/lib/stages.mjs`。M1 期间 `stages.json` 把这个模式写成了字面量
> `["05-impl/at-backend.md"]`，后果见 `docs/11` §5.6。
```

- [ ] **Step 2: §4.2 ③ 补写时强制**

找到 §4.2 ③「返工预算硬上限且不可重置」那段，在段末追加一句：

```markdown
**写时强制由 H6 承担（M2a 补）**：`validateState` 只能在事后报出「计数与 `history` 对不上」，
拦不住那一次写入——要看到改之前那一版，而 `PostToolUse` 看不到。H6 挂在 `PreToolUse` 的
写路径 matcher 上：触发时磁盘上还是旧版、`tool_input` 里是新版，两边都在手上。
`validateState` 的同名校验降为第二道。
```

- [ ] **Step 3: §4.3 补单一真源**

在 §4.3 那张驳回路由表下方追加：

```markdown
**本表的单一真源是 `hooks/lib/state.mjs` 的 `rejectTo(kind)`（M2a 补）。**
`REJECTION_KINDS` 四类：`requirement` / `design` / `implementation` / `contract-conflict`，
前三类分别回到 S2 / S3 / S5，`contract-conflict` 返回 `null`——它不由代码决定回哪，
走 §5.1 的升级路径（`ESCALATION_KINDS` 已含 `contract-conflict`）。
```

- [ ] **Step 4: §6 闸表加 H6 一行**

在 §6 的闸表里，H5 之后加：

```markdown
| H6 | 返工预算 | `PreToolUse` / `^(Edit\|Write\|NotebookEdit)$` | **fail closed** | 只对 `runs/*/state.json` 生效。只拦**减少**（`history` 变短、某阶段出现次数变少、`rework` 低于派生值、`rework` 超 `REWORK_LIMIT`），不碰增加——PM 每推进一个阶段都要正常重写这个文件 |
```

- [ ] **Step 5: §11 补 M2 拆分说明**

在 §11 的 **M2** 段落末尾追加：

```markdown
> **M2 拆分（M2a 设计时补）**：上面这一段是好几个独立子系统，按 M1a/M1b/M1c 的先例拆成
> 三轮——M2a 阶段链与返工闭环、M2b 五个角色真正文、M2c 发布。各自的设计文档在
> `docs/superpowers/specs/`。
```

- [ ] **Step 6: `docs/11` 新增一节记那条新洞**

在 `docs/11-M1b-遗留与已知边界.md` **文件末尾**追加（**§1–§4 原文一个字不改**，这是该文件 §5 开头自己立的规矩）：

```markdown
### 5.7 ⭐ 阶段推进判据与账本比对从此依赖 `roster`（2026-09-19，M2a 设计裁定）

M2a 的 §1.1 裁定让 `isStageDone` 与 `compareArtifacts` 在 S5 把 `<role>` 展开成
**`roster ∩ producers`**——「这一趟实际派到的执行角色都交了才算 done」。

**这是一条新开的洞，不是功能增加**：`roster` 是 `state.json` 的字段，而 `state.json` 是
控制文件、**只有 PM 写得了**。所以——

**PM 漏写（或故意不写）一个角色进 `roster`，那个角色的产物就既不拖住阶段推进、
也不进账本比对的视野。** 不是「被判为合规」，是**不在视野里**。

与 `docs/13` §6.1 记的「PM 代笔与伪造在机制上无法区分」**同族**：PM 有写路径豁免是
设计使然（`docs/09` 账一），所以 PM 对控制文件的话语权永远是完整的。M2a 只是让这份
话语权多覆盖了一样东西（谁算这一趟的执行角色）。

**为什么仍然这么裁**：静态列全五个执行角色会让 `isStageDone` 在「只派了后端和前端」的
run 里**永远为假**，整条链卡死——「S5 的产物集合取决于这一趟派了谁」是这个阶段的固有
性质。退路是把 S5 拆成五个单产者阶段，代价是阶段链长度翻倍、`nextStage` 的线性推进要
改成图。

**M2b/M2c 要想清楚的**：`roster` 本身要不要有写时强制（比如「只许追加，不许删」——
与 H6 对 `history` 的判据同一手法）。本轮没做，因为 M2a 还没有真实的多角色 run 来
检验判据的误报率。
```

- [ ] **Step 7: 自检**

```bash
grep -n "M2a 补" "docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md"
```
预期：**4 处**（§4 注记、§4.2 ③、§4.3、§6 表以外的三处文字；§6 表那行不含这个字样，单独核）

```bash
grep -n "^| H6 " "docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md"
grep -n "5.7 ⭐ 阶段推进判据与账本比对从此依赖" docs/11-M1b-遗留与已知边界.md
```
预期：各 1 处。

```bash
git diff --stat docs/11-M1b-遗留与已知边界.md
```
预期：**只有追加，零删除行**。用 `git diff docs/11-M1b-遗留与已知边界.md | grep -c "^-[^-]"` 确认为 `0`。

- [ ] **Step 8: 跑测试**

Run: `node --test`（仓库根，裸命令）
Expected: `pass 407 / fail 0`——本任务不碰代码，数字不应变。

- [ ] **Step 9: 提交**

```bash
git add docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md docs/11-M1b-遗留与已知边界.md
git commit -F - <<'MSG'
docs: M2a 规格回填 —— 其中一条是新开的洞，不是功能增加

§4 阶段表点破 05-impl/<role>.md 是模式不是字面量，并把四个消费方各自的展开集合写进
注记（混用会重演 docs/11 §5.6）。§4.2 ③ 补写时强制由 H6 承担的理由。§4.3 补单一真源
是 rejectTo()。§6 闸表加 H6。§11 补 M2 拆成 M2a/M2b/M2c。

docs/11 新增 §5.7：§1.1 的裁定让阶段推进判据与账本比对从此依赖 roster，而 roster 是
只有 PM 写得了的控制文件——PM 漏写一个角色，那个角色的产物就不在视野里。与 docs/13
§6.1 同族。仍然这么裁的理由与退路都写在那一节里。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

> **提交命令一律用 `git commit -F - <<'MSG'` 这个形状，不要用 `git commit -m "$(cat <<'EOF' ...)"`。**
> 后者是嵌套 heredoc + 命令替换，本计划写作时在 git-bash 下实测会报
> `unexpected EOF while looking for matching '`。本仓库对这一族坑已经栽过三次
> （`node -e` 里的反引号被 bash 吃掉、heredoc 吞 backslash 导致正则假绿）。

---

## Task 2: 五个占位 agent 与花名册扩张

**为什么排在机制之前**：`stages.json` 的 `S6.role` 是 `at-qa`，`S7.role` 是 `at-acceptance`。`tests/roster-sync.test.mjs` 会校验白名单里的角色都是 `roster.json` 的键，`tests/agents.test.mjs` 会校验 `agents/` 目录恰好是预期的那几个文件——**先有 agent 才能改 `stages.json`**。M1b 当时也是靠 M0 留下的占位符才能先做流程层。

**真正文在 M2b**，本任务只建占位符，形状照抄现有 `agents/at-outsider.md` 的最小结构。

**Files:**
- Create: `agents/at-ui.md`、`agents/at-ios.md`、`agents/at-android.md`、`agents/at-qa.md`、`agents/at-acceptance.md`
- Modify: `roster.json`
- Modify: `templates/project.json`
- Modify: `tests/helpers/expected-agents.mjs`
- Test: `tests/agents.test.mjs`（既有测试会自动覆盖新角色）

**Interfaces:**
- Consumes: 无
- Produces: 五个角色名 `at-ui` / `at-ios` / `at-android` / `at-qa` / `at-acceptance`，供 Task 4 的 `stages.json` 与 Task 3 的 `producers` 引用

- [ ] **Step 1: 先读现有占位符的形状**

```bash
cat agents/at-outsider.md
cat tests/helpers/expected-agents.mjs
```

照它的 frontmatter 字段与正文长度来写，**不要自创结构**。

- [ ] **Step 2: 建五份占位 agent**

每份形状相同，只有 `name` / `description` / `tools` 不同。三个执行角色（`at-ui`/`at-ios`/`at-android`）的 `tools` 与 `at-frontend` 一致（含 `Bash`，理由见主规格 §6.2 与 §3.3.1 的上界模型）；`at-qa` 需要 `Bash` 跑测试；`at-acceptance` 只读。

`agents/at-ui.md`：

```markdown
---
name: at-ui
description: 交互与视觉设计。M2a 占位符，真正文在 M2b。
tools: Bash, Read, Glob, Write, Edit
---

# at-ui（占位）

这是 M2a 建的**占位符**，真正文在 M2b 写。

现在只保证三件事：
- 这个角色在 `roster.json` 里有条目，`stages.json` 引用它不会指向一个不存在的 agent
- `tools:` 行符合主规格 §6.1：**不含 `Skill` / `SendMessage` / `ListAgents`**
- 它不认领任何阶段（M1 的阶段链里没有它的 `role`），H2 fail-open、H5 返回
  `skipped:'role-not-in-stage'`，都是设计内的行为
```

`agents/at-ios.md`、`agents/at-android.md` 同上，把 `at-ui` 换成对应名字、`description` 换成「iOS 端实现」「Android 端实现」。

`agents/at-qa.md`：`description` 写「测试。M2a 占位符，真正文在 M2b。」，`tools: Bash, Read, Glob, Write`。

`agents/at-acceptance.md`：`description` 写「业务验收。M2a 占位符，真正文在 M2b。」，`tools: Read, Glob, Write`（**不给 `Bash`**——验收角色不该改代码，也不需要跑构建）。

- [ ] **Step 3: 改 `tests/helpers/expected-agents.mjs`**

把五个新名字加进去。**这是身份锚的单一真源**，`tests/agents.test.mjs` 与 `tests/tool-surface.test.mjs` 都 import 它。

- [ ] **Step 4: 跑测试，看哪些红**

Run: `node --test`
Expected: **会红**。红的应当是花名册同步类的测试（新 agent 不在 `roster.json` 里）。把红的条目抄进报告——这是下一步的判据。

- [ ] **Step 5: 改 `roster.json`**

五个新角色都加为键。派发边按规格 §3.3 的层级：

```json
{
  "__main__":       { "can_delegate_to": ["at-product", "at-architect"] },
  "at-pm":          { "can_delegate_to": ["at-product", "at-architect"] },
  "at-product":     { "can_delegate_to": ["at-backend", "at-ui"] },
  "at-architect":   { "can_delegate_to": ["at-backend", "at-frontend", "at-ui", "at-ios", "at-android", "at-qa"] },
  "at-backend":     { "can_delegate_to": [] },
  "at-frontend":    { "can_delegate_to": [] },
  "at-ui":          { "can_delegate_to": [] },
  "at-ios":         { "can_delegate_to": [] },
  "at-android":     { "can_delegate_to": [] },
  "at-qa":          { "can_delegate_to": [] },
  "at-acceptance":  { "can_delegate_to": [] },
  "at-outsider":    { "can_delegate_to": [] }
}
```

**两条不变量必须保持**（现有测试钉着）：没有任何角色能派到 `at-pm`；没有任何角色能派到 `at-outsider`。
**`at-acceptance` 不在任何人的 `can_delegate_to` 里**——它由 PM 在 S7 直接派，而 PM 的白名单在 `agents/at-pm.md` 的 `Agent(...)` 里，**那一行本任务不改**（M2b 写真正文时一并处理）。这会让 S7 在 M2a 跑不起来，**这是预期的**：M2a 的验收标准是机制成立，不是十角色真跑通。

- [ ] **Step 6: 改 `templates/project.json`**

```json
{
  "stack": {
    "languages": ["TypeScript"],
    "frameworks": [],
    "package_manager": "npm"
  },
  "available_roles": ["at-product", "at-architect", "at-backend", "at-frontend", "at-ui", "at-ios", "at-android", "at-qa", "at-acceptance"],
  "paths": {
    "at-product": ["docs/product/"],
    "at-architect": ["docs/arch/"],
    "at-backend": ["src/server/", "src/shared/"],
    "at-frontend": ["src/web/", "src/shared/"],
    "at-ui": ["docs/ui/"],
    "at-ios": ["src/ios/"],
    "at-android": ["src/android/"]
  },
  "build": "npm run build",
  "test": "npm test"
}
```

**`at-qa` 与 `at-acceptance` 故意不在 `paths` 里** —— 这正是 `docs/11` §1.3 点名的那件事：「可用班底」**再也不能从 `paths` 的键集合派生**，`available_roles` 是唯一来源。漏了它，`/at` 收尾会静默漏算 `never_invoked`，而那正是规格 §4.2 ④ 引用 aws-samples「安全架构师角色整个多日项目从未被调用且无人发现」要防的东西。

- [ ] **Step 7: 补一条测试钉住「`available_roles` 不是从 `paths` 派生的」**

写进 `tests/templates.test.mjs`：

```js
test('available_roles 不等于 paths 的键集合——at-qa/at-acceptance 不认领路径，班底只能从 available_roles 读', () => {
  const p = JSON.parse(readFileSync(new URL('../templates/project.json', import.meta.url), 'utf8'))
  const pathKeys = Object.keys(p.paths).sort()
  const roles = [...p.available_roles].sort()
  assert.notDeepEqual(roles, pathKeys)
})
```

```js
test('前置条件：available_roles 真的比 paths 的键多，且多出来的正是不认领路径的那两个', () => {
  const p = JSON.parse(readFileSync(new URL('../templates/project.json', import.meta.url), 'utf8'))
  const extra = p.available_roles.filter((r) => !Object.hasOwn(p.paths, r)).sort()
  assert.deepEqual(extra, ['at-acceptance', 'at-qa'])
})
```

**第二条是第一条的正向自检锚**：光有 `notDeepEqual` 时，两个集合以任何方式不等都会绿——包括「`paths` 里多了个 `available_roles` 没有的角色」这种反向错误。

- [ ] **Step 8: 跑测试**

Run: `node --test`
Expected: `pass 409 / fail 0`（407 + 新增 2）。Step 4 里红的那些现在应当全绿。

- [ ] **Step 9: 变异验证**

用 `cp` 备份 `templates/project.json` 到临时目录，然后：把 `at-qa` 与 `at-acceptance` 加进 `paths`（各给一个假路径）→ 裸 `node --test` → **Step 7 第二条必须红**（`extra` 变成空数组）。`cp` 还原，确认回到 409/0。

- [ ] **Step 10: 提交**

```bash
git add agents/ roster.json templates/project.json tests/helpers/expected-agents.mjs tests/templates.test.mjs
git commit -F - <<'MSG'
feat: 五个占位 agent 与花名册扩张 —— at-qa/at-acceptance 故意不认领路径

真正文在 M2b，本轮只建占位符，让 stages.json 的 S6.role/S7.role 有 agent 可指。

at-qa 与 at-acceptance 故意不进 templates/project.json 的 paths——这正是 docs/11 §1.3
点名的那件事：「可用班底」再也不能从 paths 的键集合派生，available_roles 是唯一来源。
补了两条测试钉住这一点，第二条是第一条的正向自检锚（光有 notDeepEqual 时反向错误也会绿）。

at-acceptance 暂不在任何人的 can_delegate_to 里，S7 因此在 M2a 跑不起来——这是预期的，
M2a 的验收标准是机制成立，不是十角色真跑通。at-pm.md 的 Agent(...) 白名单留给 M2b。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 3: `stages.mjs` 的三个新函数与 `isStageDone` 的 roster 参数

**这是整个 M2a 的地基**——Task 4 的四个消费方接线全部从这里取。

**⚠️ 两个集合，不是一个**：`producedNames` 与 `expectedArtifacts` 答的是**不同的问题**，不能合并。`validateState` 校验 `artifacts` 的键时要问「这个名字是不是**任何一个**合法产者的产物」（账本里记着 `05-impl/at-frontend.md` 必须算合法）；`compareArtifacts` 要问「**这一趟**该有哪些」。合并会重演设计 §1.1 那张表警告的混用。

**Files:**
- Modify: `hooks/lib/stages.mjs`
- Modify: `hooks/lib/state.mjs:84-88`（`isStageDone`）与顶部 import
- Create: `tests/stages.test.mjs`
- Modify: `tests/state.test.mjs`

**Interfaces:**
- Consumes: `hooks/lib/stages.mjs` 现有的 `producedNames(stages)` 与文件内的 `isPlainObject`
- Produces:
  - `stageRoles(stage) -> string[]`——`producers ?? [role]`，形状不对返回 `[]`
  - `expandProduces(stage, roles) -> string[]`——`<role>` 按 `roles` 展开；不含占位符的条目原样保留**一次**
  - `expectedArtifacts(stages, roster) -> Set<string>`——`<role>` 只按 `roster ∩ producers` 展开
  - `producedNames(stages) -> Set<string>`——签名不变，改为按**全部** `stageRoles` 展开
  - `isStageDone({stage, stages, artifactExists, roster}) -> boolean`——`roster` 可选

- [ ] **Step 1: 写失败测试 `tests/stages.test.mjs`**

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { stageRoles, expandProduces, producedNames, expectedArtifacts } from '../hooks/lib/stages.mjs'

const S5 = { role: 'at-backend', producers: ['at-backend', 'at-frontend', 'at-ui'], produces: ['05-impl/<role>.md'] }
const S1 = { role: 'at-pm', produces: ['00-contract.md'] }
const STAGES = { S1, S5 }

test('stageRoles：有 producers 时返回 producers，不是 role', () => {
  assert.deepEqual(stageRoles(S5), ['at-backend', 'at-frontend', 'at-ui'])
})

test('stageRoles：没有 producers 时退回 [role]', () => {
  assert.deepEqual(stageRoles(S1), ['at-pm'])
})

test('stageRoles：形状不对时返回空数组，不抛', () => {
  assert.deepEqual(stageRoles(null), [])
})

test('expandProduces：<role> 按给定角色逐个展开', () => {
  assert.deepEqual(expandProduces(S5, ['at-frontend', 'at-ui']), ['05-impl/at-frontend.md', '05-impl/at-ui.md'])
})

test('expandProduces：不含 <role> 的条目原样保留一次，不随角色数量翻倍', () => {
  assert.deepEqual(expandProduces(S1, ['at-pm', 'at-product']), ['00-contract.md'])
})

test('expandProduces：角色集合为空时，带 <role> 的条目一个都不产出', () => {
  assert.deepEqual(expandProduces(S5, []), [])
})

test('producedNames：<role> 按全部 producers 展开——validateState 要接受任何合法产者的文件', () => {
  assert.deepEqual(
    [...producedNames(STAGES)].sort(),
    ['00-contract.md', '05-impl/at-backend.md', '05-impl/at-frontend.md', '05-impl/at-ui.md'],
  )
})

test('expectedArtifacts：<role> 只按 roster ∩ producers 展开', () => {
  assert.deepEqual(
    [...expectedArtifacts(STAGES, ['at-frontend'])].sort(),
    ['00-contract.md', '05-impl/at-frontend.md'],
  )
})

test('expectedArtifacts：roster 里的角色不是这一阶段的 producer 时不产出它的名字', () => {
  assert.deepEqual([...expectedArtifacts({ S5 }, ['at-product'])], [])
})

test('expectedArtifacts：不含 <role> 的条目不受 roster 影响——S1 的产物与谁被派了无关', () => {
  assert.deepEqual([...expectedArtifacts({ S1 }, [])], ['00-contract.md'])
})

test('expectedArtifacts：roster 缺省时退回全部 producers，与 producedNames 一致', () => {
  assert.deepEqual([...expectedArtifacts(STAGES, undefined)].sort(), [...producedNames(STAGES)].sort())
})
```

- [ ] **Step 2: 跑，确认失败**

Run: `node --test`（仓库根，裸命令）
Expected: FAIL，`stageRoles is not a function` 之类。

- [ ] **Step 3: 实现——追加进 `hooks/lib/stages.mjs`，保留该文件原有头部注释**

```js
// M2a：规格 §4 阶段表写的是 `05-impl/<role>.md`——一个**模式**。M1 期间 stages.json 把它
// 写成了字面量 ["05-impl/at-backend.md"]，后果是执行角色干完活写不进自己的实现记录
// （docs/11 §5.6，2026-09-19 的真实 run 里逼 PM 代笔）。这里把模式恢复成模式。
//
// ⚠️ producedNames 与 expectedArtifacts 答的是**不同的问题**，别合并：
//   - producedNames：这个名字是不是**任何一个**合法产者的产物（validateState 校验
//     artifacts 的键用它——账本里记着 05-impl/at-frontend.md 必须算合法）
//   - expectedArtifacts：**这一趟**该有哪些（compareArtifacts 与 isStageDone 用它）
// 混用会重演 docs/11 §5.6 那个缺口。设计 §1.1 的表里写死了四个消费方各自的集合。
const ROLE_TOKEN = '<role>'

/** 这一阶段允许的产出角色。有 producers 用 producers，否则退回 [role]。 */
export function stageRoles(stage) {
  if (!isPlainObject(stage)) return []
  if (Array.isArray(stage.producers)) return stage.producers.filter((r) => typeof r === 'string')
  return typeof stage.role === 'string' ? [stage.role] : []
}

/** produces 里的 <role> 按 roles 展开。不含占位符的条目原样保留**一次**——否则 S1 的
 * 00-contract.md 会随角色数量翻倍。 */
export function expandProduces(stage, roles) {
  const out = []
  if (!isPlainObject(stage) || !Array.isArray(stage.produces)) return out
  const list = Array.isArray(roles) ? roles.filter((r) => typeof r === 'string') : []
  for (const p of stage.produces) {
    if (typeof p !== 'string') continue
    if (!p.includes(ROLE_TOKEN)) { out.push(p); continue }
    for (const r of list) out.push(p.split(ROLE_TOKEN).join(r))
  }
  return out
}

/** 这一趟**该有**的产物名。<role> 只按 roster ∩ producers 展开；不含占位符的条目不受
 * roster 影响（S1 的产物与谁被派了无关）。roster 缺省时退回全部 producers。 */
export function expectedArtifacts(stages, roster) {
  const out = new Set()
  if (!isPlainObject(stages)) return out
  const inRun = Array.isArray(roster) ? new Set(roster.filter((r) => typeof r === 'string')) : null
  for (const s of Object.values(stages)) {
    const roles = stageRoles(s)
    const scoped = inRun === null ? roles : roles.filter((r) => inRun.has(r))
    for (const n of expandProduces(s, scoped)) out.add(n)
  }
  return out
}
```

把文件里现有的 `producedNames` 整个替换成走同一套展开：

```js
export function producedNames(stages) {
  const out = new Set()
  if (!isPlainObject(stages)) return out
  for (const s of Object.values(stages)) {
    for (const n of expandProduces(s, stageRoles(s))) out.add(n)
  }
  return out
}
```

- [ ] **Step 4: 改 `isStageDone`（`hooks/lib/state.mjs:84`）与顶部 import**

import 那行改成：

```js
import { producedNames, stageRoles, expandProduces } from './stages.mjs'
```

函数体：

```js
// M2a：加可选 roster。S5 的产物集合取决于这一趟派了谁（producers × roster），静态列全
// 五个执行角色会让只派了两个角色的 run 永远不 done、整条链卡死。roster 缺省时退回全部
// producers——S1–S4/S6–S8 没有 producers，行为完全不变，tests/state.test.mjs 现有的
// 6 条 isStageDone 测试因此不需要改签名、必须仍然全绿。
export function isStageDone({ stage, stages, artifactExists, roster }) {
  const current = isPlainObject(stages) ? stages[stage] : undefined
  if (!current) return false
  const roles = stageRoles(current)
  const scoped = Array.isArray(roster) ? roles.filter((r) => roster.includes(r)) : roles
  const names = expandProduces(current, scoped)
  if (names.length === 0) return false
  return names.every((p) => artifactExists(p))
}
```

- [ ] **Step 5: 给 `isStageDone` 补 roster 测试（追加进 `tests/state.test.mjs`）**

```js
const S5_MULTI = {
  S5: { role: 'at-backend', producers: ['at-backend', 'at-frontend'], produces: ['05-impl/<role>.md'] },
}

test('isStageDone：S5 只按 roster 里的执行角色判——没派到的角色不拖住推进', () => {
  const done = isStageDone({
    stage: 'S5', stages: S5_MULTI, roster: ['at-backend'],
    artifactExists: have('05-impl/at-backend.md'),
  })
  assert.equal(done, true)
})

test('isStageDone：roster 里有两个执行角色而只交了一个时为 false', () => {
  const done = isStageDone({
    stage: 'S5', stages: S5_MULTI, roster: ['at-backend', 'at-frontend'],
    artifactExists: have('05-impl/at-backend.md'),
  })
  assert.equal(done, false)
})

test('isStageDone：roster 缺省时按全部 producers 判——不传 roster 不等于不判', () => {
  const done = isStageDone({
    stage: 'S5', stages: S5_MULTI,
    artifactExists: have('05-impl/at-backend.md'),
  })
  assert.equal(done, false)
})
```

（`have(...)` 是该文件里已有的夹具助手，直接用，不要另写一份。）

- [ ] **Step 6: 跑测试**

Run: `node --test`
Expected: `pass 423 / fail 0`（409 + 11 + 3）。

**`tests/state.test.mjs` 原有的 5 条 `isStageDone` 测试必须仍然全绿**——它们不传 `roster`，走「退回全部 producers」那条路。若它们红了，说明改动波及了不该波及的阶段，**停下来查，不要改那 5 条去迁就实现**。

- [ ] **Step 7: 变异验证（`cp` 备份，逐条做、每条做完还原）**

1. `stageRoles` 恒返回 `[]` → `stageRoles` 三条 + 其余多条必须红
2. `expandProduces` 里 `if (!p.includes(ROLE_TOKEN)) { out.push(p); continue }` 这一行删掉（让不含占位符的条目也走展开分支）→「不含 `<role>` 的条目原样保留一次」那条必须红
3. `expectedArtifacts` 的 `inRun` 过滤去掉（恒用全部 `roles`）→「只按 roster ∩ producers 展开」那条必须红
4. `isStageDone` 的 `scoped` 恒等于 `roles` → Step 5 第一条必须红

每次还原后确认回到 `423 / 0` 且 `git status --short` 为空。

- [ ] **Step 8: 提交**

```bash
git add hooks/lib/stages.mjs hooks/lib/state.mjs tests/stages.test.mjs tests/state.test.mjs
git commit -F - <<'MSG'
feat: stages.mjs 恢复 <role> 模式 —— 两个集合，不是一个

规格 §4 写的是 05-impl/<role>.md 一个模式，M1 期间被实现成了字面量，后果是执行角色
干完活写不进自己的实现记录（docs/11 §5.6）。

producedNames 与 expectedArtifacts 答的是不同的问题，故意分成两个函数：前者是「这个
名字是不是任何一个合法产者的产物」（validateState 校验 artifacts 的键用它），后者是
「这一趟该有哪些」（compareArtifacts 与 isStageDone 用它）。合并会重演 §5.6 那个缺口。

isStageDone 的 roster 做成可选：缺省时退回全部 producers，S1–S4/S6–S8 行为完全不变，
state.test.mjs 原有的 6 条测试不改签名且必须仍然全绿——这既是兼容性要求也是变异锚。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 4: `stages.json` 扩到 S8，四个消费方接线

**裁定（设计文档没覆盖，这里补上）：`requires` 里不放 `<role>`。**
S6 逻辑上要求「S5 的实现都交了」，但那个集合是 roster 依赖的，而 `requires` 由 H2 就绪门禁按静态名字查。把 `<role>` 放进 `requires` 会逼 `readiness.mjs` 也做 roster 展开，把 roster 依赖扩散到第五个消费方。**S6 的 `requires` 只写静态产物（`03-arch.md` / `04-dispatch.md`）；「S5 是否真交齐了」由阶段推进判据 `isStageDone(S5, roster)` 管——那本来就是它的职责。** 若错：S6 可能在 S5 没交齐时就被放行，而 `isStageDone` 会挡住推进，两道判据一前一后，不会漏。

**Files:**
- Modify: `stages.json`
- Modify: `hooks/lib/writepath.mjs`（`stageOwnerOfRunPath`）
- Modify: `hooks/lib/artifact-drift.mjs`（`compareArtifacts` 加 `roster`）
- Modify: `hooks/gate.mjs`（`deliverable` 分支把 `ctx.state?.roster` 传给 `compareArtifacts`）
- Modify: `stages.README.md`
- Test: `tests/stages.test.mjs`、`tests/writepath.test.mjs`、`tests/artifact-drift.test.mjs`

**Interfaces:**
- Consumes: Task 3 的 `stageRoles` / `expandProduces` / `expectedArtifacts`
- Produces: 完整的 S1–S8 阶段链；`compareArtifacts({artifacts, stages, artifactBytes, roster})`

- [ ] **Step 1: 写 `stages.json`**

```json
{
  "S1": { "role": "at-pm",         "requires": [],                                "produces": ["00-contract.md"] },
  "S2": { "role": "at-product",    "requires": ["00-contract.md"],                "produces": ["01-prd.md"] },
  "S3": { "role": "at-architect",  "requires": ["00-contract.md", "01-prd.md"],   "produces": ["03-arch.md"] },
  "S4": { "role": "at-pm",         "requires": ["01-prd.md", "03-arch.md"],       "produces": ["04-dispatch.md"] },
  "S5": { "role": "at-backend",
          "producers": ["at-backend", "at-frontend", "at-ui", "at-ios", "at-android"],
          "requires": ["03-arch.md", "04-dispatch.md"],
          "produces": ["05-impl/<role>.md"] },
  "S6": { "role": "at-qa",         "requires": ["03-arch.md", "04-dispatch.md"],  "produces": ["06-test.md"] },
  "S7": { "role": "at-acceptance", "requires": ["01-prd.md", "06-test.md"],       "produces": ["07-acceptance.md"] },
  "S8": { "role": "at-pm",         "requires": ["06-test.md", "07-acceptance.md"], "produces": ["08-delivery.md"] }
}
```

**`S5.role` 保留 `at-backend`**：`role` 现在的语义是「这一阶段的主执行者」，`deliverable.mjs`/`readiness.mjs` 仍按它判。`producers` 是新增的、只影响产物槽位。**不要删 `role`**——删了会让那两个模块拿到 `undefined`。

- [ ] **Step 2: 补一条不变量测试（`tests/stages.test.mjs`）**

带 `producers` 的阶段，它的 `produces` 每一条都必须含 `<role>`。否则 `stageOwnerOfRunPath` 会把一个静态产物名归给 `producers[0]`，归属就错了。

```js
test('不变量：带 producers 的阶段，produces 每一条都必须含 <role>', () => {
  const stages = JSON.parse(readFileSync(new URL('../stages.json', import.meta.url), 'utf8'))
  const bad = []
  for (const [id, s] of Object.entries(stages)) {
    if (!Array.isArray(s.producers)) continue
    for (const p of s.produces) if (!p.includes('<role>')) bad.push(`${id}:${p}`)
  }
  assert.deepEqual(bad, [])
})

test('前置条件：stages.json 里确实有带 producers 的阶段——上一条不是空转', () => {
  const stages = JSON.parse(readFileSync(new URL('../stages.json', import.meta.url), 'utf8'))
  const withProducers = Object.keys(stages).filter((id) => Array.isArray(stages[id].producers))
  assert.deepEqual(withProducers, ['S5'])
})
```

- [ ] **Step 3: 改 `stageOwnerOfRunPath`（`hooks/lib/writepath.mjs`）**

```js
export function stageOwnerOfRunPath(stages, rd, target) {
  if (!stages || typeof stages !== 'object') return null
  for (const [stageId, s] of Object.entries(stages)) {
    // M2a：<role> 逐个角色展开，返回的 role 是**匹配上的那个具体产者**，不是 s.role。
    // H3 要回答的是「这条路径归谁」，S5 的 05-impl/at-frontend.md 归 at-frontend，
    // 不归 s.role（at-backend）。
    for (const role of stageRoles(s)) {
      for (const p of expandProduces(s, [role])) {
        if (norm(`${rd}/${p}`) === target) return { stageId, role, produces: p }
      }
    }
  }
  return null
}
```

顶部加 import：`import { stageRoles, expandProduces } from './stages.mjs'`

- [ ] **Step 4: 给 H3 补测试（`tests/writepath.test.mjs`）**

```js
test('S5 多产者：at-frontend 写自己的实现记录，归属算到 at-frontend 而不是 S5.role', () => {
  const owner = stageOwnerOfRunPath(STAGES_M2A, '/p/.agent-team/runs/r1', norm('/p/.agent-team/runs/r1/05-impl/at-frontend.md'))
  assert.equal(owner.role, 'at-frontend')
})

test('S5 多产者：不在 producers 里的角色名不产生归属', () => {
  const owner = stageOwnerOfRunPath(STAGES_M2A, '/p/.agent-team/runs/r1', norm('/p/.agent-team/runs/r1/05-impl/at-outsider.md'))
  assert.equal(owner, null)
})
```

`STAGES_M2A` 用该文件里既有的夹具风格自建一份（含 `producers` 的 S5），不要读真 `stages.json`——单测要能独立于真实配置。

- [ ] **Step 5: 改 `compareArtifacts`（`hooks/lib/artifact-drift.mjs`）**

签名加 `roster`，内部把 `producedNames(stages)` 换成 `expectedArtifacts(stages, roster)`：

```js
export function compareArtifacts({ artifacts, stages, artifactBytes, roster }) {
  const empty = { drifted: [], missing: [], unrecorded: [] }
  if (typeof artifactBytes !== 'function') return empty
  const recorded = isPlainObject(artifacts) ? artifacts : {}
  if (!isPlainObject(stages)) return empty

  // M2a：从 producedNames（全部可能的产物名）换成 expectedArtifacts（这一趟该有的）。
  // 两者答的是不同的问题，见 stages.mjs 头部。用错会让没派到的角色的产物被报成 missing。
  const produced = expectedArtifacts(stages, roster)
  ...
```

import 改成 `import { expectedArtifacts } from './stages.mjs'`。

- [ ] **Step 6: `gate.mjs` 把 roster 传进去**

找到 `deliverable` 分支里 `compareArtifacts({...})` 的调用点，加一项：

```js
roster: Array.isArray(ctx.state?.roster) ? ctx.state.roster : undefined,
```

**`undefined` 而不是 `[]`**：`expectedArtifacts` 把 `undefined` 当「退回全部 producers」，把 `[]` 当「这一趟一个执行角色都没派」。`state.json` 坏掉时应当退回**更宽**的集合（多报几条，不要漏报）。

- [ ] **Step 7: 给账本比对补测试（`tests/artifact-drift.test.mjs`）**

```js
test('S5 多产者：roster 里没有的角色，它的实现记录不会被报成 missing', () => {
  const r = compareArtifacts({
    artifacts: {}, stages: STAGES_M2A, roster: ['at-backend'],
    artifactBytes: bytesOf({}),
  })
  assert.deepEqual(r.missing, [])
})

test('S5 多产者：roster 里有的角色，它的实现记录记了而磁盘没有时报 missing', () => {
  const r = compareArtifacts({
    artifacts: { '05-impl/at-frontend.md': A }, stages: STAGES_M2A, roster: ['at-frontend'],
    artifactBytes: bytesOf({}),
  })
  assert.deepEqual(r.missing, [{ name: '05-impl/at-frontend.md', recorded: A }])
})
```

- [ ] **Step 8: 改 `stages.README.md`**

两处：(a) `docs/11` §1.5 第 2 点说的「又抄了一遍四项控制文件清单」——改成指向 `hooks/lib/control-files.mjs` 的单一真源，不再抄；(b) 加一段说明 `producers` 与 `<role>`，并指向主规格 §4 的 M2a 注记。

- [ ] **Step 9: 跑测试**

Run: `node --test`
Expected: `pass 429 / fail 0`（423 + 6）。**既有的 `gate-deliverable` / `gate-writepath` / `gate-ledger` 测试必须全绿**——它们用的夹具 `stages.json` 是真实文件，S5 形状变了。若红，先看是夹具期望过时还是接线错了，**不要直接改测试去迁就**。

- [ ] **Step 10: 变异验证**

1. `stageOwnerOfRunPath` 里 `return { stageId, role, produces: p }` 的 `role` 改回 `s.role` → Step 4 第一条必须红
2. `compareArtifacts` 改回 `producedNames(stages)` → Step 7 第一条必须红
3. `gate.mjs` 传的 `roster` 改成恒 `undefined` → 需要有测试红；若没有，**补一条**（这是接线，纯函数测试覆盖不到）

- [ ] **Step 11: 提交**

```bash
git add stages.json hooks/lib/writepath.mjs hooks/lib/artifact-drift.mjs hooks/gate.mjs stages.README.md tests/
git commit -F - <<'MSG'
feat: 阶段链接到 S8，四个消费方各按自己的集合展开 <role>

stages.json 加 S6/S7/S8；S5 加 producers 与 <role> 模式。

stageOwnerOfRunPath 返回的 role 改成「匹配上的那个具体产者」而不是 s.role——H3 要回答
的是「这条路径归谁」，05-impl/at-frontend.md 归 at-frontend，不归 S5.role。

compareArtifacts 从 producedNames 换成 expectedArtifacts(stages, roster)：用错会让没派到
的角色的产物被报成 missing。gate.mjs 传 ctx.state?.roster，坏掉时传 undefined 而不是 []
——退回更宽的集合，多报几条不要漏报。

裁定：requires 里不放 <role>。S6 逻辑上要求「S5 都交了」，但那个集合是 roster 依赖的，
放进 requires 会逼 readiness.mjs 也做 roster 展开，把依赖扩散到第五个消费方。S6 的
requires 只写静态产物，「S5 是否真交齐」由 isStageDone 管——那本来就是它的职责。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 5: H6 返工预算写时强制

**`docs/11` §1.2 卡了一个里程碑，是因为那句话只排除了一半**：「要看到改之前那一版，`PostToolUse` 看不到」——对，但 `PreToolUse` 看得到。触发时磁盘上还是旧版、`tool_input` 里是新版。

**Files:**
- Create: `hooks/lib/rework-guard.mjs`、`tests/rework-guard.test.mjs`、`tests/gate-rework.test.mjs`
- Modify: `hooks/gate.mjs`（加 `CHECK === 'rework'` 分支）、`hooks/hooks.json`、`hooks/lib/state.mjs` 头部 ⚠️ 注记

**Interfaces:**
- Consumes: `reworkFromHistory(history)`、`REWORK_LIMIT`（均已在 `hooks/lib/state.mjs` 导出）
- Produces: `decideRework({ before, after }) -> { ok: true } | { ok: false, reason: string }`

- [ ] **Step 1: 写失败测试 `tests/rework-guard.test.mjs`（一条断言一个 test）**

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { decideRework } from '../hooks/lib/rework-guard.mjs'

const H = (...stages) => stages.map((s) => ({ stage: s, at: '2026-09-19T00:00:00Z' }))
const st = (history, rework) => ({ stage: 'S5', history, rework })

test('正常推进：history 追加一条，放行', () => {
  const r = decideRework({ before: st(H('S1', 'S2'), {}), after: st(H('S1', 'S2', 'S3'), {}) })
  assert.equal(r.ok, true)
})

test('正常返工：history 追加重复阶段、rework 跟着涨，放行', () => {
  const r = decideRework({ before: st(H('S1', 'S5'), {}), after: st(H('S1', 'S5', 'S5'), { S5: 1 }) })
  assert.equal(r.ok, true)
})

test('history 整体变短：拒', () => {
  const r = decideRework({ before: st(H('S1', 'S2', 'S3'), {}), after: st(H('S1', 'S2'), {}) })
  assert.equal(r.ok, false)
})

test('history 长度没变但某阶段出现次数变少：拒', () => {
  const r = decideRework({ before: st(H('S1', 'S5', 'S5'), { S5: 1 }), after: st(H('S1', 'S5', 'S2'), {}) })
  assert.equal(r.ok, false)
})

test('rework 低于 history 派生值：拒', () => {
  const r = decideRework({ before: st(H('S1', 'S5', 'S5'), { S5: 1 }), after: st(H('S1', 'S5', 'S5'), { S5: 0 }) })
  assert.equal(r.ok, false)
})

test('rework 超过硬上限 3：拒', () => {
  const h = H('S5', 'S5', 'S5', 'S5', 'S5')
  const r = decideRework({ before: st(H('S5'), {}), after: st(h, { S5: 4 }) })
  assert.equal(r.ok, false)
})

test('旧 state 不存在（本趟第一次写）：放行', () => {
  const r = decideRework({ before: null, after: st(H('S1'), {}) })
  assert.equal(r.ok, true)
})

test('新内容不是合法 JSON 对象：放行，不拒——拒了会让人连修回去都做不到', () => {
  const r = decideRework({ before: st(H('S1'), {}), after: null })
  assert.equal(r.ok, true)
})

test('拒的时候要说清是哪一条判据、哪个阶段', () => {
  const r = decideRework({ before: st(H('S1', 'S5', 'S5'), { S5: 1 }), after: st(H('S1', 'S5', 'S5'), { S5: 0 }) })
  assert.match(r.reason, /S5/)
})
```

- [ ] **Step 2: 跑，确认失败**

Run: `node --test` → FAIL（模块不存在）

- [ ] **Step 3: 实现 `hooks/lib/rework-guard.mjs`**

```js
// H6 返工预算写时强制（规格 §4.2 ③、M2a 设计 §1.2）。
//
// 为什么是 PreToolUse：docs/11 §1.2 记的是「拦住一次『把计数改小』的写入要看到改之前
// 那一版，PostToolUse 看不到」。这句话对，但它只排除了 PostToolUse——PreToolUse 触发时
// 磁盘上还是旧版、tool_input 里是新版，两边都在手上。
//
// ⚠️ 只拦**减少**，不碰增加。PM 每推进一个阶段都要正常重写 state.json，拦增加会把整条
// 链锁死。四条判据全部是「新的比旧的少」的形状。
//
// ⚠️ parse 不出来就放行。一个写坏的 state.json 会被 ledger 的 kind:'state' 报出来；
// 在这里 deny 会让 PM 连「把写坏的文件修回去」都做不到——与 gate.mjs 里 I2 豁免同一个
// 理由（不要把运维人逼进死角）。
import { reworkFromHistory, REWORK_LIMIT } from './state.mjs'

function isPlainObject(v) {
  return v !== null && typeof v === 'object' && !Array.isArray(v)
}

function counts(history) {
  const c = {}
  if (!Array.isArray(history)) return c
  for (const e of history) {
    if (isPlainObject(e) && typeof e.stage === 'string') c[e.stage] = (c[e.stage] ?? 0) + 1
  }
  return c
}

export function decideRework({ before, after }) {
  // 旧的读不出来（本趟第一次写）或新的 parse 不出来：放行，理由见头部。
  if (!isPlainObject(before) || !isPlainObject(after)) return { ok: true }

  const hb = Array.isArray(before.history) ? before.history : []
  const ha = Array.isArray(after.history) ? after.history : []

  if (ha.length < hb.length) {
    return { ok: false, reason: `history 从 ${hb.length} 条变成 ${ha.length} 条——阶段进入日志只许追加，不许删。返工计数是它的派生量（规格 §4.2 ③），删 history 等于改计数。` }
  }

  const cb = counts(hb)
  const ca = counts(ha)
  for (const [stage, n] of Object.entries(cb)) {
    if ((ca[stage] ?? 0) < n) {
      return { ok: false, reason: `history 里 ${stage} 的出现次数从 ${n} 变成 ${ca[stage] ?? 0}——只许追加，不许删。` }
    }
  }

  const derived = reworkFromHistory(ha)
  const rw = isPlainObject(after.rework) ? after.rework : {}
  for (const [stage, n] of Object.entries(derived)) {
    const v = rw[stage] ?? 0
    if (v < n) {
      return { ok: false, reason: `rework["${stage}"] 写成 ${v}，但 history 里 ${stage} 出现 ${n + 1} 次、派生值是 ${n}——返工计数不可重置（规格 §4.2 ③）。` }
    }
  }
  for (const [stage, v] of Object.entries(rw)) {
    if (typeof v === 'number' && v > REWORK_LIMIT) {
      return { ok: false, reason: `rework["${stage}"] 是 ${v}，超过硬上限 ${REWORK_LIMIT}（规格 §4.2 ③：第 ${REWORK_LIMIT} 轮终局，不过则升级）。` }
    }
  }
  return { ok: true }
}
```

- [ ] **Step 4: 跑，确认通过**

Run: `node --test` → 新增 9 条全绿。

- [ ] **Step 5: 接进 `gate.mjs`**

在 `CHECK === 'contract'` 分支之后加一个 `CHECK === 'rework'` 分支。要点：

1. 只对 `runs/*/state.json` 生效——复用 `hooks/lib/control-files.mjs` 的判据，**不要另写一份路径匹配**
2. 读旧文件：`readFileSync(target)`，读不到当 `before = null`
3. 算新内容：
   - `tool_name === 'Write'` → `input.tool_input.content`
   - `tool_name === 'Edit'` → 读旧文本，按 `old_string`/`new_string`/`replace_all` **自己套用一次替换**
   - `tool_name === 'NotebookEdit'` → 不适用，直接放行
4. 两边 `JSON.parse` 包 try/catch，失败当 `null`（→ 放行）
5. `decideRework` 返回 `ok:false` 时走既有的 `denyAndExit(r.reason, ...)`，**fail closed**

- [ ] **Step 6: 注册进 `hooks/hooks.json`**

在 `PreToolUse` 的 `^(Edit|Write|NotebookEdit)$` 那一组里，追加第三个 hook：

```json
{
  "type": "command",
  "command": "node",
  "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs", "rework"],
  "timeout": 30,
  "statusMessage": "校验返工预算…"
}
```

- [ ] **Step 7: 写传导链测试 `tests/gate-rework.test.mjs`**

用 `tests/helpers/gate-runner.mjs` 的 `run` 与 `GATE`（**不要另算一份 GATE 路径**——win32 上 `URL.pathname` 前面带斜杠，`gate-ledger.test.mjs` 头部记着这个坑）。至少四条：

1. 正常推进的 Write → 放行（exit 0，无 deny）
2. 把 `history` 删一条的 Write → deny，stderr 含 `只许追加`
3. 把 `rework` 改小的 Edit → deny（**这条专门覆盖 Edit 路径**，Step 5 第 3 点那段自己套用替换的逻辑只有它能测到）
4. 写的不是 `state.json`（比如 `01-prd.md`）→ 放行，**且这条是前 3 条的正向自检锚**：证明这个检查项真的被执行到了、只是对无关路径保持沉默

- [ ] **Step 8: 改 `hooks/lib/state.mjs` 头部那段 ⚠️ 注记**

现在写的是「本模块只做校验，不做写时强制……它属于 M2，和 S6/S7 一起设计」。M2a 做完了，改成指向 H6：

```js
// ⚠️ 本模块只做校验，不做写时强制——写时强制由 H6（hooks/lib/rework-guard.mjs）承担，
// M2a 补。理由见那个文件头部：PostToolUse 看不到改之前那一版，PreToolUse 看得到。
// validateState 的同名校验降为第二道（事后告警），两道都要在。
```

- [ ] **Step 9: 跑测试**

Run: `node --test`
Expected: `pass 442 / fail 0`（429 + 9 + 4）。

- [ ] **Step 10: 变异验证**

1. `decideRework` 恒返回 `{ ok: true }` → Step 1 的四条 deny 测试 + Step 7 的两条必须红
2. 四条判据各短路一条 → 各自对应的那条必须红
3. `gate.mjs` 的 Edit 分支不做替换、直接用旧文本 → Step 7 第 3 条必须红
4. `hooks.json` 里把 `rework` 那项删掉 → Step 7 前三条必须红（**这条验的是注册，不是逻辑**）

- [ ] **Step 11: 提交**

```bash
git add hooks/lib/rework-guard.mjs hooks/gate.mjs hooks/hooks.json hooks/lib/state.mjs tests/rework-guard.test.mjs tests/gate-rework.test.mjs
git commit -F - <<'MSG'
feat: H6 返工预算写时强制 —— docs/11 §1.2 那句话只排除了一半

§1.2 记的是「拦住一次把计数改小的写入要看到改之前那一版，PostToolUse 看不到」。
这句话对，但它只排除了 PostToolUse——PreToolUse 触发时磁盘上还是旧版、tool_input 里
是新版，两边都在手上。挂在既有的写路径 matcher 上，fail closed。

四条判据全部是「新的比旧的少」的形状，只拦减少不碰增加——PM 每推进一个阶段都要正常
重写 state.json，拦增加会把整条链锁死。

Edit 路径单独处理：读旧文本、按 old_string/new_string/replace_all 自己套用一次替换再
parse。传导链测试里有一条专测它——纯函数测试覆盖不到那段。

parse 不出来就放行：写坏的 state.json 由 ledger 的 kind:'state' 报出来，在这里 deny
会让 PM 连把文件修回去都做不到，与 gate.mjs 的 I2 豁免同一个理由。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 6: H5a 静默判据重算（`docs/11` §1.1 强制）

**扩链强制触发**：H5a 那条「哑火告警」现在在「返回角色能传递派发到当前阶段执行角色」时静默。M1 里只有停在 S5 会被静默，而 **S5 是 M1 的最后一段——「停在 S5」在 M1 里不存在**。接上 S6–S8 之后，「实际在 S6、`state.stage` 停在 S5」就是标准形状，而那正是这条告警存在的理由。

**Files:**
- Modify: `hooks/gate.mjs:621` 附近（`deliverable` 分支的静默条件）
- Modify: `stages.README.md`（那张静默表）
- Modify: `docs/11-M1b-遗留与已知边界.md`（§1.1 标记结清）
- Test: `tests/gate-deliverable.test.mjs`

**Interfaces:**
- Consumes: Task 3 的 `isStageDone({..., roster})`
- Produces: 无新接口

- [ ] **Step 1: 看清现状**

`hooks/gate.mjs` 的 `deliverable` 分支里，告警触发条件当前逐字是：

```js
if (CHECK === 'deliverable' && r.skipped === 'role-not-in-stage' && !isCoordinatorFor(ctx, role)) {
```

**注意它是「告警触发」条件，静默是它的反面。**

- [ ] **Step 2: 写失败测试（`tests/gate-deliverable.test.mjs`，三行真值各一条）**

```js
test('H5a：协调者返回且当前阶段未 done —— 静默（这是合法的层级协调）', () => {
  // 夹具：stage S5，05-impl/ 下一个文件都没有，返回角色是能派到 at-backend 的 at-architect
  // 断言 stdout 为空
})

test('H5a：协调者返回但当前阶段已经 done —— 报（这正是「停在旧阶段」）', () => {
  // 夹具：stage S5，roster 只有 at-backend，05-impl/at-backend.md 已在磁盘上
  // 断言 stdout 非空且含「停在旧阶段」
})

test('H5a：非协调者返回 —— 报（判据这一半不变）', () => {
  // 夹具：返回角色派不到当前阶段的 role
})

test('前置条件：上面三条的夹具互不相同——否则三条测的是同一个场景', () => {
  // 断言三份夹具的 (stage, roster, 磁盘产物) 三元组两两不等
})
```

**第四条是前三条的锚**：三条否定/肯定断言若共用同一个夹具，改一处实现可能让三条一起绿而什么都没守住。

- [ ] **Step 3: 改判据**

```js
// M2a（docs/11 §1.1）：原判据只看「谁能派到谁」，用可达性近似区分「协调者合法返回」与
// 「停在旧阶段」。扩到 S6–S8 之后这个近似不成立了——实际在 S6、stage 停在 S5 时，任何
// 能传递派到 at-backend 的协调者返回都会被静默，而那正是这条告警要抓的东西。
// 补上「且当前阶段尚未 done」之后两者就分开了：没推进的充分判据是产物还没齐。
const stageDone = isStageDone({
  stage: ctx.state?.stage,
  stages: ctx.stages,
  artifactExists: ctx.artifactExists,
  roster: Array.isArray(ctx.state?.roster) ? ctx.state.roster : undefined,
})
if (CHECK === 'deliverable' && r.skipped === 'role-not-in-stage' && (!isCoordinatorFor(ctx, role) || stageDone)) {
```

**这是新增一处 `isStageDone` 调用**——现有那处在 `ledger` 分支（`gate.mjs:514`，`CHECK === 'ledger'` 起于 `:412`），与这里不是同一个分支，不能复用。

- [ ] **Step 4: 跑测试**

Run: `node --test`
Expected: `pass 446 / fail 0`（442 + 4）。既有的 H5a 测试（`S5 派 at-architect 去分发（正路）：H5a 不发 warning`）**必须仍然绿**——那条的夹具里 S5 产物没齐，走的是新判据的第一行。

- [ ] **Step 5: 改 `stages.README.md` 的静默表**

把 `docs/11` §1.1 那张四行表更新成 S1–S8 的版本，并写明判据已从「可达性」改成「可达性 **且** 未 done」。

- [ ] **Step 6: `docs/11` §1.1 标记结清**

**§1–§4 原文一个字不改**——在文件末尾的 §5 区追加一条 `### 5.8`，说明 §1.1 已由 M2a Task 6 结清，并指向新判据与 `stages.README.md`。同时把 §1.1 那条「账本比对不受静默表约束」的追记结论也记进去：**账本比对是独立信号（产物内容对不对得上账），与阶段推进与否无关，裁定为继续不受静默约束。**

- [ ] **Step 7: 变异验证**

1. 判据里的 `|| stageDone` 去掉 → Step 2 第二条必须红
2. `stageDone` 的 `roster` 传成恒 `undefined` → 若无测试红，说明第二条的夹具没覆盖 roster 影响，**补一条**

- [ ] **Step 8: 提交**

```bash
git add hooks/gate.mjs stages.README.md docs/11-M1b-遗留与已知边界.md tests/gate-deliverable.test.mjs
git commit -F - <<'MSG'
fix: H5a 静默判据重算 —— 扩链之后「可达性」这个近似不成立了

docs/11 §1.1 点名的入口条件。原判据用「返回角色能不能传递派到当前阶段的 role」近似
区分「协调者合法返回」与「停在旧阶段」。M1 里只有停在 S5 会被静默，而 S5 是 M1 的
最后一段——「停在 S5」在 M1 里不存在，所以那张表当时是完整的。

接上 S6–S8 之后缺一角：实际在 S6、stage 停在 S5 时，任何能传递派到 at-backend 的
协调者返回都会被静默，而那正是这条告警存在的理由。

补上「且当前阶段尚未 done」——没推进的充分判据是产物还没齐，不是谁能派到谁。
三行真值各一条测试，外加一条「三份夹具互不相同」的前置锚。

这是新增一处 isStageDone 调用：现有那处在 ledger 分支，与 deliverable 分支不是同一个。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 7: 驳回路由 `rejectTo`

规格 §4.3 那张表**现在没有任何代码消费它**。`nextStage` 只会前进一格，没有任何回退路径。

**Files:**
- Modify: `hooks/lib/state.mjs`（加 `REJECTION_KINDS` / `rejectTo`）
- Test: `tests/state.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `REJECTION_KINDS: string[]`、`rejectTo(kind) -> 'S2' | 'S3' | 'S5' | null`

- [ ] **Step 1: 写失败测试（一条断言一个 test）**

```js
test('rejectTo：需求理解错回到 S2', () => {
  assert.equal(rejectTo('requirement'), 'S2')
})

test('rejectTo：设计错回到 S3', () => {
  assert.equal(rejectTo('design'), 'S3')
})

test('rejectTo：实现错回到 S5', () => {
  assert.equal(rejectTo('implementation'), 'S5')
})

test('rejectTo：契约内在矛盾不由代码决定回哪，返回 null 走升级', () => {
  assert.equal(rejectTo('contract-conflict'), null)
})

test('rejectTo：不认识的 kind 返回 null，不抛', () => {
  assert.equal(rejectTo('nonsense'), null)
})

test('前置条件：REJECTION_KINDS 恰好是规格 §4.3 的四类', () => {
  assert.deepEqual([...REJECTION_KINDS], ['requirement', 'design', 'implementation', 'contract-conflict'])
})

test('contract-conflict 同时也是 ESCALATION_KINDS 的一类——它走升级不走路由', () => {
  assert.ok(ESCALATION_KINDS.includes('contract-conflict'))
})
```

- [ ] **Step 2: 跑，确认失败** → `node --test`，FAIL

- [ ] **Step 3: 实现（追加进 `hooks/lib/state.mjs`）**

```js
// 规格 §4.3 驳回路由。这张表此前只活在规格里，没有任何代码消费它。
//
// contract-conflict 返回 null 是**有意的**：规格 §4.3 的原话是「唯一升级情形：驳回暴露
// 00-contract.md 自身存在内在矛盾——非执行错误，只有用户能裁」。那一类不由代码决定回哪，
// 走 §5.1 的升级路径（ESCALATION_KINDS 已含同名的一类）。
export const REJECTION_KINDS = ['requirement', 'design', 'implementation', 'contract-conflict']

const REJECT_TARGET = {
  requirement: 'S2',
  design: 'S3',
  implementation: 'S5',
  'contract-conflict': null,
}

export function rejectTo(kind) {
  return typeof kind === 'string' && Object.hasOwn(REJECT_TARGET, kind) ? REJECT_TARGET[kind] : null
}
```

- [ ] **Step 4: 补一条「回退后的 state 仍然合法」的测试**

这条防的是将来有人顺手给 `validateState` 加一条「stage 只能前进」的校验，把回退路径静默掐断：

```js
test('回退后的 state 仍然合法：stage 指向更早的阶段、history 追加一条、rework 跟着涨', () => {
  const state = {
    run_id: '20260919-0421-x', stage: 'S5', contract_sha: SHA,
    roster: ['at-backend'], artifacts: {}, never_invoked: [], escalations: [],
    rework: { S5: 1 },
    history: [{ stage: 'S5', at: T }, { stage: 'S6', at: T }, { stage: 'S5', at: T }],
  }
  assert.deepEqual(validateState(state, { stages: STAGES }).problems, [])
})
```

**注意 `rework.S5` 必须是 1**：`history` 里 S5 出现两次 → 派生值 1。写 0 或 2 都该被 `validateState` 报出来，这条测的是「合法的回退不被误报」。

- [ ] **Step 5: 补规格↔代码的闭包测试**

```js
test('规格 §4.3 表里的每一行都能在 rejectTo 里命中', () => {
  const spec = readFileSync(new URL('../docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md', import.meta.url), 'utf8')
  const rows = [...spec.matchAll(/^\| (需求理解错|设计错|实现错) \| .*? \| (S\d) → S8 \|$/gm)]
  const byLabel = { 需求理解错: 'requirement', 设计错: 'design', 实现错: 'implementation' }
  for (const m of rows) {
    assert.equal(rejectTo(byLabel[m[1]]), m[2], `规格 §4.3 的「${m[1]}」写的是回到 ${m[2]}，rejectTo 答的是 ${rejectTo(byLabel[m[1]])}`)
  }
})

test('前置条件：上一条真的从规格里解析出了 3 行——否则它是空转', () => {
  const spec = readFileSync(new URL('../docs/superpowers/specs/2026-09-15-agent-team-plugin-design.md', import.meta.url), 'utf8')
  const rows = [...spec.matchAll(/^\| (需求理解错|设计错|实现错) \| .*? \| (S\d) → S8 \|$/gm)]
  assert.equal(rows.length, 3)
})
```

**这两条必须成对**——正则一旦因为规格改排版而匹配不到，上一条会因为循环零次而全绿。

⚠️ 写这两条时**用 Write 工具，不要走 heredoc**：正则里的 `\|` 与 `\d` 经 heredoc 会被吞掉反斜杠，测试会假绿（M1c 终审复评踩过这个坑）。

- [ ] **Step 6: 跑测试** → `pass 455 / fail 0`（446 + 9）

- [ ] **Step 7: 变异验证**

1. `rejectTo` 恒返回 `null` → Step 1 前三条 + Step 5 第一条必须红
2. `REJECT_TARGET` 的 `implementation` 改成 `'S3'` → Step 1 第三条 + Step 5 第一条必须红（**闭包测试与单测同时红，说明规格与代码真的被钉在一起了**）
3. 把 Step 5 的正则改成匹配不到任何行 → 前置条件那条必须红

- [ ] **Step 8: 提交**

```bash
git add hooks/lib/state.mjs tests/state.test.mjs
git commit -F - <<'MSG'
feat: 驳回路由 rejectTo —— 规格 §4.3 那张表此前没有任何代码消费它

三类执行错误分别回到 S2/S3/S5；contract-conflict 返回 null 是有意的——规格原话是
「唯一升级情形：驳回暴露 00-contract.md 自身存在内在矛盾，非执行错误，只有用户能裁」，
那一类不由代码决定回哪，走 §5.1 的升级路径。

补了两条成对的闭包测试把规格表与代码钉在一起，第二条是第一条的正向锚：正则一旦因为
规格改排版而匹配不到，第一条会因为循环零次而全绿。

另补一条「回退后的 state 仍然合法」——防的是将来有人顺手给 validateState 加一条
「stage 只能前进」的校验，把回退路径静默掐断。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 8: `loadRoster()` 留痕与三处文档漂移点

收尾 `docs/11` §1.4 与 §1.5。**这四条都很小，合成一个任务**——它们各自不值得一轮评审，但合起来是一个连贯的「把 M1b 留的尾巴扫干净」。

**Files:**
- Modify: `hooks/gate.mjs`（`loadRoster`）
- Modify: `docs/09-M1b-入口决策.md` 或主规格 §6.2.1（§1.5 第 1 点，二选一，见下）
- Modify: `stages.README.md`（§1.5 第 2 点——Task 4 Step 8 可能已经做掉，确认即可）
- Test: `tests/writepath.test.mjs` 或 `tests/gate-writepath.test.mjs`（§1.5 第 3 点）

- [ ] **Step 1: `loadRoster()` 加 try/catch（`docs/11` §1.4）**

现状：`isCoordinatorFor` 让 H5a 这条路径**第一次读 `roster.json`**，而 `loadRoster()` 不带 try/catch。`deliverable` 是 `failClosed: false`，于是 `roster.json` 读坏时会被 `main()` 外层 catch 吞成 **exit 0、零 stdout、零 stderr**——**这正是这个项目一路被咬的「静默放行」形状**（`docs/08` §0）。

```js
function loadRoster() {
  try {
    return JSON.parse(readFileSync(new URL('../roster.json', import.meta.url), 'utf8'))
  } catch (e) {
    // docs/11 §1.4：不要静默吞掉。这是插件自带文件，坏掉概率低，但静默放行正是本项目
    // 一路被咬的形状（docs/08 §0）。返回空花名册 + 往 stderr 留一行痕。
    process.stderr.write(`agent-team：roster.json 读不出来（${e?.message ?? e}），本次按空花名册处理——派发白名单与协调者判定都会失效，请检查插件安装。\n`)
    return {}
  }
}
```

补一条测试：把 `roster.json` 临时换成坏 JSON（`cp` 备份！），跑 `deliverable`，断言 **stderr 非空**。

- [ ] **Step 2: §1.5 第 1 点——`docs/09` 账二 ③ 与实现的分叉**

`docs/09` 账二 ③ 说触达表「落盘进 run」，而实现与规格 §6.4/§6.2.1 把它放在 `.agent-team/reach.json`（run 之外的控制文件层）。

**裁定：改规格 §6.2.1 那半句，不改 `docs/09`。** `docs/09` 是带日期的决策记录，体例是只追加不改原文（M1c 已经为它加过一次后记）。在规格 §6.2.1 补一句点破这条分叉，并指向 `docs/09` 的后记。

- [ ] **Step 3: §1.5 第 2 点——`stages.README.md` 抄了一遍控制文件清单**

Task 4 Step 8 已经做了。这里只需确认：`grep -c "current-run" stages.README.md` 应为 0（清单不再被抄），且文件里有指向 `hooks/lib/control-files.mjs` 的话。

- [ ] **Step 4: §1.5 第 3 点——H3 补「兄弟目录同前缀粘连」测试**

`/proj/.agent-team-backup` 对 `/proj/.agent-team` 必须为 false。该场景现在只被 `tests/path-norm.test.mjs` 顶住，H3 层面没有专属测试。

```js
test('H3：run 目录的兄弟目录同前缀粘连不算在 run 目录内（.agent-team-backup vs .agent-team）', () => {
  // 用 gate-runner 真 spawn 一次 writepath 检查，file_path 指向
  // <project>/.agent-team-backup/runs/r1/01-prd.md，断言它不被当成 run 目录下的产物
})
```

- [ ] **Step 5: `docs/11` §1.2/§1.3/§1.4/§1.5 标记结清**

同 Task 6 Step 6 的办法：**§1–§4 原文一个字不改**，在 §5 区追加一条 `### 5.9`，逐条写明哪一条由哪个 Task 结清、证据在哪。**§1.1 已在 Task 6 记过，这里不重复。**

- [ ] **Step 6: 跑测试** → `pass 457 / fail 0`（455 + 2）

- [ ] **Step 7: 提交**

```bash
git add hooks/gate.mjs stages.README.md docs/ tests/
git commit -F - <<'MSG'
fix: loadRoster 留痕与三处文档漂移点 —— 把 M1b 留的尾巴扫干净

docs/11 §1.4：isCoordinatorFor 让 H5a 第一次读 roster.json，而 loadRoster 不带
try/catch、deliverable 又是 failClosed:false——读坏时会被外层 catch 吞成 exit 0、
零 stdout、零 stderr。这正是本项目一路被咬的静默放行形状（docs/08 §0）。加 try/catch
+ stderr 留一行痕。

§1.5 三点：触达表落点的分叉改规格不改 docs/09（后者是带日期的决策记录，只追加）；
stages.README.md 不再抄控制文件清单，改为指向 control-files.mjs 单一真源；
H3 补上「兄弟目录同前缀粘连」的专属测试（此前只被 path-norm 顶住）。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 9: 真实环境验证 → `docs/14-M2a-实测结论.md`

**Files:**
- Create: `docs/14-M2a-实测结论.md`

**环境（已确认，不用再查）**：CLI `v2.1.276`，`claude auth status` 为 `loggedIn: true`。

**三条操作纪律**（`docs/13` §11 与 M1c 期间踩出来的）：

1. **绝对不要用 `claude plugin enable`**——它会接管正在运行的会话，且 `disable` 不恢复工具面（`docs/04` §9 ③）。一律用 `--plugin-dir`。
2. **可以用 `claude --bg` 起常驻后台会话**（不是 `-p`，进程不随一轮对话结束而退出），`claude logs <id>` 读输出。`docs/13` §10.1 已实测：常驻会话里异步派发正常完成，卡死是 `-p` 专属。
3. **`claude --resume` 不继承 `--plugin-dir`**（`docs/11` §5.5）——续会话必须每次都带 `--plugin-dir`，否则插件连同工具面限制一起失效。

**探针工程**：临时目录里的最小骨架，`package.json` / `tsconfig.json` / `src/server/` / `src/web/` / `docs/` 都要有（少了目录 `at-init` 可能只分出一个执行角色）。

- [ ] **Step 1: 跑一趟到 S6**

M2a 的验收标准是**机制成立**，不是十角色真跑通——`at-acceptance` 还不在任何人的派发白名单里（Task 2 Step 5 说明了为什么），S7 跑不起来是预期的。目标是走到 S6 并观察：

| # | 要验的 | 判据 |
|---|---|---|
| 1 | S5 多产者槽位 | 两个执行角色**各自**写出 `05-impl/<自己>.md`，**没有 PM 代笔** |
| 2 | `artifacts` 记账 | 五个以上键，逐个用 `sha256OfContract` 独立复算，全 MATCH |
| 3 | 阶段推进到 S6 | `isStageDone(S5)` 在两个角色都交了之后才为真 |
| 4 | H6 真的拦得住 | 手工构造一次「把 `rework` 改小」的写入，**必须被 deny** |
| 5 | H5a 新判据 | 构造「stage 停在 S5 但 S5 已 done」，协调者返回时**必须报** |
| 6 | `never_invoked` | 收尾时列出没被叫到的角色，且 `at-qa`/`at-acceptance` 在 `available_roles` 里 |

- [ ] **Step 2: 第 4、5 条用夹具构造，不要等真实 run 自然触发**

真实 run 不会主动去改小返工计数，也不容易自然停在旧阶段。**用 `claude --bg` 起一个会话，直接让它按给定内容写 `state.json`**，观察 deny。这两条是 M2a 的核心机制，**不能只有单测证据**。

- [ ] **Step 3: 写 `docs/14-M2a-实测结论.md`**

体例照 `docs/13`：先摆事实、推论单独成段、**证据等级要标明**（记录级 / 模型自述）。必须包含：

1. 六条验证项逐条的**实际命令与实际输出**，不要只写「通过」
2. 没能验证的项**说清是哪一种**：环境不允许 / 需要人来操作 / 结构上不可能 / 我没做
3. CLI 版本号
4. `docs/11` §5.7 那条新洞（推进判据依赖 `roster`）在真实 run 里有没有观察到征兆
5. 环境恢复情况：探针目录、后台会话、**以及 run 起来的任何服务进程**（M1c 那趟留了一个监听 3000 端口的 node 服务，收尾时要 `netstat` 查一遍再 `taskkill`）

- [ ] **Step 4: 跑测试** → `pass 457 / fail 0`（本任务不碰代码）

- [ ] **Step 5: 提交**

```bash
git add docs/14-M2a-实测结论.md
git commit -F - <<'MSG'
docs: M2a 真实环境实测结论

六条验证项逐条给实际命令与实际输出，证据等级逐条标明。H6 与 H5a 新判据用夹具构造触发
（真实 run 不会主动去改小返工计数，也不容易自然停在旧阶段），不只有单测证据。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## 自审（写完计划后自己跑的检查，不派子代理）

**1. 规格覆盖**：设计文档 §2 的机制改动清单九项 + §4 的回填五项，逐项对到 Task：

| 设计 §2 | Task |
|---|---|
| 1 `stages.json` 加 S6–S8、S5 producers | Task 4 Step 1 |
| 2 `stages.mjs` 的 `stageRoles`/`expandProduces` | Task 3 Step 3 |
| 2b `isStageDone` 加 roster | Task 3 Step 4 |
| 2c/2d 调用点与可选参数 | Task 3 Step 4 + Task 6 Step 3 |
| 3 `rework-guard.mjs` + gate 分支 + hooks.json | Task 5 |
| 4 `deliverable` 静默判据 | Task 6 |
| 5 `REJECTION_KINDS`/`rejectTo` | Task 7 |
| 6 五个占位 agent + roster.json | Task 2 |
| 7 `available_roles` | Task 2 Step 6–7 |
| 8 `loadRoster` try/catch | Task 8 Step 1 |
| 9 三处文档漂移点 | Task 8 Step 2–4 |
| 设计 §4 回填五项 | Task 1 |

无遗漏。

**2. 占位符扫描**：无 TBD / TODO / 「类似 Task N」/ 无代码的代码步骤。

**3. 类型一致性**：
- `stageRoles(stage)` / `expandProduces(stage, roles)` / `expectedArtifacts(stages, roster)` 三处签名在 Task 3 定义、Task 4 与 Task 6 使用，名字与参数一致
- `decideRework({before, after})` 在 Task 5 定义与使用一致
- `rejectTo(kind)` 在 Task 7 定义与使用一致
- `compareArtifacts` 加的是 `roster`，与 `expectedArtifacts` 的第二参数同名同义

**4. 测试数递推**：407 →（T2 +2）409 →（T3 +14）423 →（T4 +6）429 →（T5 +13）442 →（T6 +4）446 →（T7 +9）455 →（T8 +2）457。每个 Task 的 Expected 与这条链一致。

> **递推数只是预期值，不是验收判据。** 实现者按 §3.3 拆测试时条数可能与这里不同——**以「不得有任何一条从绿变红」为准**，数字对不上时在报告里说明多/少了哪几条、为什么，不要为了凑数删测试或硬拆。
