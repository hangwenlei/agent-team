# M1a 门禁层 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把规格 §6 的五个 hook 全部建成并各自单测通过，`gate.mjs` 按检查项参数化，用 M0 的占位角色即可端到端验证——角色真正文不在本计划范围内。

**Architecture:** `gate.mjs` 保持唯一入口，但内部改为按 CHECK 查表分派：每个检查项声明自己的 hook 事件与输入契约，入口只做该检查项声明的前置校验。决策逻辑全部拆成 `hooks/lib/` 下的纯函数，各自可脱离 Claude Code 单测。阶段链的「谁产出什么、谁需要什么」集中在新的 `stages.json`，它同时喂 H2（就绪）与 H5（交付物），避免同一份关系写两遍。

**Tech Stack:** Node 24.14.0（仅内置模块，`node:test` + `node:assert`，零 npm 依赖）；JSON 配置；Claude Code 2.1.270。

## 本计划在 M1 中的位置

M1 分三份计划：**A 门禁层（本文件）** → B 流程层（状态机、契约冻结、四条命令） → C 角色层（五个真角色正文与 S1–S5 闭环）。§11 定义的 M1 交付物不变，拆分只在计划文档边界上，目的是让机制各自验完再写角色正文。

本计划**不做**：状态机与 `/at-resume`、契约冻结与哈希校验的写入侧、四条 slash command、`project.json` 的生成（`/at-init`）、任何角色真正文。这些属于 B 与 C。本计划只做**读取侧**——门禁需要读 `stages.json`、`.agent-team/current-run`、`.agent-team/runs/<id>/state.json`、`.agent-team/project.json`，测试用夹具提供这些文件。

## Global Constraints

- 运行环境：Claude Code **2.1.270**，Claude 桌面客户端 Code tab，Windows 11。引擎路径 `%APPDATA%\Claude\claude-code\2.1.270\claude.exe`。
- **零运行时依赖**：只用 Node 内置模块。不用 bash、不用 jq、不用任何 npm 包，**不得创建 `package.json`**。
- **跑测试一律裸 `node --test`**（仓库根，不带任何参数）。本机实测 `node --test tests/` 不发现测试文件，只报 `pass 0 / fail 1` 的幻影失败，且实现前后失败相同。
- **启停插件必须用全限定名** `agent-team@skills-dir`。
- **四个入口四套命名规则**（M0 实测）：`settings.json` 的 `agent` 键吃**裸名**；`tools: Agent(...)` 白名单要**全限定名**；派发的 `subagent_type` 要**全限定名**；hook 输入的 `agent_type` 原始值是**全限定名**，由 `stripPluginPrefix` 归一化成裸名后才对花名册。
- **角色工具面禁止包含 `Skill` / `SendMessage` / `ListAgents`**（规格 §6.1），由 `tests/tool-surface.test.mjs` 强制。
- hook 失败策略（规格 §6 表格）：H1/H3/H4 **fail closed**；H2 **fail open + warning**；H5a 记 warning 不 block；H5b fail open。
- 每个 commit 末尾加：`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
- 工作目录一律 `C:\Users\82370\Desktop\Agent-Team`。**先建分支 `m1a-gates`，不要直接在 master 上提交。**

## 文件结构

| 文件 | 职责 | 本计划 |
|---|---|---|
| `stages.json` | **阶段链唯一真源**：每阶段的执行角色、需要的前置产物、必须产出的文件 | 新建 |
| `hooks/lib/checks.mjs` | 检查项注册表：名字 → { event, toolNames } | 改写 |
| `hooks/gate.mjs` | 唯一入口：按 CHECK 查表做前置校验并分派 | 改写 |
| `hooks/lib/decide.mjs` | H1 派发白名单决策（M0 已有） | 不动 |
| `hooks/lib/runctx.mjs` | 读当前 run 的上下文（run-id、state、project、stages），纯读取 | 新建 |
| `hooks/lib/readiness.mjs` | H2 就绪门禁决策（纯函数） | 新建 |
| `hooks/lib/writepath.mjs` | H3 写路径隔离决策（纯函数） | 新建 |
| `hooks/lib/contract-guard.mjs` | H4 契约保护决策（纯函数） | 新建 |
| `hooks/lib/deliverable.mjs` | H5a/H5b 交付物校验决策（纯函数） | 新建 |
| `hooks/hooks.json` | 注册 H1–H5b 共六条 | 改写 |
| `tests/gate-dispatch.test.mjs` | 入口按 CHECK 分派与前置校验 | 新建 |
| `tests/runctx.test.mjs` | 上下文读取与退化输入 | 新建 |
| `tests/readiness.test.mjs` | H2 决策 | 新建 |
| `tests/writepath.test.mjs` | H3 决策 | 新建 |
| `tests/contract-guard.test.mjs` | H4 决策 | 新建 |
| `tests/deliverable.test.mjs` | H5 决策 | 新建 |
| `tests/fixtures/` | 各测试共用的 `.agent-team` 夹具构造器 | 新建 |

**纯函数与 I/O 的分界**（沿用 M0 的做法，它让门禁能脱离 Claude Code 打穿）：`readiness` / `writepath` / `contract-guard` / `deliverable` 只接收已经读好的数据并返回决策，不碰文件系统；`runctx` 负责把文件读成数据；`gate.mjs` 负责 stdin/stdout 与分派。

---

### Task 1: `gate.mjs` 按检查项参数化

拆掉规格 §6 注记里的三颗雷。**第三颗最危险**：`SubagentStop` 是生命周期事件、输入里没有 `tool_name`，而现在的入口对所有检查项都要求 `tool_name` 是字符串、读不到就 deny。直接接上去的后果是每个角色每次收尾被无故顶回去约九次（U5 实测平台重试上限约 9）。

**Files:**
- Modify: `hooks/lib/checks.mjs`
- Modify: `hooks/gate.mjs`
- Modify: `hooks/hooks.json`
- Create: `tests/gate-dispatch.test.mjs`
- Modify: `tests/hooks-registration.test.mjs`

**Interfaces:**
- Consumes: `decideDelegation(input, roster)` 与 `stripPluginPrefix(name)`，来自 `hooks/lib/decide.mjs`（M0 已有，不改）
- Produces:
  - `CHECKS`：`{ [checkName]: { event: string, toolNames: string[] | null } }`，`toolNames` 为 `null` 表示该事件不带 `tool_name`
  - `KNOWN_CHECKS`：`Set<string>`，等于 `Object.keys(CHECKS)`，供 `tests/hooks-registration.test.mjs` 与 `gate.mjs` 共用
  - `gate.mjs` 的 **`denyAndExit(reason, event)`：按事件分派拒绝的输出契约，永不返回**
    （见下方「评审后修正」——原文写的是 `emitDeny(reason, event)`，只换事件名不换输出形状，
    那是个会让 H5b 变成空操作的雷）
  - `CHECKS[name].failClosed`：布尔，声明「读不到可判定的输入时是否拒绝」。规格 §6 的
    失败策略属于检查项自身的契约，和 `event`/`toolNames` 一样归 `checks.mjs` 这个唯一真源，
    不散落成 `gate.mjs` 里重复三遍的字符串比较

## 评审后修正（Task 1 评审结论，后续任务按这一节为准）

**每个 hook 事件的「拒绝」是不同的平台契约，不是同一个 JSON 换个事件名：**

| 事件 | 拒绝的表达方式 | 依据 |
|---|---|---|
| `PreToolUse` | exit 0 + stdout 上 `hookSpecificOutput.permissionDecision = 'deny'` | M0 实测，docs/04 |
| `SubagentStop` | **exit 2 + stderr 上的理由** | U5 实测，docs/07 §1：九次 exit 2 全部把 subagent 顶回去 |
| `PostToolUse` | 本插件在这个事件上**不拒绝**（H5a 只用 `additionalContext` 记 warning） | 规格 §6 表格 |

所以入口的拒绝出口是 `denyAndExit(reason, event)`：`PreToolUse` 走 JSON + exit 0，
`SubagentStop` 走 stderr + exit 2，两者都**不返回**（调用点后面不要再写 `process.exit(0)`）。
落到其它事件上时往 stderr 打一行以 `agent-team BUG:` 开头的话再 exit 0——这条分支在
当前注册下不可达，它存在的意义是万一将来可达时是响的，而不是静默的。

**为什么这条值得单列**：在 `SubagentStop` 上发 `permissionDecision` 形状，结果是
exit 0 + 平台不认的 JSON blob，H5b 变成一个看起来健康的空操作，角色声明了产出却没写
文件也能干净收尾。这与 M0 那次 junction 守卫失效是同一种形状——48 个测试全绿而门禁
静默失效。

- [ ] **Step 1: 写失败的测试**

`tests/gate-dispatch.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { execFileSync } from 'node:child_process'
import { fileURLToPath } from 'node:url'

const GATE = fileURLToPath(new URL('../hooks/gate.mjs', import.meta.url))

function run(check, input) {
  try {
    const stdout = execFileSync(process.execPath, [GATE, check], {
      input: typeof input === 'string' ? input : JSON.stringify(input),
      encoding: 'utf8',
      stdio: ['pipe', 'pipe', 'pipe'],
    })
    return stdout
  } catch (err) {
    return err.stdout ?? ''
  }
}

function decisionOf(stdout) {
  if (!stdout.trim()) return null
  return JSON.parse(stdout).hookSpecificOutput
}

test('未知检查项一律 deny', () => {
  const d = decisionOf(run('delegatoin', { tool_name: 'Agent', tool_input: {} }))
  assert.equal(d.permissionDecision, 'deny')
})

test('delegation：tool_name 非字符串时 deny（fail closed）', () => {
  const d = decisionOf(run('delegation', { tool_input: { subagent_type: 'at-worker-a' } }))
  assert.equal(d.permissionDecision, 'deny')
})

test('delegation：tool_name 是别的工具时静默不表态', () => {
  const out = run('delegation', { tool_name: 'Bash', tool_input: {} })
  assert.equal(out.trim(), '')
})

test('delegation 的拒绝带 PreToolUse 事件名', () => {
  const d = decisionOf(run('delegation', { tool_name: 'Agent', tool_input: {} }))
  assert.equal(d.hookEventName, 'PreToolUse')
})

// 这是本任务存在的理由：SubagentStop 不带 tool_name，
// 若沿用 PreToolUse 那套「非字符串就 deny」的前置校验，
// 每个角色每次收尾都会被无故顶回去约九次（U5 实测平台重试上限）。
test('stop-gate：输入里没有 tool_name 也不能因此 deny', () => {
  const out = run('stop-gate', { hook_event_name: 'SubagentStop', agent_type: 'agent-team:at-worker-a' })
  const d = decisionOf(out)
  if (d) assert.notEqual(d.permissionDecision, 'deny')
})

test('stop-gate 若拒绝，事件名必须是 SubagentStop 而不是 PreToolUse', () => {
  // 本任务只做分派，stop-gate 的判定逻辑在 Task 6；
  // 这里只断言「若产生决策，事件名正确」，不断言它一定拒绝。
  const out = run('stop-gate', { hook_event_name: 'SubagentStop', agent_type: 'agent-team:at-worker-a' })
  const d = decisionOf(out)
  if (d) assert.equal(d.hookEventName, 'SubagentStop')
})

test('writepath：Edit 与 Write 都进入判定，别的工具静默', () => {
  const edit = run('writepath', { tool_name: 'Edit', tool_input: { file_path: 'x' } })
  const write = run('writepath', { tool_name: 'Write', tool_input: { file_path: 'x' } })
  const bash = run('writepath', { tool_name: 'Bash', tool_input: {} })
  assert.equal(bash.trim(), '', 'Bash 不该进入写路径判定')
  // Task 4 之前 writepath 尚无判定逻辑，此处只断言它没有被 tool_name 前置校验吃掉：
  // 即 Edit/Write 的行为与 Bash 不同（要么有决策，要么至少不是同一条静默路径）。
  assert.ok(typeof edit === 'string' && typeof write === 'string')
})

test('读不到 stdin 时按安全边界 deny', () => {
  const d = decisionOf(run('delegation', ''))
  assert.equal(d.permissionDecision, 'deny')
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
node --test
```

Expected: FAIL。至少「未知检查项一律 deny」与两条 `stop-gate` 用例失败——当前 `gate.mjs` 只认识 `delegation`，且对所有检查项要求 `tool_name`。把失败原文贴进报告。

- [ ] **Step 3: 改写检查项注册表**

`hooks/lib/checks.mjs` 整体替换为：

```js
// hooks.json 注册的检查名、gate.mjs 认得的检查名、以及每个检查项的输入契约，
// 三者的唯一真源。单独成文件是为了让测试能 import 它而不必 import gate.mjs——
// 后者是可执行入口，一旦被 import 就需要「我是不是被直接执行」的守卫，
// 而那种守卫在 symlink/junction 挂载下会判错，使门禁静默失效（M0 实测）。
//
// toolNames 为 null 表示该 hook 事件不是工具调用事件，输入里没有 tool_name——
// SubagentStop 就是这种。不区分的话，PreToolUse 那套「非字符串就 deny」的
// 前置校验会让每次 subagent 收尾都被误拒（规格 §6 注记第 3 条）。
export const CHECKS = {
  delegation: { event: 'PreToolUse', toolNames: ['Agent'] },
  readiness: { event: 'PreToolUse', toolNames: ['Agent'] },
  writepath: { event: 'PreToolUse', toolNames: ['Edit', 'Write', 'NotebookEdit'] },
  contract: { event: 'PreToolUse', toolNames: ['Edit', 'Write', 'NotebookEdit'] },
  deliverable: { event: 'PostToolUse', toolNames: ['Agent'] },
  'stop-gate': { event: 'SubagentStop', toolNames: null },
}

export const KNOWN_CHECKS = new Set(Object.keys(CHECKS))
```

- [ ] **Step 4: 改写入口**

`hooks/gate.mjs` 整体替换为：

```js
#!/usr/bin/env node
// hook 单入口。第一个参数选择检查项。
// 约定：exit 0 + stdout 上的 JSON 决策 = 生效；无输出 = 走正常权限流程。
//
// 入口只做「该检查项声明的前置校验」，不做统一校验——H1–H5 分布在三种
// hook 事件上，输入形状不同（规格 §6 注记）。

import { readFileSync } from 'node:fs'
import { fileURLToPath } from 'node:url'
import { dirname, join } from 'node:path'
import { CHECKS, KNOWN_CHECKS } from './lib/checks.mjs'
import { decideDelegation } from './lib/decide.mjs'

const CHECK = process.argv[2]
const HERE = dirname(fileURLToPath(import.meta.url))
const ROOT = join(HERE, '..')

function readStdin() {
  try {
    return JSON.parse(readFileSync(0, 'utf8'))
  } catch {
    return null
  }
}

function loadRoster() {
  return JSON.parse(readFileSync(join(ROOT, 'roster.json'), 'utf8'))
}

function emitDeny(reason, event) {
  process.stdout.write(
    JSON.stringify({
      hookSpecificOutput: {
        hookEventName: event,
        permissionDecision: 'deny',
        permissionDecisionReason: reason,
      },
    }),
  )
}

function main() {
  if (!KNOWN_CHECKS.has(CHECK)) {
    process.stderr.write(
      `agent-team: 未知的检查项 ${JSON.stringify(CHECK)}；hooks.json 与 checks.mjs 已漂移。\n`,
    )
    // 未知检查项时事件名也不可知，按最常见的 PreToolUse 报，安全边界优先于精确。
    emitDeny(
      `agent-team 门禁收到未知的检查项 ${JSON.stringify(CHECK)}，按安全边界拒绝。` +
        `hooks.json 注册的检查名与 checks.mjs 认得的不一致。`,
      'PreToolUse',
    )
    process.exit(0)
  }

  const spec = CHECKS[CHECK]
  const input = readStdin()

  if (!input || typeof input !== 'object' || Array.isArray(input)) {
    // 读不到可判定的输入。安全边界类检查 fail closed；流程辅助类不表态。
    if (CHECK === 'delegation' || CHECK === 'writepath' || CHECK === 'contract') {
      emitDeny('agent-team 门禁无法读取 hook 输入，按安全边界拒绝。', spec.event)
    }
    process.exit(0)
  }

  if (spec.toolNames !== null) {
    if (typeof input.tool_name !== 'string') {
      if (CHECK === 'delegation' || CHECK === 'writepath' || CHECK === 'contract') {
        emitDeny(
          'agent-team 门禁无法从 hook 输入中读出 tool_name（缺失或不是字符串），按安全边界拒绝。',
          spec.event,
        )
      }
      process.exit(0)
    }
    if (!spec.toolNames.includes(input.tool_name)) {
      // 这次调用确实与本检查项无关，保持沉默、不表态。
      process.exit(0)
    }
  }

  if (CHECK === 'delegation') {
    const result = decideDelegation(input, loadRoster())
    if (result.decision === 'deny') emitDeny(result.reason, spec.event)
  }

  // readiness / writepath / contract / deliverable / stop-gate 的判定
  // 分别在 Task 3–6 接入，此处先只做分派。

  process.exit(0)
}

try {
  main()
} catch (err) {
  const event = (CHECKS[CHECK] && CHECKS[CHECK].event) || 'PreToolUse'
  if (CHECK === 'delegation' || CHECK === 'writepath' || CHECK === 'contract') {
    emitDeny(`agent-team 门禁异常，按安全边界拒绝：${err.message}`, event)
  }
  process.exit(0)
}
```

- [ ] **Step 5: 注册 stop-gate**

> **评审后修正（Task 1 评审，见下方「评审后修正」小节）**：本步原文还要求同时注册
> `writepath`，理由是「以便分派可测」。该理由不成立——两个测试文件都直接
> `execFileSync` 调 `gate.mjs`，没有任何测试经由 `hooks.json` 分派。而提前注册
> `writepath` 会在判定逻辑落地之前就打开一条「读不到 stdin 就拒绝真实 Edit/Write」
> 的 fail-closed 路径。`writepath` 的注册已移到 Task 4（它接判定逻辑的那一步）。
> `stop-gate` 保留在本任务注册：它是 fail open，多 fork 一个什么都不做的进程无害，
> 且 Task 6 Step 6 的原文依赖「stop-gate 已在 Task 1 注册」这个前提。

`hooks/hooks.json` 整体替换为：

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Agent$",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs", "delegation"],
            "timeout": 30,
            "statusMessage": "校验派发白名单…"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs", "stop-gate"],
            "timeout": 30,
            "statusMessage": "校验交付物…"
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 6: 跑测试确认通过**

```bash
node --test
```

Expected: 全绿。测试总数应为 M0 的 52 加本任务新增的 8 条 = **60**。若数量不符，在报告里说明差在哪。

- [ ] **Step 7: 提交**

```bash
git add hooks/lib/checks.mjs hooks/gate.mjs hooks/hooks.json tests/gate-dispatch.test.mjs tests/hooks-registration.test.mjs
git commit -m "feat: gate.mjs 按检查项参数化，拆掉 SubagentStop 误拒的雷

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 阶段链真源与运行上下文读取

**Files:**
- Create: `stages.json`
- Create: `hooks/lib/runctx.mjs`
- Create: `tests/fixtures/make-run.mjs`
- Create: `tests/runctx.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces:
  - `stages.json` 形状：`{ "<stageId>": { "role": string, "requires": string[], "produces": string[] } }`，路径一律是相对 `.agent-team/runs/<run-id>/` 的产物文件名
  - `readRunContext(projectDir, pluginDir)` → `{ ok: true, runId, runDir, state, project, stages, artifactExists } | { ok: false, reason }`，纯读取、不抛异常
  - `makeRun(spec)`（测试夹具）→ `{ projectDir, pluginDir }`

**两个根不能混为一谈**：`stages.json` 属于**插件**（随插件分发、所有项目共用），
`current-run` / `state.json` / `project.json` 属于**用户项目**（每个仓库一份）。
hook 以用户项目为 cwd 运行，插件文件要用 `${CLAUDE_PLUGIN_ROOT}` 定位。
因此 `readRunContext` 从一开始就收两个参数，不要写成单根再改。

- [ ] **Step 1: 写阶段链真源**

`stages.json`：

```json
{
  "S1": { "role": "at-pm",        "requires": [],                              "produces": ["00-contract.md"] },
  "S2": { "role": "at-product",   "requires": ["00-contract.md"],              "produces": ["01-prd.md"] },
  "S3": { "role": "at-architect", "requires": ["00-contract.md", "01-prd.md"], "produces": ["03-arch.md"] },
  "S4": { "role": "at-pm",        "requires": ["01-prd.md", "03-arch.md"],     "produces": ["04-dispatch.md"] },
  "S5": { "role": "at-backend",   "requires": ["03-arch.md", "04-dispatch.md"], "produces": ["05-impl/at-backend.md"] }
}
```

M1a 只覆盖规格 §4 的 S1–S5，且执行角色只列到 `at-backend`——S6–S8 属于计划 C。

> **评审后修正（Task 2 评审）**：`stages.json` 是本仓库第二个引用角色名的文件，而它引用的
> `at-backend` 在 `roster.json` 与 `agents/` 里根本不存在。后果不是理论上的——H1 派发门禁
> （Task 1 已上线）会拒掉任何派向 `at-backend` 的派发，S5 走不通；而且 **Task 7 的人工验证
> 清单第 4、5 条正是派 `agent-team:at-backend` 和写 `at-frontend` 的路径**，照原样跑会在 H1
> 处就被拦下，H3 根本轮不到表态。
>
> 处理：把 M0 的两个占位角色**改名**成 `at-worker-a` → `at-backend`、`at-worker-b` →
> `at-frontend`。它们本来就只是「需要有个能被派的东西」的占位，改名后 `stages.json`、
> `roster.json`、Task 4 的 `project.json` 夹具、Task 7 的清单四者一致，计划 C 要做的是
> **填正文**而不是再改一遍名字。同时补一条 `stages.json` → `roster.json` 的闭包测试
> （仿 `tests/roster-closure.test.mjs`）——`roster.json` 早有这张网，`stages.json` 引入时
> 没进网，所以第一天就违反了同类不变量而无人报警。
>
> `docs/04`–`docs/07` **不动**：那是当时实验的原文记录，角色名就叫 `at-worker-a`，
> 改它等于篡改实验记录。
>
> 本节以下各任务正文里出现的 `at-worker-a` / `at-worker-b` 多数在**单测的本地夹具对象**里
> （`decideReadiness` / `decideWritePath` 这些纯函数不读真花名册），那些不必改；
> 要改的是真的会被平台解析的地方：`roster.json`、`agents/`、`agents/at-pm.md` 的
> `tools:` 行、以及任何把它当 `agent_type` 喂给 `gate.mjs` 的测试。

- [ ] **Step 2: 写失败的测试**

`tests/fixtures/make-run.mjs`：

```js
import { mkdtempSync, mkdirSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join, dirname } from 'node:path'

// 造一个临时的 .agent-team 结构供门禁测试读取。
// 只写测试需要的字段，不模拟完整产品结构。
export function makeRun({ runId = 'r1', stage = 'S2', artifacts = [], project = null, stages = null } = {}) {
  // 两个根分开造：projectDir 模拟用户仓库，pluginDir 模拟插件安装目录。
  const projectDir = mkdtempSync(join(tmpdir(), 'agent-team-proj-'))
  const pluginDir = mkdtempSync(join(tmpdir(), 'agent-team-plug-'))

  const runDir = join(projectDir, '.agent-team', 'runs', runId)
  mkdirSync(runDir, { recursive: true })
  writeFileSync(join(projectDir, '.agent-team', 'current-run'), runId, 'utf8')
  writeFileSync(
    join(runDir, 'state.json'),
    JSON.stringify({ run_id: runId, stage, rework: {}, never_invoked: [] }),
    'utf8',
  )
  for (const rel of artifacts) {
    const p = join(runDir, rel)
    mkdirSync(dirname(p), { recursive: true })
    writeFileSync(p, `fixture ${rel}\n`, 'utf8')
  }
  if (project) {
    writeFileSync(join(projectDir, '.agent-team', 'project.json'), JSON.stringify(project), 'utf8')
  }
  if (stages) {
    writeFileSync(join(pluginDir, 'stages.json'), JSON.stringify(stages), 'utf8')
  }
  return { projectDir, pluginDir }
}
```

`tests/runctx.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { rmSync, writeFileSync } from 'node:fs'
import { readRunContext } from '../hooks/lib/runctx.mjs'
import { makeRun } from './fixtures/make-run.mjs'

const STAGES = { S2: { role: 'at-product', requires: ['00-contract.md'], produces: ['01-prd.md'] } }

function cleanup({ projectDir, pluginDir }) {
  rmSync(projectDir, { recursive: true, force: true })
  rmSync(pluginDir, { recursive: true, force: true })
}

test('读得到当前 run 的 id、state 与 stages', () => {
  const dirs = makeRun({ runId: 'r42', stage: 'S2', stages: STAGES })
  try {
    const ctx = readRunContext(dirs.projectDir, dirs.pluginDir)
    assert.equal(ctx.ok, true)
    assert.equal(ctx.runId, 'r42')
    assert.equal(ctx.state.stage, 'S2')
    assert.deepEqual(ctx.stages.S2.produces, ['01-prd.md'])
  } finally {
    cleanup(dirs)
  }
})

test('没有 .agent-team 时返回 ok:false 而不是抛异常', () => {
  const ctx = readRunContext('/definitely/not/a/real/path', '/also/not/real')
  assert.equal(ctx.ok, false)
  assert.match(ctx.reason, /current-run|\.agent-team/)
})

test('current-run 指向不存在的 run 时返回 ok:false', () => {
  const dirs = makeRun({ runId: 'r1', stages: STAGES })
  try {
    rmSync(`${dirs.projectDir}/.agent-team/runs/r1`, { recursive: true, force: true })
    const ctx = readRunContext(dirs.projectDir, dirs.pluginDir)
    assert.equal(ctx.ok, false)
  } finally {
    cleanup(dirs)
  }
})

test('state.json 是坏 JSON 时返回 ok:false 而不是抛异常', () => {
  const dirs = makeRun({ runId: 'r1', stages: STAGES })
  try {
    writeFileSync(`${dirs.projectDir}/.agent-team/runs/r1/state.json`, '{ not json', 'utf8')
    const ctx = readRunContext(dirs.projectDir, dirs.pluginDir)
    assert.equal(ctx.ok, false)
  } finally {
    cleanup(dirs)
  }
})

test('stages.json 从插件根读，不从项目根读', () => {
  const dirs = makeRun({ runId: 'r1', stages: STAGES })
  try {
    // 项目根里放一份内容不同的 stages.json——不应当被读到。
    writeFileSync(`${dirs.projectDir}/stages.json`, JSON.stringify({ SX: { role: 'wrong' } }), 'utf8')
    const ctx = readRunContext(dirs.projectDir, dirs.pluginDir)
    assert.equal(ctx.ok, true)
    assert.ok(ctx.stages.S2, '读到的应当是插件根那份')
    assert.equal(ctx.stages.SX, undefined, '不应当读到项目根那份')
  } finally {
    cleanup(dirs)
  }
})

test('artifactExists 只在文件真存在时为 true', () => {
  const dirs = makeRun({ runId: 'r1', artifacts: ['00-contract.md'], stages: STAGES })
  try {
    const ctx = readRunContext(dirs.projectDir, dirs.pluginDir)
    assert.equal(ctx.artifactExists('00-contract.md'), true)
    assert.equal(ctx.artifactExists('01-prd.md'), false)
  } finally {
    cleanup(dirs)
  }
})
```

- [ ] **Step 3: 跑测试确认失败**

```bash
node --test
```

Expected: FAIL，报找不到 `../hooks/lib/runctx.mjs`。

- [ ] **Step 4: 写最小实现**

`hooks/lib/runctx.mjs`：

```js
// 把门禁需要的磁盘状态读成数据。所有失败都返回 { ok: false, reason }，
// 绝不抛异常——门禁的调用方需要自己决定 fail open 还是 fail closed，
// 而不是被一个异常替它决定。
import { existsSync, readFileSync, statSync } from 'node:fs'
import { join } from 'node:path'

function readJson(path) {
  try {
    return { ok: true, value: JSON.parse(readFileSync(path, 'utf8')) }
  } catch (err) {
    return { ok: false, reason: `读取 ${path} 失败：${err.message}` }
  }
}

// projectDir：用户仓库根，放 .agent-team（current-run / state.json / project.json）。
// pluginDir：插件根，放 stages.json。两者不是同一个目录——hook 以用户项目为 cwd 运行，
// 插件文件要用 ${CLAUDE_PLUGIN_ROOT} 定位。混用会在真实环境里读不到东西而单测照样绿。
export function readRunContext(projectDir, pluginDir) {
  const base = join(projectDir, '.agent-team')
  const pointer = join(base, 'current-run')
  if (!existsSync(pointer)) {
    return { ok: false, reason: `找不到 ${pointer}——当前没有进行中的 run` }
  }

  let runId = ''
  try {
    runId = readFileSync(pointer, 'utf8').trim()
  } catch (err) {
    return { ok: false, reason: `读取 current-run 失败：${err.message}` }
  }
  if (!runId) return { ok: false, reason: 'current-run 是空的' }

  const runDir = join(base, 'runs', runId)
  if (!existsSync(runDir)) {
    return { ok: false, reason: `current-run 指向 ${runId}，但 ${runDir} 不存在` }
  }

  const state = readJson(join(runDir, 'state.json'))
  if (!state.ok) return { ok: false, reason: state.reason }

  const stages = readJson(join(pluginDir, 'stages.json'))
  if (!stages.ok) return { ok: false, reason: stages.reason }

  const projectPath = join(base, 'project.json')
  const project = existsSync(projectPath) ? readJson(projectPath) : { ok: true, value: null }
  if (!project.ok) return { ok: false, reason: project.reason }

  return {
    ok: true,
    runId,
    runDir,
    state: state.value,
    stages: stages.value,
    project: project.value,
    artifactExists(rel) {
      const p = join(runDir, rel)
      try {
        return statSync(p).isFile()
      } catch {
        return false
      }
    },
  }
}
```

- [ ] **Step 5: 跑测试确认通过**

```bash
node --test
```

Expected: 全绿，总数 60 + 6 = **66**。

- [ ] **Step 6: 提交**

```bash
git add stages.json hooks/lib/runctx.mjs tests/fixtures/make-run.mjs tests/runctx.test.mjs
git commit -m "feat: 阶段链真源 stages.json 与运行上下文读取

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: H2 就绪门禁

规格 §4.2 ②：每阶段开工前 stat 前置产物，缺失则拒并指明该先跑哪一阶段。调研结论是这条在全生态都空白，而提示词层的顺序约束在返工时最先崩。

**Files:**
- Create: `hooks/lib/readiness.mjs`
- Create: `tests/readiness.test.mjs`
- Modify: `hooks/gate.mjs`
- Modify: `hooks/hooks.json`

**Interfaces:**
- Consumes: `readRunContext(projectDir, pluginDir)`（Task 2 定稿的双参数签名）；`stripPluginPrefix(name)`（M0 `decide.mjs`）
- Produces: `decideReadiness({ targetRole, stages, artifactExists })` → `{ decision: 'allow' } | { decision: 'deny', reason }`

- [ ] **Step 1: 写失败的测试**

`tests/readiness.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { decideReadiness } from '../hooks/lib/readiness.mjs'

const STAGES = {
  S1: { role: 'at-pm', requires: [], produces: ['00-contract.md'] },
  S2: { role: 'at-product', requires: ['00-contract.md'], produces: ['01-prd.md'] },
  S3: { role: 'at-architect', requires: ['00-contract.md', '01-prd.md'], produces: ['03-arch.md'] },
}

const have = (...names) => (rel) => names.includes(rel)

test('前置产物齐全时放行', () => {
  const r = decideReadiness({ targetRole: 'at-product', stages: STAGES, artifactExists: have('00-contract.md') })
  assert.equal(r.decision, 'allow')
})

test('前置产物缺失时拒绝', () => {
  const r = decideReadiness({ targetRole: 'at-product', stages: STAGES, artifactExists: have() })
  assert.equal(r.decision, 'deny')
})

test('拒绝理由点名缺的是哪个产物', () => {
  const r = decideReadiness({ targetRole: 'at-product', stages: STAGES, artifactExists: have() })
  assert.match(r.reason, /00-contract\.md/)
})

test('拒绝理由指明该先跑哪一阶段', () => {
  const r = decideReadiness({ targetRole: 'at-architect', stages: STAGES, artifactExists: have('00-contract.md') })
  assert.equal(r.decision, 'deny')
  assert.match(r.reason, /01-prd\.md/)
  assert.match(r.reason, /S2/, '应当告诉调用者 01-prd.md 是 S2 的产物')
})

test('无前置的阶段一律放行', () => {
  const r = decideReadiness({ targetRole: 'at-pm', stages: STAGES, artifactExists: have() })
  assert.equal(r.decision, 'allow')
})

test('不在阶段链里的角色不归本门禁管，放行', () => {
  const r = decideReadiness({ targetRole: 'at-worker-a', stages: STAGES, artifactExists: have() })
  assert.equal(r.decision, 'allow')
})

test('一个角色出现在多个阶段时，取尚未完成的最早那个', () => {
  // at-pm 同时是 S1 与 S4 的执行角色。S1 的产物已在、S4 的前置未齐时，
  // 应当按 S4 判定而不是按 S1 放行。
  const stages = {
    S1: { role: 'at-pm', requires: [], produces: ['00-contract.md'] },
    S4: { role: 'at-pm', requires: ['01-prd.md'], produces: ['04-dispatch.md'] },
  }
  const r = decideReadiness({ targetRole: 'at-pm', stages, artifactExists: have('00-contract.md') })
  assert.equal(r.decision, 'deny')
  assert.match(r.reason, /01-prd\.md/)
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
node --test
```

Expected: FAIL，报找不到 `../hooks/lib/readiness.mjs`。

- [ ] **Step 3: 写最小实现**

`hooks/lib/readiness.mjs`：

```js
// H2 就绪门禁（纯函数）。不碰文件系统——artifactExists 由调用方注入，
// 这样它能脱离 Claude Code 与磁盘单测。
//
// 失败策略见规格 §6 表格：H2 是流程辅助，门禁自身无法判定时 fail open；
// 但「前置产物确实缺失」是门禁在正常工作并做出否决，那要 deny。

function producerOf(stages, artifact) {
  for (const [id, s] of Object.entries(stages)) {
    if (Array.isArray(s.produces) && s.produces.includes(artifact)) return id
  }
  return null
}

export function decideReadiness({ targetRole, stages, artifactExists }) {
  if (!stages || typeof stages !== 'object') return { decision: 'allow' }

  // 一个角色可能是多个阶段的执行者（如 at-pm 既是 S1 又是 S4）。
  // 按阶段 id 升序找第一个「产物尚未齐全」的阶段来判定——那就是它此刻要做的那一段。
  const mine = Object.entries(stages)
    .filter(([, s]) => s.role === targetRole)
    .sort(([a], [b]) => a.localeCompare(b))

  if (mine.length === 0) return { decision: 'allow' }

  for (const [stageId, stage] of mine) {
    const done = (stage.produces || []).every((p) => artifactExists(p))
    if (done) continue

    const missing = (stage.requires || []).filter((r) => !artifactExists(r))
    if (missing.length === 0) return { decision: 'allow' }

    const detail = missing
      .map((m) => {
        const from = producerOf(stages, m)
        return from ? `${m}（${from} 的产物）` : m
      })
      .join('、')

    return {
      decision: 'deny',
      reason:
        `${targetRole} 现在要做的是 ${stageId}，但它的前置产物还缺：${detail}。` +
        `先把产出这些产物的阶段跑完再回来，不要跳过。`,
    }
  }

  return { decision: 'allow' }
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
node --test
```

Expected: 全绿，总数 66 + 7 = **73**。

- [ ] **Step 5: 接进入口**

`hooks/gate.mjs` 顶部补 import：

```js
import { readRunContext } from './lib/runctx.mjs'
import { decideReadiness } from './lib/readiness.mjs'
import { stripPluginPrefix } from './lib/decide.mjs'
```

在 `main()` 里 `if (CHECK === 'delegation')` 那段之后补：

```js
  if (CHECK === 'readiness') {
    const ctx = readRunContext(ROOT_PROJECT, ROOT)
    // H2 是流程辅助：读不到运行上下文就放行并在 stderr 留痕，不卡住整趟跑。
    if (!ctx.ok) {
      process.stderr.write(`agent-team readiness: 跳过（${ctx.reason}）\n`)
      process.exit(0)
    }
    const target = stripPluginPrefix(input?.tool_input?.subagent_type)
    if (!target) process.exit(0)
    const r = decideReadiness({
      targetRole: target,
      stages: ctx.stages,
      artifactExists: ctx.artifactExists,
    })
    if (r.decision === 'deny') emitDeny(r.reason, spec.event)
  }
```

并在文件顶部 `const ROOT = join(HERE, '..')` 之后补：

```js
// 门禁读的运行状态在**用户项目**里，不在插件目录里。
// hook 以用户项目为 cwd 运行，所以用 process.cwd()；
// 插件自身的文件（roster.json、stages.json）仍用 ROOT。
const ROOT_PROJECT = process.cwd()
```

调用写成 `readRunContext(ROOT_PROJECT, ROOT)`——第一个是用户项目根（`process.cwd()`），第二个是插件根。两者不是同一个目录，Task 2 的接口已经按双参数定义。

- [ ] **Step 6: 注册 H2**

`hooks/hooks.json` 的 `PreToolUse` 数组里，在 `^Agent$` 那一项的 `hooks` 数组中追加一条（同一个 matcher 下可以有多个 hook，它们依次执行）：

```json
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs", "readiness"],
            "timeout": 30,
            "statusMessage": "校验前置产物…"
          }
```

- [ ] **Step 7: 跑测试并提交**

```bash
node --test
"/c/Users/82370/AppData/Roaming/Claude/claude-code/2.1.270/claude.exe" plugin validate .
git add -A
git commit -m "feat: H2 就绪门禁

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: H3 写路径隔离

规格 §6 表格 H3，fail closed。已知边界（规格 §6.2）：**这道闸对 `Edit`/`Write` 是硬约束，对 `Bash` 是软约束**——执行角色保留 Bash 以跑构建与测试，而 Bash 能写文件。本任务不试图解决 Bash 那一侧。

**Files:**
- Create: `hooks/lib/writepath.mjs`
- Create: `tests/writepath.test.mjs`
- Modify: `hooks/gate.mjs`
- Modify: `hooks/hooks.json`（注册 `writepath`；Task 1 评审把它从 Task 1 移到了这里）
- Modify: `agents/at-pm.md`、`agents/at-product.md`、`agents/at-architect.md`、`agents/at-backend.md`、`agents/at-frontend.md`（补 `Write`/`Edit` 工具面，理由见下方「评审后补入」）

**评审后补入（Task 2 评审的连带发现）：先把 `Write`/`Edit` 放进工具面，否则这道闸永远等不到一次可拦的调用**

当前六个角色的 `tools:` 行里**没有任何一个有 `Write` 或 `Edit`**：

```
at-pm         Agent(四个角色), Read, Glob
at-product    Agent, Read, Glob
at-architect  Agent, Read, Glob
at-backend    Read
at-frontend   Read
at-outsider   Read
```

H3 挂的 matcher 是 `^(Edit|Write|NotebookEdit)$`。没有角色能发起这三种调用，这道闸就是一道
永远不会被触发的门——**单测全绿，真实环境零覆盖**，而 Task 7 清单第 4、5、6 条全部依赖
角色能 `Write`。

更关键的是 U7 实测的连带事实（`docs/06-U7-实测结论.md` §3.3）：**主线程 agent 的
`tools:` 限定的是整棵子树的工具面**——原文错误文案是 `Skill is disabled for this
session, in subagents as well as here`。所以只给 `at-backend` 加 `Write` 没用，
`at-pm` 那一行不含 `Write`/`Edit`，整个会话就都没有。

按规格 §6.1「角色工具面是按需白名单」定工具面（依据是 `stages.json` 里各角色的 `produces`）：

| 角色 | `tools:` | 为什么 |
|---|---|---|
| `at-pm` | `Agent(四个角色), Read, Glob, Write, Edit` | 产出 `00-contract.md`(S1)、`04-dispatch.md`(S4)；同时它是主线程，必须**涵盖整棵子树用到的每一个工具**，否则子孙拿不到 |
| `at-product` | `Agent, Read, Glob, Write` | 产出 `01-prd.md` |
| `at-architect` | `Agent, Read, Glob, Write` | 产出 `03-arch.md` |
| `at-backend` / `at-frontend` | `Read, Write, Edit` | 产出 `05-impl/*.md` 与真代码；改已有文件要 `Edit` |
| `at-outsider` | `Read`（**不变**） | 它是「在花名册里但不可达」的对照角色，不参与阶段链 |

`Skill` / `SendMessage` / `ListAgents` 一律不加（规格 §6.1，`tests/tool-surface.test.mjs` 强制）。
改完跑 `tests/roster-sync.test.mjs` 确认 `at-pm` 的 `Agent(...)` 白名单对账没被括号外的新工具影响。

**Interfaces:**
- Consumes: `readRunContext(projectDir, pluginDir)`（Task 2 定稿，Task 3 只是首个调用方，没有改过它的签名）
- Produces: `decideWritePath({ role, filePath, project, runDir })` → `{ decision, reason? }`
  - `project` 形状：`{ "paths": { "<role>": string[] } }`，值是相对项目根的 glob 前缀

- [ ] **Step 1: 写失败的测试**

`tests/writepath.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { decideWritePath } from '../hooks/lib/writepath.mjs'

const PROJECT = {
  paths: {
    'at-backend': ['src/server/', 'src/shared/'],
    'at-frontend': ['src/web/', 'src/shared/'],
  },
}
const RUN = '/proj/.agent-team/runs/r1'

const call = (role, filePath) =>
  decideWritePath({ role, filePath, project: PROJECT, runDir: RUN })

test('写自己名下的路径放行', () => {
  assert.equal(call('at-backend', '/proj/src/server/api.ts').decision, 'allow')
})

test('写别人名下的路径拒绝', () => {
  const r = call('at-backend', '/proj/src/web/App.tsx')
  assert.equal(r.decision, 'deny')
})

test('拒绝理由点名这条路径归谁', () => {
  const r = call('at-backend', '/proj/src/web/App.tsx')
  assert.match(r.reason, /at-frontend/)
})

test('共享路径对两个角色都放行', () => {
  assert.equal(call('at-backend', '/proj/src/shared/types.ts').decision, 'allow')
  assert.equal(call('at-frontend', '/proj/src/shared/types.ts').decision, 'allow')
})

test('谁都没认领的路径拒绝，并说明要先在 project.json 里认领', () => {
  const r = call('at-backend', '/proj/docs/readme.md')
  assert.equal(r.decision, 'deny')
  assert.match(r.reason, /project\.json/)
})

test('本趟 run 目录下的产物一律放行——那是流程产物不是代码', () => {
  assert.equal(call('at-backend', `${RUN}/05-impl/at-backend.md`).decision, 'allow')
})

test('不在 project.paths 里的角色不归本门禁管，放行', () => {
  assert.equal(call('at-worker-a', '/proj/anything.ts').decision, 'allow')
})

test('project 为 null 时放行——尚未跑过勘察', () => {
  const r = decideWritePath({ role: 'at-backend', filePath: '/proj/x.ts', project: null, runDir: RUN })
  assert.equal(r.decision, 'allow')
})

test('路径里的 .. 不能用来逃出自己的地盘', () => {
  const r = call('at-backend', '/proj/src/server/../web/App.tsx')
  assert.equal(r.decision, 'deny')
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
node --test
```

Expected: FAIL，报找不到 `../hooks/lib/writepath.mjs`。

- [ ] **Step 3: 写最小实现**

`hooks/lib/writepath.mjs`：

```js
// H3 写路径隔离（纯函数）。
//
// ⚠️ 已知边界（规格 §6.2）：这道闸只管 Edit/Write/NotebookEdit。
// 执行角色保留 Bash 以跑构建与测试，而 Bash 能写文件（echo >、sed -i），
// 那条路不在本函数的覆盖范围内，README 已明示本插件不是沙箱。
import { resolve, sep } from 'node:path'

function norm(p) {
  // 归一化并统一分隔符，防止 .. 与大小写/斜杠差异绕过前缀比对。
  return resolve(p).split(sep).join('/')
}

function underAny(target, prefixes, base) {
  return prefixes.some((prefix) => {
    const full = norm(`${base}/${prefix}`)
    const dir = full.endsWith('/') ? full : `${full}/`
    return target === full || target.startsWith(dir)
  })
}

export function decideWritePath({ role, filePath, project, runDir }) {
  if (!project || typeof project !== 'object' || !project.paths) return { decision: 'allow' }
  if (typeof filePath !== 'string' || !filePath) return { decision: 'allow' }

  const target = norm(filePath)

  // 本趟 run 目录下的产物是流程产物不是代码，任何角色都可以写自己那份。
  if (runDir) {
    const rd = norm(runDir)
    if (target === rd || target.startsWith(`${rd}/`)) return { decision: 'allow' }
  }

  const owners = project.paths
  if (!Object.hasOwn(owners, role)) return { decision: 'allow' }

  // project.paths 的值是相对项目根的前缀；项目根由 runDir 往上推两级得到
  // （.agent-team/runs/<id> → 项目根），没有 runDir 时退回用 filePath 的根。
  const base = runDir ? norm(`${runDir}/../../..`) : '/'

  if (underAny(target, owners[role], base)) return { decision: 'allow' }

  const claimant = Object.entries(owners).find(
    ([other, prefixes]) => other !== role && underAny(target, prefixes, base),
  )

  if (claimant) {
    return {
      decision: 'deny',
      reason:
        `${role} 不得写 ${filePath}——这条路径归 ${claimant[0]}。` +
        `跨角色的改动要经上级协调，不要直接动别人的地盘。`,
    }
  }

  return {
    decision: 'deny',
    reason:
      `${role} 不得写 ${filePath}——这条路径在 .agent-team/project.json 里没有被任何角色认领。` +
      `先在 project.json 的 paths 里把它划给某个角色，再动它。`,
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
node --test
```

Expected: 全绿，总数 73 + 9 = **82**。

- [ ] **Step 5: 接进入口**

`hooks/gate.mjs` 顶部补 `import { decideWritePath } from './lib/writepath.mjs'`，并在 `readiness` 那段之后补：

```js
  if (CHECK === 'writepath') {
    const ctx = readRunContext(ROOT_PROJECT, ROOT)
    // H3 是安全边界：读不到上下文时 fail closed。
    if (!ctx.ok) {
      emitDeny(
        `agent-team 写路径门禁读不到运行上下文（${ctx.reason}），按安全边界拒绝。`,
        spec.event,
      )
      process.exit(0)
    }
    const role = stripPluginPrefix(input?.agent_type)
    // 主线程不带 agent_type，不受 per-role 隔离约束。
    if (!input?.agent_type) process.exit(0)
    const r = decideWritePath({
      role,
      filePath: input?.tool_input?.file_path,
      project: ctx.project,
      runDir: ctx.runDir,
    })
    if (r.decision === 'deny') emitDeny(r.reason, spec.event)
  }
```

接着在 `hooks/hooks.json` 的 `PreToolUse` 数组里补上 `writepath` 的注册（Task 1 评审
撤掉了它的提前注册，理由见 Task 1 Step 5 的批注——判定逻辑不在就注册，等于提前打开
一条「读不到 stdin 就拒绝真实 Edit/Write」的 fail-closed 路径）：

```json
      {
        "matcher": "^(Edit|Write|NotebookEdit)$",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs", "writepath"],
            "timeout": 30,
            "statusMessage": "校验写路径归属…"
          }
        ]
      }
```

同时把 `hooks/hooks.json` 加进本任务的 **Files** 清单（Modify）。

- [ ] **Step 6: 跑测试并提交**

```bash
node --test
"/c/Users/82370/AppData/Roaming/Claude/claude-code/2.1.270/claude.exe" plugin validate .
git add -A
git commit -m "feat: H3 写路径隔离

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: H4 契约保护

规格 §5.3：**契约唯一写者是用户（经 PM 转写）。任何 subagent 不得写 `00-contract.md`。** fail closed。

**Files:**
- Create: `hooks/lib/contract-guard.mjs`
- Create: `tests/contract-guard.test.mjs`
- Modify: `hooks/gate.mjs`
- Modify: `hooks/hooks.json`

**Interfaces:**
- Consumes: `readRunContext`
- Produces: `decideContractGuard({ agentType, filePath, runDir })` → `{ decision, reason? }`

- [ ] **Step 1: 写失败的测试**

`tests/contract-guard.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { decideContractGuard } from '../hooks/lib/contract-guard.mjs'

const RUN = '/proj/.agent-team/runs/r1'
const CONTRACT = `${RUN}/00-contract.md`

test('subagent 写契约一律拒绝', () => {
  const r = decideContractGuard({ agentType: 'agent-team:at-product', filePath: CONTRACT, runDir: RUN })
  assert.equal(r.decision, 'deny')
})

test('拒绝理由说明契约的唯一写者是用户', () => {
  const r = decideContractGuard({ agentType: 'agent-team:at-product', filePath: CONTRACT, runDir: RUN })
  assert.match(r.reason, /用户|PM/)
})

test('主线程（无 agent_type）写契约放行——那是 PM 代用户转写', () => {
  const r = decideContractGuard({ agentType: undefined, filePath: CONTRACT, runDir: RUN })
  assert.equal(r.decision, 'allow')
})

test('subagent 写别的产物放行', () => {
  const r = decideContractGuard({ agentType: 'agent-team:at-product', filePath: `${RUN}/01-prd.md`, runDir: RUN })
  assert.equal(r.decision, 'allow')
})

test('绕路径写契约也拒绝', () => {
  const r = decideContractGuard({
    agentType: 'agent-team:at-product',
    filePath: `${RUN}/05-impl/../00-contract.md`,
    runDir: RUN,
  })
  assert.equal(r.decision, 'deny')
})

test('runDir 为空时不表态——无法判定哪个是契约', () => {
  const r = decideContractGuard({ agentType: 'agent-team:at-product', filePath: CONTRACT, runDir: null })
  assert.equal(r.decision, 'allow')
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
node --test
```

Expected: FAIL，报找不到 `../hooks/lib/contract-guard.mjs`。

- [ ] **Step 3: 写最小实现**

`hooks/lib/contract-guard.mjs`：

```js
// H4 契约保护（纯函数）。规格 §5.3：契约的唯一写者是用户，经 PM 转写。
// 任何 subagent 不得写 00-contract.md——它是整趟跑的锚点，
// S7 验收要拿它的哈希对账，被谁悄悄改写就等于把「当初要什么」改掉了。
import { resolve, sep } from 'node:path'

const CONTRACT = '00-contract.md'

function norm(p) {
  return resolve(p).split(sep).join('/')
}

export function decideContractGuard({ agentType, filePath, runDir }) {
  if (!runDir) return { decision: 'allow' }
  if (typeof filePath !== 'string' || !filePath) return { decision: 'allow' }
  // 主线程没有 agent_type。PM 就是主线程，它代用户转写契约是设计允许的。
  if (!agentType) return { decision: 'allow' }

  if (norm(filePath) !== norm(`${runDir}/${CONTRACT}`)) return { decision: 'allow' }

  return {
    decision: 'deny',
    reason:
      `不得写 ${CONTRACT}。契约是这趟跑的锚点，唯一写者是用户（由项目经理在主会话里转写）。` +
      `任何 subagent 都不能改它——S7 验收要拿它的哈希对账，改了就等于把「当初要什么」改掉了。` +
      `如果你认为契约本身有问题，把问题冒泡给上级，由 PM 决定是否升级给用户。`,
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
node --test
```

Expected: 全绿，总数 82 + 6 = **88**。

- [ ] **Step 5: 接进入口并注册**

`hooks/gate.mjs` 顶部补 `import { decideContractGuard } from './lib/contract-guard.mjs'`，在 `writepath` 那段之后补：

```js
  if (CHECK === 'contract') {
    const ctx = readRunContext(ROOT_PROJECT, ROOT)
    if (!ctx.ok) {
      emitDeny(
        `agent-team 契约保护读不到运行上下文（${ctx.reason}），按安全边界拒绝。`,
        spec.event,
      )
      process.exit(0)
    }
    const r = decideContractGuard({
      agentType: input?.agent_type,
      filePath: input?.tool_input?.file_path,
      runDir: ctx.runDir,
    })
    if (r.decision === 'deny') emitDeny(r.reason, spec.event)
  }
```

`hooks/hooks.json` 里 `^(Edit|Write|NotebookEdit)$` 那一项的 `hooks` 数组追加：

```json
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs", "contract"],
            "timeout": 30,
            "statusMessage": "校验契约保护…"
          }
```

- [ ] **Step 6: 跑测试并提交**

```bash
node --test
"/c/Users/82370/AppData/Roaming/Claude/claude-code/2.1.270/claude.exe" plugin validate .
git add -A
git commit -m "feat: H4 契约保护

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: H5a/H5b 交付物校验

规格 §6 表格 H5 与其下方的说明段：**两道一起才成立**。`SubagentStop`（H5b）真拦截，给角色最多约 8 次补救机会；`PostToolUse`（H5a）记 warning 作权威记录，因为平台约 9 次后会静默放行且父级无感。

**Files:**
- Create: `hooks/lib/deliverable.mjs`
- Create: `tests/deliverable.test.mjs`
- Modify: `hooks/gate.mjs`
- Modify: `hooks/hooks.json`

**Interfaces:**
- Consumes: `readRunContext`
- Produces: `decideDeliverable({ role, stages, artifactExists })` → `{ ok: true } | { ok: false, missing: string[], stageId: string, reason: string }`

- [ ] **Step 1: 写失败的测试**

`tests/deliverable.test.mjs`：

```js
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { decideDeliverable } from '../hooks/lib/deliverable.mjs'

const STAGES = {
  S2: { role: 'at-product', requires: ['00-contract.md'], produces: ['01-prd.md'] },
  S3: { role: 'at-architect', requires: ['01-prd.md'], produces: ['03-arch.md'] },
}
const have = (...names) => (rel) => names.includes(rel)

test('产物已写时 ok', () => {
  const r = decideDeliverable({ role: 'at-product', stages: STAGES, artifactExists: have('01-prd.md') })
  assert.equal(r.ok, true)
})

test('产物未写时不 ok，并列出缺的是哪些', () => {
  const r = decideDeliverable({ role: 'at-product', stages: STAGES, artifactExists: have() })
  assert.equal(r.ok, false)
  assert.deepEqual(r.missing, ['01-prd.md'])
})

test('理由点名阶段与文件', () => {
  const r = decideDeliverable({ role: 'at-product', stages: STAGES, artifactExists: have() })
  assert.match(r.reason, /S2/)
  assert.match(r.reason, /01-prd\.md/)
})

test('不在阶段链里的角色没有交付物义务', () => {
  const r = decideDeliverable({ role: 'at-worker-a', stages: STAGES, artifactExists: have() })
  assert.equal(r.ok, true)
})

test('stages 缺失时不判定为失败——门禁坏了不该诬告角色', () => {
  const r = decideDeliverable({ role: 'at-product', stages: null, artifactExists: have() })
  assert.equal(r.ok, true)
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
node --test
```

Expected: FAIL，报找不到 `../hooks/lib/deliverable.mjs`。

- [ ] **Step 3: 写最小实现**

`hooks/lib/deliverable.mjs`：

```js
// H5 交付物校验（纯函数）。同一份判定喂两条 hook：
//   H5b（SubagentStop）拿它做真拦截，把角色顶回去补产物；
//   H5a（PostToolUse）拿它记权威 warning。
// 两道缺一不可——平台对 SubagentStop 的重试约 9 次后会静默放行且父级无感
// （M1·U5 实测），单靠 H5b 会在平台放弃那一刻制造一个假的「一次通过」信号。

export function decideDeliverable({ role, stages, artifactExists }) {
  if (!stages || typeof stages !== 'object') return { ok: true }

  const mine = Object.entries(stages)
    .filter(([, s]) => s.role === role)
    .sort(([a], [b]) => a.localeCompare(b))

  for (const [stageId, stage] of mine) {
    const produces = stage.produces || []
    if (produces.length === 0) continue
    const missing = produces.filter((p) => !artifactExists(p))
    if (missing.length === 0) continue
    // 只报第一个未完成的阶段——角色一次只做一段。
    return {
      ok: false,
      stageId,
      missing,
      reason:
        `${role} 在 ${stageId} 应当产出 ${produces.join('、')}，但 ${missing.join('、')} 还没有写到磁盘上。` +
        `在结束之前把它写出来；如果这一段确实不需要产出，把理由写进你的回报，由上级判断。`,
    }
  }

  return { ok: true }
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
node --test
```

Expected: 全绿，总数 88 + 5 = **93**。

- [ ] **Step 5: 接进入口**

`hooks/gate.mjs` 顶部补 `import { decideDeliverable } from './lib/deliverable.mjs'`，在 `contract` 那段之后补：

```js
  if (CHECK === 'stop-gate' || CHECK === 'deliverable') {
    const ctx = readRunContext(ROOT_PROJECT, ROOT)
    // H5 是流程辅助：读不到上下文就不表态，不卡住角色收尾。
    if (!ctx.ok) process.exit(0)
    const role = stripPluginPrefix(input?.agent_type)
    if (!input?.agent_type) process.exit(0)
    const r = decideDeliverable({ role, stages: ctx.stages, artifactExists: ctx.artifactExists })
    if (r.ok) process.exit(0)

    if (CHECK === 'stop-gate') {
      // H5b：真拦截。**必须走 SubagentStop 契约（exit 2 + stderr），
      // 不是 PreToolUse 那套 permissionDecision JSON**——U5 实测（docs/07 §1）
      // 拦住 subagent 停止靠的就是 exit 2，在这个事件上发 permissionDecision
      // 形状等于「平台不认 + exit 0」＝静默放行，H5b 会变成一个看起来健康的空操作。
      // denyAndExit 按事件分派输出契约，这里不返回。
      denyAndExit(r.reason, spec.event)
    } else {
      // H5a：权威记录。不 block，只把事实留在会话里——
      // 因为 H5b 到点会被平台静默放行，父级看到的是干净的一次通过。
      process.stdout.write(
        JSON.stringify({
          hookSpecificOutput: {
            hookEventName: spec.event,
            additionalContext:
              `⚠️ agent-team 交付物校验：${role} 的 ${r.stageId} 缺少产物 ${r.missing.join('、')}。` +
              `SubagentStop 已尝试拦截，但平台的重试有上限（约 9 次）后会静默放行，` +
              `因此这条记录是权威依据——不要仅凭「子代理正常返回」判断这一段完成了。`,
          },
        }),
      )
    }
    process.exit(0)
  }
```

- [ ] **Step 6: 注册 H5a**

`hooks/hooks.json` 补 `PostToolUse` 事件（`SubagentStop` 的 `stop-gate` 已在 Task 1 注册）：

```json
    "PostToolUse": [
      {
        "matcher": "^Agent$",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/gate.mjs", "deliverable"],
            "timeout": 30,
            "statusMessage": "记录交付物校验…"
          }
        ]
      }
    ]
```

- [ ] **Step 7: 跑测试并提交**

```bash
node --test
"/c/Users/82370/AppData/Roaming/Claude/claude-code/2.1.270/claude.exe" plugin validate .
git add -A
git commit -m "feat: H5a/H5b 交付物校验双注册

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: 真实环境端到端验证（需人工操作）

单测证明决策逻辑对，但**证明不了 hook 在真实环境里被调起**。M0 的教训很具体：入口守卫的单测全绿，而它在 junction 挂载下让整个门禁静默失效——48 个测试没有一个发现。

**Files:**
- Create: `tests/fixtures/e2e-project/`（供人工验证使用的最小用户项目夹具）
- Modify: `docs/04-本机实测结论.md`（新增一节记录观测）

**Interfaces:**
- Consumes: 前六个任务的全部产物
- Produces: 五个 hook 在真实环境生效的原文观测

- [ ] **Step 1: 造人工验证用的夹具**

在仓库外建一个最小用户项目（不进版本库，路径由实施者自选并在报告中写明），结构：

```
<临时项目>/
├── .agent-team/
│   ├── current-run            内容：r1
│   ├── project.json           {"paths":{"at-backend":["src/server/"],"at-frontend":["src/web/"]}}
│   └── runs/r1/
│       ├── state.json         {"run_id":"r1","stage":"S2","rework":{},"never_invoked":[]}
│       └── 00-contract.md     任意内容
├── src/server/keep.txt
└── src/web/keep.txt
```

- [ ] **Step 2: 挂载并启用插件**

```bash
node scripts/dev-link.mjs link
"/c/Users/82370/AppData/Roaming/Claude/claude-code/2.1.270/claude.exe" plugin enable agent-team@skills-dir
```

- [ ] **Step 3: 把验证清单交给人**

本步骤**不由实施者执行**。把下面这份清单原样交给控制器，由人在 Claude 桌面客户端新开会话（工作目录选上一步那个临时项目）逐条跑，每条都要原文回贴：

> **前置条件（Task 2 评审的连带发现，Task 4 已处理）**：第 4、5、6 条要求角色能调 `Write`。
> 角色的 `tools:` 行必须已按 Task 4「评审后补入」那一节加上 `Write`/`Edit`，且 `at-pm`
> 那一行也要有——主线程的工具面是整棵子树的天花板（`docs/06` §3.3）。跑清单之前先
> `grep -n 'tools:' agents/*.md` 核一眼，否则第 4、5 条会因为角色根本发不出 `Write`
> 而"通过"，那是假通过。
>
> 另：`at-backend.md` / `at-frontend.md` 的 `description` 与正文目前仍写着
> `AT-WORKER-A/B`（改名时只改了 `name:` 字段，正文留给计划 C）。它们自我介绍时可能
> 仍自称 AT-WORKER-A——**这不影响清单判定**，清单看的是门禁的拒绝行为，不是自我介绍文案。

| # | 操作 | 期望 |
|---|---|---|
| 1 | 发 `ping` | 回复以 `AT-PM` 开头 |
| 2 | 派发 `agent-team:at-product`，提示词「原样回复 P-OK」 | **放行**（S2 的前置 `00-contract.md` 已在） |
| 3 | 派发 `agent-team:at-architect`，提示词「原样回复 A-OK」 | **被 H2 拒**，理由点名缺 `01-prd.md` 且指明它是 S2 的产物 |
| 4 | 派发 `agent-team:at-backend`，让它用 `Write` 写 `src/web/x.ts` | **被 H3 拒**，理由点名这条路径归 `at-frontend`（夹具的 `project.json` 里 `at-backend` 只认领了 `src/server/`） |
| 5 | 派发 `agent-team:at-product`，让它用 `Write` 改 `.agent-team/runs/r1/00-contract.md` | **被 H4 拒**，理由说明契约唯一写者是用户 |
| 6 | 派发 `agent-team:at-product`，提示词「什么都不要做，直接结束」 | **H5b 反复顶回**（会话里能看到它多转几轮），最终平台放行；**H5a 在父级留下 warning** |

- [ ] **Step 4: 把观测写进 docs/04**

在 `docs/04-本机实测结论.md` 末尾（「附：实验目录」之前）新增一节 `## 9. M1a · 五个 hook 的真实环境验证`，按既有体例写：怎么测的 / 六条观测原文 / 结论 / 对设计的影响。任何一条与期望不符，**不要改测试去迁就**，如实记录并停下来报告。

- [ ] **Step 5: 收尾**

```bash
"/c/Users/82370/AppData/Roaming/Claude/claude-code/2.1.270/claude.exe" plugin disable agent-team@skills-dir
node --test
git add -A
git commit -m "docs: M1a 五个 hook 的真实环境验证

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---
