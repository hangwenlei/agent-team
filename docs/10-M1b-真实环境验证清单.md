# M1b 真实环境验证清单 —— 待执行

> 2026-09-17。分支 `m1b-flow`；本文随终审修复轮更新（新增第 8 条）。
> 首版写于 `3eee199..065581e`（18 个 commit、304 条单测 `fail 0`）。
> **本清单尚未执行。** 执行不了的原因与它意味着什么，见 §0。
> 按 `docs/08` §3.1 的要求，对着**当前**的 `roster.json` 与 `stages.json` 重新推演，
> **没有照搬** M1a 那份（那份的第 4、6 条在执行时被证明是错的，改法依赖当时那一版的花名册）。
> 体例照 `docs/04`–`docs/09`：先摆事实，推论单独成段。

---

## 0. 为什么还没执行，以及这意味着什么

### 事实

本机两个引擎都在，但存储的凭据已过期：

    C:\Users\82370\.local\bin\claude.exe                              2.1.237
    C:\Users\82370\AppData\Roaming\Claude\claude-code\2.1.270\claude.exe
    C:\Users\82370\AppData\Roaming\Claude\claude-code\2.1.271\claude.exe

两个都试过 `-p --plugin-dir <repo> --permission-mode acceptEdits "/at-status"`，原样返回：

    Failed to authenticate: OAuth session expired and could not be refreshed

`~/.claude/.credentials.json` 的时间戳是 9 月 1 日。开发会话自己的认证在桌面应用进程里，
子进程借不到。

而**不能就地在开发会话里验**：`docs/04` §9 ③ 实测，`plugin enable` 会接管**正在跑的**会话
（Bash/PowerShell/WebSearch 当场消失），且 `plugin disable` 之后同一会话的工具面**不恢复**
——工具面在会话生命周期内是固定的。

### 顺带更正 `docs/04` 末尾的一处

`docs/04` 记「`%APPDATA%\Claude\claude-code\<版本>\claude.exe` 在用户自己的 PowerShell 里
**不存在**，只有本会话的工具侧能看到」。这次在工具侧确认它**存在**（2.1.270 与 2.1.271 两个
版本目录都在）。所以那条不是「路径是假的」，是**两侧看到的文件系统视图不同**，原文
「两侧为何不一致本轮没有查清」仍然成立。

### 零成本已确认的（唯一一条真实证据）

    claude --plugin-dir <repo> plugin details agent-team

    Skills (4)  at, at-init, at-resume, at-status
    Agents (6)  at-architect, at-backend, at-frontend, at-outsider, at-pm, at-product
    Hooks (3)   PreToolUse, SubagentStop, PostToolUse
    Always-on:  ~105 tok；/at 按次调用约 780 tok，其余三条 230–270 tok

**四条命令确实被注册了，三类 hook 事件也都注册了。** 但注意两点：平台把 `commands/` 归在
**Skills** 名下，所以**实际调用名**（`/at-init` 还是 `/agent-team:at-init`）这一步看不出来；
这条是静态清单，**不证明任何一条在运行时会被触发**。

### 推论

> **这条分支 304 条单测证明的是「给定输入，判定是否正确」，一条都不证明「这段代码在真实
> 系统里会被调用到」。**

这正是 `docs/08` §2.5 记的那个结构性限制，也是 M1a 终审 I5 的原话。M1a 那次靠真实环境跑一遍
抓到了前三层**全部漏掉**的一处缺陷（⑦：`at-pm` 根本派不动 `at-backend`）。本轮尚无对应证据。

**最承重的一条**（计划开头已单列）：`additionalContext` 在 `PostToolUse` / matcher
`^(Edit|Write|NotebookEdit)$` 上对主会话是否可见。本项目**实测见过**的是同一事件的 `^Agent$`
matcher（`docs/04` §9 ④：PM 当时在拿上一轮的 H5a 警告推断 run 状态）。换 matcher 是**推断**。
它若不成立，`ledger` 整条回传通道变成一个看起来健康的空操作——契约哈希、触达表、状态校验
问题都到不了 PM 手里，而 PM 很可能**自己编一个格式正确的 sha256 填进 `state.json`**，
`validateState` 的形状校验挡不住。第 3 条专验它。

---

## 1. 执行前置

### 1.1 环境

**必须在新开的会话里做**，不要在正在跑的开发会话里启用插件（见 §0）。两条路任选：

- **交互**：在桌面应用里新开一个会话，工作目录指向下面的探针工程，启用插件。
- **非交互**：先让 CLI 重新认证，然后

      cd <探针工程>
      claude -p --plugin-dir "C:/Users/82370/Desktop/Agent-Team" \
             --permission-mode acceptEdits \
             --output-format stream-json --verbose \
             "<要跑的命令>"

  `--plugin-dir` 只为这一次会话加载插件，**不污染全局配置**，也不接管别的会话。
  `--output-format stream-json` 能看到 hook 的原始输出，第 3 条要靠它。

**引擎版本要记下来。** 本机 CLI 是 2.1.237，桌面端是 2.1.270/2.1.271，规格记的运行环境是
**2.1.270**。用哪个跑就记哪个，**不要把 2.1.237 的结论当成 2.1.270 的结论**。

### 1.2 探针工程

一个空的临时目录（**不要用本仓库**，`.agent-team/` 会污染开发环境）：

    probe/
      README.md
      package.json         # scripts: { build, test }
      src/server/api.ts
      src/web/App.tsx
      docs/

### 1.3 一条会影响判读的事实

`agents/at-pm.md` 的**正文仍然是 M0 占位版**（「你是 AT-PM（M0 占位版）…只做被明确要求的
动作，不自作主张」）。真角色正文属 M1c。

`docs/04` §9 ⑤ 实测过：**两行占位正文就长出了拒绝行为**——占位 PM 两次拒绝执行（一次拒绝帮
`at-backend` 绕花名册，一次拒绝在无备份前提下不可逆覆盖契约）。

**所以：如果某一条的现象是「PM 没有动手」，先分清是门禁拦的、还是 PM 自己不肯做。**
这两者在会话里看起来一模一样——与 `docs/08` ④ 是同一个形状。

---

## 2. 逐条

每条三样：**怎么跑** / **预期** / **独立证据**。
「独立证据」一栏是硬要求：`docs/08` §3 第 5 问——断言错了如果「看起来正常」，就必须有
不依赖会话转述的证据（磁盘状态、原始 JSONL 字段、退出码）。

### 第 1 条 · 命令能不能解析，实际叫什么名字

- **跑**：四条命令各敲一次。先试裸名（`/at-init`），不行再试全限定名（`/agent-team:at-init`）。
- **预期**：至少一种形式被识别并执行。
- **独立证据**：把**实际可用的那个名字**原样记下来。四条都记。
- **为什么重要**：`docs/04` §4 已经定过「阶段启动必须由 slash command 触发」，但本项目从未
  实测过 slash command 本身。`plugin details` 把它们列在「Skills」名下，命名规则未知。

### 第 2 条 · `$ARGUMENTS` 是否生效，契约是否逐字

- **跑**：`/at 做一个登录页，支持手机号+验证码`
- **预期**：`00-contract.md` 第 1 节**逐字**含这句话。
- **独立证据**：`cat .agent-team/runs/*/00-contract.md`，与你输入的那句话**逐字符比对**。
- **注意**：这一条会起一趟完整的 run。规格 §10 U6 的推算是 **100 万–250 万 token/趟**。
  只想验 `$ARGUMENTS` 的话，跑到契约写完就可以中断。

### 第 3 条 · ⭐ `additionalContext` 在 `^(Edit|Write|NotebookEdit)$` 上是否可见

**这是整条分支最承重的一条。**

- **跑**：`/at-init`。PM 会勘察工程、写 `.agent-team/project.json`，随后应当收到一段以
  「agent-team 账本回传」开头的上下文，里面有触达表 JSON，并被要求原样写进
  `.agent-team/reach.json`。
- **预期**：`.agent-team/reach.json` 存在，且内容是**算出来的**而不是 PM 编的。
- **独立证据**（不要只看 PM 说了什么）：

      node -e "import('file:///C:/Users/82370/Desktop/Agent-Team/hooks/lib/reach.mjs').then(async m=>{const fs=await import('node:fs');const roster=JSON.parse(fs.readFileSync('C:/Users/82370/Desktop/Agent-Team/roster.json','utf8'));const proj=JSON.parse(fs.readFileSync('.agent-team/project.json','utf8'));console.log(JSON.stringify(m.computeReach({roster,paths:proj.paths}),null,2))})"

  把输出与磁盘上的 `.agent-team/reach.json` **逐字符比对**。
  用 `--output-format stream-json` 的话，还能直接看到 hook 那条 `additionalContext` 的原文。

- **判读规则**（先写下来，不要临场决定）：
  - 两者**逐字符相等** → 假设成立。
  - `reach.json` **不存在**、或 PM 完全没提到账本回传 → **假设不成立**，走计划开头写好的
    退路：给 `at-pm.md` 的 `tools:` 加 `Bash`，把 `hooks/lib/ledger.mjs` 的同一批纯函数包成
    `scripts/at-ledger.mjs`，四条命令改为显式调用它。**纯函数一行不用改**——这正是把计算
    放在 `hooks/lib/` 而不是写死在 `gate.mjs` 里的原因之一。
    走这条退路必须回头重新评估 `docs/09` 账一 #2（PM 有了 `Bash` 就能经 §6.2 的软约束口子
    伪造阶段产物），结论写进本文件。
  - `reach.json` **存在但内容不等** → 假设不成立**且更糟**：PM 在编。同样走退路，另外单记
    一条——这说明「PM 会照抄回传」这个前提本身不可靠，不只是通道问题。

### 第 4 条 · 账一生效：PM 能写控制文件

- **跑**：第 2、3 条跑完之后看磁盘。
- **预期**：`.agent-team/current-run`、`.agent-team/project.json`、`.agent-team/reach.json`、
  `.agent-team/runs/<id>/state.json` 四个文件都在。
- **独立证据**：`ls .agent-team/` 与 `ls .agent-team/runs/*/`。
- **⚠️ 必须分清两条路径**（它们是两条不同的规则，别把其中一条的成功当成另一条生效）：
  - **建第一个 run 时** `state.json` 还不存在 → `readRunContext` 返回 `kind:'unreadable'`
    → 放行 PM 的是 `gate.mjs` 里 M1a 留下的 **I2 豁免**。这条路径 stderr 会留一行
    「读不到运行上下文…调用者是 PM」。
  - **run 建好之后**（`ctx.ok`）放行它的才是本轮新加的**控制文件规则**。
  - 这正是 `docs/08` ② ⑤ ⑦ 三次栽过的「拦是拦住了，但拦它的是另一道闸」的反面。
    有 `stream-json` 就看 stderr 有没有那行；没有就跑第二次写入（run 已存在时）来区分。

### 第 5 条 · 账一 #2：PM 仍然不能伪造阶段产物

- **跑**：run 建好之后，明确要求 PM 直接写 `.agent-team/runs/<id>/01-prd.md`。
- **预期**：被 H3 拒，理由形如「这是 S2 的产物（`01-prd.md`），归 `at-product`」。
- **独立证据**：拒绝原文里**不得**出现「控制文件」四个字（出现就说明走错了分支）；
  且 `ls` 确认文件**确实没有**被建出来。
- **前置核对**：`at-pm` 的 produces 只有 `00-contract.md`(S1) 与 `04-dispatch.md`(S4)，
  `01-prd.md` 归 S2/`at-product`——这三条来自当前 `stages.json`，跑之前再看一眼没变。
- **⚠️ 判读陷阱**：若 PM **自己拒绝动手**（占位正文的谨慎，见 §1.3），那不是 H3 生效。
  要明确要求它动手，并看拒绝理由是谁给的。

### 第 6 条 · H5 按 `state.stage` 判定的真拦截

这一条的串联关系最长，**三条前置全部要先核**（`docs/08` §3 第 1、2 问；M1a 的 ⑤ ⑦ 就死在这）：

| # | 前置 | 为什么 |
|---|---|---|
| 1 | 夹具里 `03-arch.md` + `04-dispatch.md` **必须已在磁盘上** | 它们是 S5 的 `requires`，缺了会被 **H2** 先拒，H5 根本轮不到表态 |
| 2 | **必须经 `at-architect` 派** `at-backend` | `at-pm.can_delegate_to` 只有 `at-product`/`at-architect`；直接派会被 **H1** 拒，而那是 H1 判对了 |
| 3 | `state.json` 的 `stage` **必须是 `S5`** | 本轮新前提：H5 按 `state.stage` 判定，停在别的阶段会返回 `skipped: 'role-not-in-stage'` 并**哑掉** |

- **跑**：夹具备好（`00-contract.md`/`01-prd.md`/`03-arch.md`/`04-dispatch.md` 在，
  `05-impl/at-backend.md` **不在**，`state.stage = 'S5'`），经 `at-architect` 派 `at-backend`，
  让它**不要**写产物就结束。
- **预期**：H5b 用 `exit 2` 把它顶回去（平台重试上限实测约 9 次，见 `docs/07` §1）。
- **独立证据**：**原始 JSONL 里那次 `SubagentStop` 的退出码与 stderr**，以及 H5a 那条
  `additionalContext`。**不要只记会话里的转述。**
- **⚠️ 判读陷阱**：这一条错了会看到「**没有发生拦截**」——与 `docs/08` ⑥ 同型的假通过。
  所以反面也要验：把 `state.stage` 改成 `S2` 再跑一次，H5a 应当发出「刚返回的 `at-backend`
  不是当前阶段的执行者、**而且它也派不到那个执行者**，所以这次校验**没有意见**」那条
  warning。**两次都做，才算验到。**
- **⚠️ 第二个判读陷阱（终审 C4 新增）**：**不要拿 `at-architect` 来验这条反面。**
  `state.stage = 'S5'` 时 `at-architect` 返回是**正路**（它能传递地派到 `at-backend`，
  是一个合法的协调者），H5a 现在对它**静默**——那不是「H5a 没生效」。要验 warning 还在，
  得用一个**派不到**当前阶段执行角色的角色，`at-outsider` 是现成的
  （`can_delegate_to` 是空）。

### 第 7 条 · PM 手里有没有 `AskUserQuestion`（执行中新增）

这条不在原计划里。它来自 Task 9 的人工走读：`agents/at-pm.md` 的 `tools:` 行里原本**没有**
`AskUserQuestion`，而 `/at` 的五类强制升级硬性要求它。规格自己是写了的（§7 目录树、§3.1、
§5.3、§9 测试策略第 4 条），只是没传播到文件里，也没有任何测试守。已补（`5df992e`），并加了
一条闭包测试（`tests/command-tool-closure.test.mjs`）。

**但那条测试证明的是「工具名在 `tools:` 行里」，不是「PM 真的能调用它」。**

- **跑**：构造一个契约有洞的需求（例如 `/at 做个后台` 这种缺关键信息、任一猜测都可能白做的），
  看 PM 会不会用 `AskUserQuestion` 打断。
- **预期**：PM 发起一次带选项的提问，而不是自己猜。
- **独立证据**：原始 JSONL 里有一次 `AskUserQuestion` 的 `tool_use`。
- **注意**：占位正文的 PM 未必会升级（§1.3）。**这一条主要验「工具在不在工具面里」——
  报错形如 `AskUserQuestion is disabled for this session` 就是明确的否**；至于 PM 判不判得出
  该升级，那属 M1c 的角色正文，不在本清单的范围内。

### 第 8 条 · ⭐ 命令读不读得到插件内部的文件（终审 C3 新增）

**这一条零成本可验，而且不需要跑完一趟 run。**

四条命令都要 PM 去读**插件自带**的文件：`${CLAUDE_PLUGIN_ROOT}/templates/*`、
`${CLAUDE_PLUGIN_ROOT}/stages.json`、`${CLAUDE_PLUGIN_ROOT}/roster.json`。终审前它们
写的是**裸相对路径**（`templates/state.json`、「插件的 `stages.json`」），而裸相对路径按
**会话工作目录**解析，插件目录不在用户项目里——这是一条记了三次、从未被回答的承重假设
（`docs/02` P0-#1/#2、`docs/03` P0-3、`docs/04` §6 开放问题 3：「插件根在工作目录之外，
要不要 `/add-dir` 没有一行文档确认」）。已改成 `${CLAUDE_PLUGIN_ROOT}` 前缀，但
**「这个变量在命令正文里会不会被展开、展开之后 `Read` 够不够得着」本项目从未实测过。**

- **跑**（探针工程里，`.agent-team/` 可以是空的）：

      claude -p --plugin-dir "C:/Users/82370/Desktop/Agent-Team" \
             --output-format stream-json --verbose \
             "/at-status"

  `/at-status` 是四条里最轻的一条（只读不写，约 230–270 tok），而它第 1 节就要求读
  `${CLAUDE_PLUGIN_ROOT}/stages.json`。

- **预期**：PM 发起一次 `Read`，目标路径是**展开后的插件目录**下的 `stages.json`，
  并且读到了 S1–S5 五个阶段。
- **独立证据**（不要只看 PM 转述的阶段名——它可能是从别处猜的）：
  - `stream-json` 里那次 `Read` 的 `tool_use` **参数原文**：`file_path` 是不是真实的
    插件目录绝对路径，还是字面量 `${CLAUDE_PLUGIN_ROOT}/stages.json`（没展开）；
  - 配对的 `tool_result` 是不是 `is_error: true`。
- **判读规则**（先写下来）：
  - `tool_use` 的路径已展开、`tool_result` 不是 error → 假设成立，四条命令的插件侧
    引用可用。
  - 路径**没展开**（原样是 `${CLAUDE_PLUGIN_ROOT}/…`）或 `tool_result` 报找不到文件
    → 假设不成立。退路两条，择一并把理由写进 `docs/12`：
    (1) 命令正文改为不依赖读插件文件——把阶段链与模板骨架**内联**进命令正文
        （代价：`stages.json` 不再是唯一真源，规格 §7 那条要重新论证）；
    (2) 让 `/at-init` 把插件侧那几份文件复制进 `.agent-team/`，命令只读用户项目侧
        （代价：多一份拷贝会分叉，与 `hooks/lib/control-files.mjs` 的单一真源立场冲突）。
  - PM **完全没去读**那个文件（既没有成功也没有失败的 `Read`）→ 这是第三种结果，
    单独记：那说明问题不在路径而在正文没被照做（`docs/10` §3 观测项 2 是同一条）。
- **⚠️ 判读陷阱**：这一条错了会看到「PM 照样报出了阶段」——它完全可以从命令正文里
  那几句 `S2 产品设计`、`S5 实现` 反推，或者干脆编。**看 `tool_use` 原文，不看结论。**
  与 `docs/08` §3 第 5 问是同一条要求。
- **顺带**：`tests/commands.test.mjs` 有一条「命令正文引用插件自带的文件时必须带
  `${CLAUDE_PLUGIN_ROOT}` 前缀」。它证明的是**正文写对了**，不是**读得到**——
  与第 7 条那对关系（工具名在 `tools:` 行里 ≠ PM 真能调用）完全同型。

---

## 3. 顺带观测项（清单没设计要测，但撞见了要记）

`docs/04` §9 ① 那条最有价值的发现（`at-product` 被 H3 拒绝后**自发**改道派给路径拥有者）
就是这么来的——不在任何清单里。以下三条是**已知可能出现、但没有设计成断言**的：

1. **`ledger` 的阶段提示会不会刷屏。** `state.stage` 停在旧阶段时，run 目录下**每一次**写入
   都会重放那段「产物已齐、该推进了」的提示。是有用的冗余还是噪音，只有跑一趟真实 run 才
   判得出来（Task 6 评审留的 ⚠️）。
2. **命令正文的长度代价。** `plugin details` 报 `/at` 按次约 780 tok。真实会话里 PM 是否真的
   照着正文的顺序做，还是只取了个大意。
3. **PM 对 run 状态的认知会不会和磁盘脱节。** `docs/04` §9 ④ 记过一次真实误报（PM 拿上一轮的
   警告推断当前状态，而文件已经被用户在终端建出来了）。本轮 `/at` 与 `/at-resume` 的正文都
   写了「去磁盘核实，不要凭子代理回报」，看它照不照做。

---

## 4. 本清单自己过一遍 `docs/08` §3 五问

| 问 | 本清单的答案 |
|---|---|
| 1. 这条路上 X 之前还有谁会先表态？ | 第 6 条的三条前置（H2 的 requires、H1 的花名册边、`state.stage`）已逐条列进表格；第 5 条核过 `at-pm` 的 produces 只有 S1/S4 两条 |
| 2. 触发 X 需要的工具/角色/产物此刻真的存在吗？ | 第 7 条就是为这一问新增的（`AskUserQuestion` 原本不在工具面里）；第 6 条的夹具状态写死在「跑」那一栏 |
| 3. 我引用的实测结论是这一版的吗？ | `docs/04` §9 ③⑤、`docs/07` §1 的重试上限、`docs/04` §4 的 slash command 结论，均已回原文核对；§0 顺带更正了 `docs/04` 末尾一处；第 8 条回核了 `docs/02` P0-#1/#2、`docs/03` P0-3、`docs/04` §6 开放问题 3 —— 三处都只**记了**「插件根在工作目录之外」，没有一处**答过**它 |
| 4. 我为了修 A 而改的东西有没有动到 B 的前提？ | 第 6 条的前置 3（`state.stage`）**正是本轮修 H5 引入的新前提**，已单列；第 4 条的两条路径区分同理 |
| 5. 这条断言如果错了我会看到什么？ | 第 3、5、6、7、8 条各写了判读陷阱与独立证据。**第 3、6、8 条的错误形式都是「看起来正常」**，所以它们的证据要求最高（逐字符比对 / 原始 JSONL 退出码 / `tool_use` 参数原文） |

---

## 5. 执行之后

把**实测结论**写成 `docs/12-M1b-实测结论.md`，体例照 `docs/04`–`docs/09`。至少要有：

- 八条的实测结果表，含**实际可用的命令名**与**实际用的引擎版本**
- 第 3 条的判读与它对执行面裁定的影响（成立 / 走退路）
- 任何清单里**没设计要测、但撞见了**的事
- 不成立的假设要**明确写成不成立**，不要含糊过去

本文件保留为「当时是怎么推演的」的记录，不要回头改它的预期去迁就实测结果。
